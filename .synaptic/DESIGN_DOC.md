# DESIGN_DOC.md - SENTINEL

## Architecture Decisions

| ID | Decision | Option Selected | Date | Rationale |
|----|----------|-----------------|------|-----------|
| DG-001 | Estructura física del monorepo OSS/Pro | **Option B** — monorepo único con workspaces marcados OSS/Pro + `publish-oss.ts` allowlist · **AMENDED por DG-066 B (2026-05-23):** bajo el giro estratégico a "full OSS Apache-2.0" no hay código Pro que filtrar; `publish-oss.ts` (nunca implementado) queda **obsoleto**. La decisión arquitectónica fue correcta para la estrategia de entonces (OSS+Pro split); el cambio es estratégico, no arquitectónico. | 2026-05-20 | Velocidad de solo founder; refactors triviales; anti-filtración en un script auditable |
| DG-002 | Creación del repositorio GitHub | **Option A** — `git init` local ahora; repo remoto GitHub diferido | 2026-05-20 | Evita infra remota prematura; respeta v0.4 §8.4 LOCKED (privado hasta MVP) |
| DG-003 | Estrategia de binarios de scanners | **Option A** — binarios nativos pinneados, descarga on-demand | 2026-05-20 | El producto debe correr sin Docker en el cliente; dev espeja prod |
| DG-004 | Reuso de `@synaptic-sre/enforcement` | **Option A** — referencia conceptual; reimplementación idiomática | 2026-05-20 | Reuso literal bajo; Sentinel autónomo y sin deuda de licencia |
| DG-005 | Alcance de la generación de scaffolding | **Option B** — esqueleto completo: 7 paquetes declarados con headers OSS/Pro | 2026-05-20 | Hace explícito y auditable el split OSS/Pro desde el día uno |
| DG-006 | Linter / formatter | **Option B** — ESLint flat (typescript-eslint) + Prettier en el scaffolding | 2026-05-20 | Un producto de calidad/seguridad de código debe tener linter real desde el primer commit |
| DG-007 | Desbloqueo del commit (interferencia de antivirus) | **Option A** — exclusión de la carpeta del proyecto en el antivirus (Norton 360) | 2026-05-20 | Ataca la causa raíz; desbloquea commit, `pnpm install` y descarga futura de scanners |
| DG-008 | Alcance del primer increment de PASO 4 | **Option A** — contrato primero: interfaz `ScoutAgent` + tipos compartidos antes del wrapper | 2026-05-20 | El contrato es el cimiento de core/scouts/agents/reporters; fijarlo y testearlo primero de-risk-ea el resto |
| DG-009 | Alcance del segundo increment de PASO 4 | **Option A** — adquisición del binario primero (`scripts/install-scanners.ts`) | 2026-05-20 | La descarga bajo Norton (TLS, checksums, cross-platform) es la pieza de mayor riesgo de entorno; aislarla deja el wrapper como lógica pura |
| DG-010 | Alcance del tercer increment de PASO 4 | **Option A** — `OpenGrepScout` + normalizer, unit-tested contra salida JSON real | 2026-05-20 | Aísla el último unknown (CLI/JSON de OpenGrep); normalizer testeado contra output real capturado |
| DG-011 | Cuarto increment de PASO 4 | **Option B** — ruleset curado + fixtures JS/TS+Python + tests de integración | 2026-05-21 | Cierra PASO 4 completo con detección real en los dos lenguajes LOCKED del MVP |
| DG-012 | Próximo paso del roadmap | **Option B** — Coordinator stage 1 + persistencia en `colony.db` | 2026-05-21 | La columna vertebral: scan → dedup → persiste; activa el schema de feromonas inerte |
| DG-013 | Driver SQLite para `colony.db` | **Option A** — `node:sqlite` (módulo integrado de Node) | 2026-05-21 | Cero dependencias y cero ABI nativa (elimina la fricción del módulo nativo en la extensión VSCode); síncrono + WAL. Desviación informada del v0.4 §9.4, que dijo "validar" |
| DG-014 | Alcance del Coordinator stage 1 | **Option A** — Coordinator stage 1 estricto + refactor `ScoutAgent`→`core` | 2026-05-21 | Alcance literal de DG-012 B; el refactor del contrato a `core` evita la dependencia circular `core`↔`scouts` |
| DG-015 | Próximo paso del roadmap | **Option A** — CLI `synaptic-sentinel scan` | 2026-05-21 | Convierte el pipeline interno en algo invocable por una persona; integra core+scouts+colony en un punto de entrada real |
| DG-016 | Próximo paso del roadmap | **Option B** — reporters: modelo del tomo + export JSON | 2026-05-21 | Cierra el ciclo scan → artefacto entregable; en el camino crítico de la definición de "Done" del MVP (§8.1) |
| DG-017 | Próximo paso del roadmap | **Option A** — `GitleaksScout` (en 2 increments: A.1 install-scanners + comprimidos, A.2 el scout) | 2026-05-21 | Cobertura de secrets, riesgo bajo (patrón probado); Gitleaks ships archivos → A.1 agrega extracción a `install-scanners` |
| DG-018 | Enfoque del `GitleaksScout` (DG-017 A.2) | **Option B** — scout completo de punta a punta + fixture con un secreto + test de integración (Gitleaks real) + wiring en el CLI | 2026-05-21 | Cierra la cobertura de secrets dejándola operativa: el `Coordinator` corre OpenGrep **y** Gitleaks, validado contra el binario real |
| DG-019 | Próximo paso del roadmap | **Option A** — `Coordinator` stage 2: dedup + `fp_known` + `lifecycleState` en re-scans | 2026-05-21 | Endurece el pipeline antes de sumar scouts o brain layer; un re-scan ya no duplica feromonas y respeta los falsos positivos confirmados |
| DG-020 | Próximo paso del roadmap | **Option B** — extensión VSCode MVP (comando Scan + diagnostics inline) | 2026-05-21 | El producto es VSCode-primary y solo existía como CLI; surfacea el pipeline determinista donde el dev trabaja |
| DG-021 | Arquitectura de la extensión VSCode MVP | **Option A** — spawn-CLI: la extensión lanza la CLI como child process, lee el tomo y pinta diagnostics | 2026-05-21 | Vuelve irrelevante el Node del extension host (FI-001); la CLI es la única fuente de verdad de ejecución; extensión delgada |
| DG-022 | Próximo paso del roadmap | **Option B** — cierre del lazo Inline UX: comando CLI `mark-fp` + Code Action "marcar falso positivo" + status bar | 2026-05-21 | DG-019 dejó el consumo de `fp_known` sin vía de creación; B cierra ese lazo end-to-end (cierra FI-007) |
| DG-023 | Próximo paso del roadmap | **Option A** — reporter HTML del tomo + `suppressedCount` en el resumen | 2026-05-21 | El tomo es el deliverable del producto pero solo existía en JSON; HTML lo vuelve un informe real (cierra FI-006) |
| DG-024 | Próximo paso del roadmap | **Option B** — Brain Layer increment 1: paquete `agents` (contrato `BrainAgent` + `LlmClient` + `AnthropicLlmClient` BYOK + Triage Agent mínimo) | 2026-05-21 | El Brain Layer es el diferenciador premium; el increment 1 lo arranca con cimientos verificables. Desviación informada del v0.4 línea 695: cliente Anthropic propio vía `fetch` por testabilidad total (ver FI-009) |
| DG-025 | Próximo paso del roadmap | **Option A** — Brain Layer increment 2: comando CLI `triage` (corre el Triage Agent, pheromone-aware, persiste veredictos) | 2026-05-21 | El paquete `agents` no tenía consumidor; el wiring lo vuelve usable. Tabla dedicada `triage_verdicts` (schema v2 aditivo) en vez de un tipo de feromona — cero riesgo de migración. Desviación informada del v0.4 línea 215 |
| DG-026 | Próximo paso del roadmap | **Option A** — surface del triage en el tomo (cada hallazgo lleva su veredicto; `byTriage` en el resumen) | 2026-05-21 | Los veredictos se persistían pero eran invisibles fuera de la consola; surfacearlos en el deliverable (tomo JSON+HTML) completa el valor del Brain Layer |
| DG-027 | Próximo paso del roadmap | **Option B** — triage en la extensión VSCode: BYOK vía `SecretStorage` + comando de triage + veredictos en los diagnostics | 2026-05-21 | El producto es VSCode-primary y el triage solo era alcanzable por CLI; el v0.4 §487 incluye "BYOK configurable vía VSCode SecretStorage" en la definición de "Done" del MVP |
| DG-028 | Próximo paso del roadmap | **Option B** — Context Agent: 2.º agente del Brain Layer (explicación contextualizada de los true positives) | 2026-05-21 | El Brain Layer con un solo agente era delgado; el Context Agent reutiliza el contrato `BrainAgent` ya de-riskeado (DG-024) y profundiza el diferenciador premium |
| DG-029 | Próximo paso del roadmap | **Option A** — wire del Context Agent: el comando `triage` lo corre sobre los TP; explicaciones persistidas (schema v3) y surfaceadas en el tomo | 2026-05-21 | El Context Agent estaba construido sin consumidor; el wiring completa DG-028 reutilizando los patrones probados de DG-025 y DG-026 |
| DG-030 | Próximo paso del roadmap | **Option A** — `TrivyScout` (cuarto scout, cobertura SCA — dependencias vulnerables) | 2026-05-21 | El Scout Layer cubría SAST y Secrets pero no SCA; Trivy es bajo riesgo (patrón de scout probado ×2) y el Brain Layer aplica gratis a sus hallazgos |
| DG-031 | Próximo paso del roadmap | **Option A** — `CheckovScout` (quinto scout, cobertura IaC — misconfiguraciones en Dockerfile/Terraform/k8s) | 2026-05-21 | El Scout Layer cubría SAST, Secrets y SCA pero no IaC; patrón de scout probado ×4 y el binario standalone de Checkov sortea la observación "Python 3.14" |
| DG-032 | Próximo paso del roadmap | **Option B** — `VibeDetectScout` (detección de código *vibe-coded* — anti-patrones de código generado por IA, categoría `VibeCoded`) | 2026-05-21 | El Scout Layer cubría SAST/Secrets/SCA/IaC pero no el diferenciador del producto; detección determinista nativa (sin binario OSS) = 100% verificable sin API key; la categoría `VibeCoded` ya existía en el schema sin uso |
| DG-033 | Próximo paso del roadmap | **Option A** — Remediation Agent (3.er agente del Brain Layer — propone cómo corregir un verdadero positivo) | 2026-05-21 | El Brain Layer tenía 2 de 3 agentes; el Remediation Agent reusa el contrato `BrainAgent` probado ×2 y con la llamada LLM verificada → riesgo bajo; completa el trío Triage→Context→Remediation |
| DG-034 | Próximo paso del roadmap | **Option B** — surface del Brain Layer completo en la extensión VSCode (hover con triage/contexto/remediación + Code Action de remediación + `.vscode/launch.json`) | 2026-05-21 | El Brain Layer completo solo era visible en el tomo/CLI; surfacearlo en el IDE (superficie primaria) y desbloquear el F5 era el paso natural de riesgo medio |
| DG-035 | Próximo paso del roadmap | **Option A** — documento de onboarding (`ONBOARDING.md` + README al día + `docs/colony-db.md` v4) | 2026-05-21 | Con la extensión F5-testeable, documentar el uso es un deliverable real del roadmap "Output & Polish" con riesgo mínimo; vuelve el producto usable sin leer el código |
| DG-036 | Próximo paso del roadmap | **Option B** — kill-switch del Coordinator: presupuesto de tiempo por scout (v0.4 §9.6) | 2026-05-21 | Un scanner colgado bloqueaba el scan entero; el kill-switch es un requisito del threat model ("Rogue Agents") y la inversión correcta en robustez antes del beta |
| DG-037 | Salida verbose dinámica del scan (UX techie/hacker) | **Option B** — increment 1/3: la CLI emite el "show" (banner + drip por scout + reveal coloreado); increments 2-3 (pseudoterminal en la extensión, webview "tomo vivo") pendientes | 2026-05-21 | La prueba F5 reveló que la extensión no daba feedback del proceso (gap no cubierto por v0.4 §4.3); la CLI como motor del show beneficia también al público OSS/terminal con un solo esfuerzo |
| DG-038 | UX verbose increment 2/3 | **Option B** — la extensión transmite el "show" de la CLI a un Pseudoterminal nativo (Scan + Triage); el comando `triage` de la CLI se enriquece | 2026-05-21 | Cierra la arquitectura "un show, dos superficies" con una API nativa (Pseudoterminal, sin webview — respeta Decisión #23); el feedback del proceso pasa a verse en el IDE |
| DG-039 | UX verbose increment 3/3 | **Option B** — webview "tomo vivo": panel lateral con los hallazgos agrupados por severidad, clickeables para saltar al código (v0.4 §4.3) | 2026-05-21 | Cierra DG-037 B con la superficie de exploración de resultados que pedía v0.4 §4.3; el webview está sancionado por §4.3 pese a la preferencia general por APIs nativas |
| DG-040 | Próximo paso del roadmap (la colonia aprende) | **Option B** — `learning_records` increment 1 (lado escritura): el triage registra el patrón generalizado de cada veredicto decisivo | 2026-05-21 | Activa el corazón conceptual del producto (la memoria del enjambre, v0.4 §3.5), dormido desde el inicio; increment de escritura acotado y verificable de-riskea el lado lectura |
| DG-041 | Próximo paso del roadmap (la colonia aprende — lado lectura) | **Option B** — `learning_records` increment 2 (lado lectura): el triage pre-clasifica los patrones conocidos sin llamada LLM (economía de tokens, v0.4 §187) | 2026-05-21 | Sin el lado lectura el aprendizaje quedaba registrado pero inerte; con él cada scan se beneficia de los anteriores. Cierra "la colonia aprende" |
| DG-042 | Próximo paso del roadmap (higiene del repo) | **Option A** — `pnpm format` sobre 44 archivos con drift + nuevo script raíz `verify` (`format:check && lint && build && test`) que codifica el gate por ciclo | 2026-05-21 | El drift se acumuló porque `format:check` nunca estuvo en el gate; el ciclo de higiene lo cierra y materializa el gate como un comando. Cierra FI-010 |
| DG-043 | Próximo paso del roadmap (ampliar el catálogo SAST) | **Option B** — el ruleset baseline de OpenGrep pasa de 4 a 11 reglas pattern-based deterministas (CWE-95/78/79/502/327) + 2 fixtures vulnerables + 2 tests de integración | 2026-05-22 | El catálogo SAST es el core value prop del scanner; expandirlo mejora directamente lo que el producto encuentra. Solo patrones de alta confianza (taint analysis diferido); `pattern-not` acota falsos positivos |
| DG-044 | Próximo paso del roadmap (reporter SARIF) | **Option B** — reporter SARIF 2.1.0 del tomo (`renderTomoSarif`) + flag CLI `--export-sarif`; reglas deduplicadas, severidad→level + `security-severity`, `fingerprint`→`partialFingerprints` | 2026-05-22 | SARIF es el formato estándar de intercambio de hallazgos; el export vuelve a Sentinel CI-native (GitHub Code Scanning, Azure DevOps). Ya estaba previsto como formato OSS en el `index.ts` de `reporters` |
| DG-045 | Próximo paso del roadmap (política de exit code para CI) | **Option B** — flag `scan --fail-on <severidad>`: exit code 2 si hay hallazgos ≥ umbral; `countBlockingFindings` (helper puro), exit 2 distinto del 1 (error), flag opt-in | 2026-05-22 | Completa la historia CI-native que abrió SARIF: un scanner que no puede hacer fallar un pipeline es media herramienta. Opt-in preserva el contrato exit 0 del `scan` (la extensión VSCode no se ve afectada — verificado) |
| DG-046 | Próximo paso del roadmap (separar test:unit / test:integration) | **Option A** — la suite Vitest se divide en dos proyectos (`test.projects` `unit` / `integration`); el gate por ciclo (`verify`) pasa de `test` a `test:unit` | 2026-05-22 | Cierra FI-002 (señalada 3 ciclos): la integración dominaba el `verify`. Split por proyecto Vitest (no por glob en scripts) — robusto cross-platform. De-riskea el ciclo de packaging que debería seguir |
| DG-047 | Próximo paso del roadmap (normalizar el ruleId de OpenGrep) | **Option A** — `canonicalRuleId` (ex `deriveTitle`) descarta el prefijo de ruta del `check_id`; el `Finding.ruleId` queda canónico | 2026-05-22 | Cierra FI-005: un `ruleId` canónico estabiliza el `patternSignature` de la memoria del enjambre y limpia el `rule.id` del SARIF. En el mismo ciclo el usuario abrió el requisito de salida en inglés → FI-011 |
| DG-048 | Próximo paso del roadmap (FI-011 etapa 1 — salida de la CLI en inglés) | **Option A** — toda la salida de la CLI (banner, `console-reporter`, `USAGE`/ayuda, errores, mensajes de `scan`/`triage`/`mark-fp`) migra a inglés | 2026-05-22 | Primer increment de FI-011; ataca la superficie más visible con bajo riesgo. Sequencing: el producto debe hablar inglés antes de empaquetar el beta. Etapas 2-5 quedan para DGs futuros |
| DG-049 | Próximo paso del roadmap (FI-011 etapa 2 — mensajes de los scouts en inglés) | **Option A** — los 11 `message` de `sentinel-baseline.yaml`, `title`+`message` de los 6 detectores de Vibe-Detect, y el glue de los normalizers de Trivy/Checkov migran a inglés | 2026-05-22 | Segundo increment de FI-011; efecto multiplicador — los mensajes de los scouts se propagan al tomo, al SARIF y al hover de la extensión. Bajo riesgo (strings) |
| DG-050 | Próximo paso del roadmap (FI-011 etapa 3 — reporter HTML del tomo en inglés) | **Option A** — los labels del reporter HTML (`html-reporter.ts`) migran a inglés: `lang="en"`, encabezados, labels de triage/contexto/remediación, columnas de tabla, pie de integridad | 2026-05-22 | Tercer increment de FI-011; el tomo HTML es el deliverable que se muestra a un stakeholder/CISO. Bajo riesgo (strings) |
| DG-051 | Próximo paso del roadmap (FI-011 etapa 4 — extensión VSCode en inglés) | **Option A** — la UI de la extensión (status bar, Code Actions, hover, webview, notificaciones, `package.json`) migra a inglés | 2026-05-22 | Cuarto increment de FI-011; la extensión es la superficie primaria del producto (v0.4 VSCode-primary) — debe hablar inglés antes de empaquetar el beta. Bajo riesgo (strings) |
| DG-052 | Próximo paso del roadmap (FI-011 etapa 5 — prompts del Brain Layer en inglés) | **Option A** — los `SYSTEM_PROMPT` y user prompts de los 3 agentes migran a inglés; el LLM responde en inglés. **Cierra FI-011** | 2026-05-22 | Quinto y último increment de FI-011; la forma JSON pedida no cambia (field names idénticos) — parser y schemas intactos. Deja el producto íntegramente en inglés |
| DG-053 | Próximo paso del roadmap (abrir Phase 8 — cache de scanners global) | **Option C** — `resolveScannerBinary` resuelve también desde `~/.synaptic-sentinel/scanners` (cache global por usuario); la cache `.scanners/` del repo mantiene prioridad | 2026-05-22 | **Abre Phase 8 (Distribución).** Con FI-011 cerrada ya no había motivo de sequencing para diferirla. Sub-increment 1 de FI-004, acotado a la resolución; prerequisito de FI-008 (`.vsix`) |
| DG-054 | Próximo paso del roadmap (modo `--global` de install-scanners) | **Option A** — `install-scanners.ts` gana el modo `--global` que instala los binarios en `~/.synaptic-sentinel/scanners`; cierra el lazo de FI-004 (resolver + instalar) | 2026-05-22 | Sub-increment 2 de FI-004; `installScanner` ya parametrizaba `installDir`, el cambio es solo de `main()` — bajo riesgo. La cache global queda operativa, prerequisito de FI-008 |
| DG-055 | Próximo paso del roadmap (runtime de Node del extension host) | **Option A** — `spawnCli` lanza la CLI con `process.execPath` (el Node del extension host) en vez de `node` del `PATH` | 2026-05-22 | Sub-increment 1 de FI-008; un `.vsix` instalado no puede asumir `node` en el `PATH` del usuario. Cambio de un solo default — bajo riesgo; de-riskea el bundle+package |
| DG-056 | Próximo paso del roadmap (preparar el manifest de la extensión para vsce) | **Option A** — `package.json` (`repository`, `homepage`, `bugs`, `keywords`) + `.vscodeignore` + `README.md` de marketplace + `LICENSE` | 2026-05-22 | Sub-increment de FI-008; el manifest es el prerequisito de menor riesgo del `.vsix`. Solo metadata. Validación `vsce ls` diferida (npx colgado en el entorno) |
| DG-057 | Próximo paso del roadmap (bundlear la CLI dentro de la extensión) | **Option B** — el script `bundle` produce dos artefactos esbuild: `extension.cjs` (extension host, CJS) y `cli.mjs` (la CLI, **ESM**); la CLI bundleada vive en `dist/cli.mjs`. Los assets de runtime (`schema.sql`, `sentinel-baseline.yaml`) se copian junto al bundle (`scripts/copy-cli-assets.mjs`); `colony-db.ts`/`rules.ts` caen al asset hermano si el path de `src/` no existe | 2026-05-22 | Sub-increment de FI-008; un `.vsix` debe ser autocontenido — la CLI ya no puede vivir en el paquete hermano `cli/dist`. La CLI bundleada va en **ESM** porque resuelve sus assets vía `new URL(…, import.meta.url)` y `import.meta` solo existe en ESM; esbuild no procesa ese patrón, de ahí el copy script. Verificado e2e contra el bundle real |
| DG-058 | Próximo paso del roadmap (producir el `.vsix` de la extensión) | **Option A** — `@vscode/vsce` como devDependency + scripts `vscode:prepublish`/`package`; `name` del paquete → `synaptic-sentinel` (vsce rechaza un name scoped); `.vscodeignore` endurecido; `runtime-check.ts` mitiga FI-001 (chequeo del Node del host en `activate()`) | 2026-05-22 | Sub-increment final del empaquetado de FI-008; `vsce` como devDependency evita el cuelgue de `npx` (Norton/TLS) y habilita la validación del manifest que DG-056 difirió — un `vsce package` exitoso **es** esa validación. Desviación honesta del texto de la opción: FI-001 se mitiga con un chequeo en runtime preciso, no elevando `engines.vscode` a un valor adivinado |
| DG-059 | Próximo paso del roadmap (sub-comando `scanners install` turnkey — **cierra FI-008**) | **Option A** — CLI gana `synaptic-sentinel scanners install [--global]` (lógica movida de `scripts/install-scanners.ts` a `packages/cli/src/scanners/install.ts`, dentro del bundle); manifest convertido a const TS tipado; la extensión contribuye el comando "Synaptic Sentinel: Install Scanners" que lo invoca con `--global` por el pseudoterminal; `scripts/install-scanners*` eliminados (los 5 unit tests se movieron a `cli/tests/scanners/`) | 2026-05-22 | Cierra el último item de FI-008 con la opción conservadora: reusa una pieza ya probada (`installScanner` de DG-053/DG-054) detrás de un sub-comando + un comando de la extensión; sin descargas en background ni recovery flow. El manifest como const TS evita el ceremonial de copiar el JSON al bundle/dist. **FI-008 cerrado → Phase 8 (Distribución) COMPLETA.** |
| DG-060 | Próximo paso del roadmap (migración de `ColonyDb` a `better-sqlite3` — **cierra FI-001**) | **Option B** — `node:sqlite` → `better-sqlite3@^12` (NAPI con prebuilts Node 24); `DatabaseSync` → `Database` default + `Database.Database` como tipo de instancia; resto del wrapper intacto (firmas idénticas). esbuild `--external:better-sqlite3` + copy step mete el paquete y su cadena (`bindings`, `file-uri-to-path`) en `dist/node_modules/`. `runtime-check.ts` (band-aid de DG-058) eliminado | 2026-05-22 | Opción balanceada de DG-060: la última fragilidad real del `.vsix` se cierra en código, no vía band-aid. La persistencia ya estaba aislada detrás de `ColonyDb` desde DG-013 → refactor local. Primer intento con `^11` falló (Node 24 sin Visual Studio); `^12` trae prebuilts NAPI para Node 24 (unblock real). El binario NAPI es ABI-estable cross-version e Electron; la verificación end-to-end en el extension host real queda como caveat honesto |
| DG-061 | Próximo paso del roadmap (taint analysis JS/TS — FI-003 etapa 1) | **Option B** — 3 reglas `mode: taint` en `sentinel-baseline.yaml`: `sentinel-js-taint-command-injection` (CWE-78, sources `req.*`/`process.argv`, sinks `exec`/`execSync`/`spawn`), `sentinel-js-taint-xss` (CWE-79, +sources `window.location`/`document.URL`, sinks `innerHTML`/`outerHTML`/`document.write`, sanitizers `DOMPurify`/`escapeHtml`/`sanitizeHtml`), `sentinel-js-taint-sql-injection` (CWE-89, sinks `db.query`/`execute`/`knex.raw`). Fixture `taint-vuln.js` + test de integración. Cobertura del ruleset: 11 → 14 reglas. Python diferido a etapa 2 | 2026-05-22 | Primer increment de FI-003 acotado a un lenguaje (JS/TS = LOCKED MVP target del v0.4), en línea con el patrón de sub-increments que cerró FI-008 y FI-011. Aprender los quirks de OpenGrep taint con scope bounded y verificable antes de duplicar a Python. El test de integración contra OpenGrep real **prueba empíricamente** que `mode: taint` y la propagación intra-procedural funcionan en este binario — lo que era el único unknown real |
| DG-062 | Próximo paso del roadmap (fix FP SQL + pivot SQLite a WASM) | **Option B** — (1) drop del sink `$DB.exec($SQL, ...)` en `sentinel-js-taint-sql-injection` (matcheaba `child_process.exec` destructurado y generaba FP descubierto al probar el `.vsix` post-DG-061); regresión asserted en el test (`sqlFindings.length === 1`). (2) Pivot del driver SQLite: `better-sqlite3@^12` → `node-sqlite3-wasm@^0.8` (WASM puro, sin ABI). ColonyDb absorbe los caveats de la lib: `createRequire` por interop CJS, binds via array, statement finalization explícita (`withStmt` helper + `db.run/get/all` para one-shots). Drop de `PRAGMA WAL`. `.vsix`: 3.65 MB → 620 KB | 2026-05-22 | El caveat FI-001 de DG-060 (compatibilidad cross-Electron de `better-sqlite3` NAPI) se materializó en la prueba del usuario: NODE_MODULE_VERSION mismatch. WASM elimina la clase entera de bug, no la parchea. Tres descubrimientos honestos absorbidos: interop CJS, finalización de statements, locks WAL — DG-060 reclamó "NAPI ABI-estable" que en la práctica no se cumplía con los prebuilts reales |
| DG-063 | Próximo paso del roadmap (taint analysis Python — FI-003 etapa 2, **cierra FI-003**) | **Option B** — 3 reglas `mode: taint` para Python en `sentinel-baseline.yaml`: `sentinel-py-taint-command-injection` (CWE-78, sources Flask `request.*` + Django `request.GET/POST/body` + `sys.argv` + `os.environ`, sinks `subprocess.*` + `os.system` + `os.popen`), `sentinel-py-taint-sql-injection` (CWE-89, sinks `$CURSOR.execute/executemany/executescript`), `sentinel-py-taint-path-traversal` (CWE-22, sinks `open`/`pathlib.Path`/`os.remove`/`unlink` + `shutil.rmtree/copy`, sanitizers `secure_filename`/`os.path.basename`). Fixture `taint-vuln.py` + test de integración. Cobertura del ruleset: 14 → 17 reglas | 2026-05-22 | Réplica directa del patrón probado en DG-061 (JS/TS); el unknown principal ya estaba resuelto. Adapta sources/sinks al ecosistema Python (Flask, Django, DB-API 2.0). CWE-22 path traversal es un sink nuevo pero los patterns (`open`, `pathlib.Path`) son standard. **FI-003 cerrado entero** — único frente OPEN remanente: FI-009 |
| DG-064 | Próximo paso del roadmap (FI-009 — migrar `AnthropicLlmClient` a `@anthropic-ai/sdk`) | **Option A** — `AnthropicLlmClient` migrado del cliente `fetch` propio al SDK oficial `@anthropic-ai/sdk@^0.40` detrás del contrato `LlmClient`. Gana retries automáticos en 429/5xx (`maxRetries`), rate-limiting nativo, soporte de streaming. Drop de `buildAnthropicRequest`; `parseAnthropicResponse` intacto. Bundle: `--external:@anthropic-ai/sdk` + `copy-cli-assets.mjs` refactor con resolución de clausura transitiva (`findPackageRoot` walk-up para deps con `package.json` bloqueado por `exports`). 14 paquetes copiados al `.vsix` (SDK + 13 transitives); `.vsix`: 621 KB → 1.25 MB | 2026-05-22 | Cierre conservador de la última deuda OPEN. Migración bounded gracias al contrato `LlmClient` (de DG-024 B): cero cambios en los 3 agentes consumidores. Dos gotchas absorbidos honestamente: inlining inicial del SDK falló (interop CJS con `node-fetch` en esbuild ESM) → fix con `--external`; `require.resolve('<pkg>/package.json')` fallaba por el campo `exports` del SDK → helper `findPackageRoot`. **Hito: `futureImprovements` queda vacía por primera vez en 57 ciclos** |
| DG-065 | Próximo paso del roadmap (marketplace polish + family alignment) | **Option A** — marketplace polish v0.1.0 alineado con la familia SYNAPTIC (sibling SYNAPTIC Expert): `package.json` version 0.0.0→0.1.0, publisher `golab`→`RealGoLab`, `+icon` (`media/icon.png`), `+galleryBanner: {color:'#1a1a2e', theme:'dark'}`, `+activationEvents: ['onStartupFinished']`, description endurecida (5 scouts + Brain Layer + IDE + BYOK), `+categories: ['Testing']`, `+keywords: synaptic / taint-analysis / ai-coding / byok`. Assets: `media/icon.svg` (glifo de neurona de la familia + escudo Sentinel-específico) + `media/icon.png` 128×128 RGBA generado por `scripts/render-icon.mjs` (encoder PNG pure-Node con `zlib`+`Buffer`, sin deps). NUEVO `CHANGELOG.md` (Keep a Changelog, entry 0.1.0). `README.md` rewritten como página de marketplace al estilo del sibling. `vsce package` produjo `synaptic-sentinel-0.1.0.vsix` (430 archivos, 1.26 MB). **Follow-up (Entry #70, commit 9f44a82, 2026-05-23):** tras comparación visual del usuario con SYNAPTIC Expert se corrigieron 3 desviaciones — (1) icono OFICIAL de la familia GoLab (`GoLab_Aplicac_Fondo_Negro_120.png`) en vez del Sentinel-específico, removidos `media/icon.svg` y `scripts/render-icon.mjs`; (2) publisher `RealGoLab`→`GoLab` (id marketplace `golab.synaptic-sentinel`); (3) casing `Synaptic Sentinel`→`SYNAPTIC Sentinel` en toda la superficie user-visible. `.vsix` final: 429 archivos, 1.27 MB | 2026-05-22 | Polish de envoltorio sin cambios funcionales. El usuario invocó explícitamente la familia y apuntó al proyecto sibling (`D:\\GoLAB\\PROYECTOS\\SYNAPTIC_VSC_EXTENSION`) como referencia — inspeccionado para extraer publisher/galleryBanner/icon-style/README-format. El icono se generó con un encoder PNG hecho a mano para no reintroducir el riesgo de ABI nativa que FI-001 acaba de cerrar — mismo principio de cero-binarios que motivó el pivot a WASM en DG-062. Las 3 correcciones del follow-up endurecen la alineación con la familia: el branding de familia (logo + casing) prima sobre la diferenciación por producto; `golab` ya era el publisher histórico del Sentinel (revertido). **`.vsix` listo para `vsce publish` — el timing es del usuario** |
| DG-066 | **Apertura de Phase 9 (Strategic Pivot)** — unificación legal del producto | **Option B** — sub-increment atómico: solo sustrato legal. `LICENSE-PRO` eliminado (era placeholder EULA Ley 17.336 CL nunca finalizado); `packages/agents/package.json` re-licenciado a `Apache-2.0` y `description` re-frase (drop "Pro"); `packages/agents/src/index.ts` header JSDoc sin tag `[PRO]`. Re-positioning textual (README/CHANGELOG/ONBOARDING) queda para DG-067; visibilidad del repo para DG-068; bump v0.2.0 + `.vsix` nuevo para DG-069. **AMENDA a DG-001 B**: `publish-oss.ts` allowlist queda obsoleto bajo el giro. | 2026-05-23 | Giro estratégico del usuario: "darle todo el potencial (incluido el reservado para la versión comercial) y lanzarlo full capabilities como el mejor sentinela del mundo para vibe-coding". Modelo elegido vía AskUserQuestion: OSS full Apache-2.0; BYOK Anthropic; monetización **diferida** (sponsors/consulting/hosted version a posteriori); repo a abrir público en Phase 9. Granularidad atómica del sub-increment B: el README/CHANGELOG que dirían "Apache-2.0 todo" serían **literalmente falsos** mientras `packages/agents/package.json` siguiera diciendo `SEE LICENSE IN LICENSE-PRO` — el sustrato legal precede al texto que lo cita. Verificable mecánicamente (grep + `pnpm verify`); reversible trivial (`git revert`). Phase 8 (Distribución) sigue COMPLETA — el `.vsix v0.1.0` sigue siendo válido funcionalmente |
| DG-067 | Phase 9 sub-increment 2 — re-positioning textual del producto | **Option B** — re-positioning completo bajo la tagline elegida por el usuario "**The vibe-coding security sentinel**" (tone conservador, no "world's best"). 5 archivos user-visible reescritos: `README.md` raíz (pitch + How it works + sección "What makes it the vibe-coding security sentinel" + tabla de paquetes Apache-2.0 unificada + License colapsado a una frase + migración a inglés); `packages/vscode-extension/README.md` (headline + "Brain Layer (Pro, BYOK)" → "Brain Layer (BYOK Anthropic)" + Licenses → License Apache-2.0 único + footer); `packages/vscode-extension/CHANGELOG.md` (nueva entrada `[0.2.0] - _Unreleased — to be cut in DG-069_` documentando el unify + drop "Pro" del entry v0.1.0); `ONBOARDING.md` (sección 1 + tabla de paquetes con `agents` en Apache-2.0); `packages/vscode-extension/package.json` (description re-led + keywords += `ai-generated-code`, `llm-security`). Version/publisher/id/icon intactos (queda para DG-069). | 2026-05-23 | Sub-increment 2 de Phase 9 acotado a texto user-visible — sin tocar versión ni regenerar `.vsix`. Tone "**The vibe-coding security sentinel**" validado con el usuario antes de implementar (versión original "the world's best" era un claim de superlativo sin benchmarks externos que lo sustentaran — claim de marketing irrespetuoso del anti-optimismo ilusorio). Las superficies viven en `main` privado; el marketplace seguirá mostrando el README del `.vsix v0.1.0` hasta que DG-069 + Phase 10 publiquen `.vsix v0.2.0` |
| DG-068 | Phase 9 sub-increment 3 — apertura del repo a público | **Option B** — pre-flight + metadata + visibility change atómico, con guardrail explícito. Pre-flight ejecutado: `gitleaks detect` sobre historia completa = 1 hit en fixture deliberadamente vulnerable de `tests/.../fixtures/secrets/` (excluido por guardrail, no leak real); sweep secundario de patrones `sk-ant-*`/`AKIA*`/`ghp_*`/`xox_*` fuera de `tests/**/fixtures/**` = 0 hits adicionales; revisión manual de `.synaptic/` y `context/` = sólo personal info LOW severity (Pedro author credit, `golab.develop@gmail.com` ya discoverable en perfil GitHub, paths locales `D:\GoLAB\PROYECTOS\SENTINEL`) — todos anticipados en mis caveats y el usuario no pidió redacción; `.gitignore` respetado (0 leaks de `node_modules`/`dist`/`.scanners`/`*.vsix`/`.env`); 184 archivos tracked en dirs sensatos. Acciones gh CLI: metadata refresh (description nuevo, homepage al marketplace listing, 10 topics — `vibe-coding`/`security`/`sast`/`taint-analysis`/`ai-coding`/`ai-generated-code`/`llm-security`/`byok`/`vscode-extension`/`synaptic`) + `gh repo edit --visibility public --accept-visibility-change-consequences`. Verificación post: `gh repo view` confirma `visibility: PUBLIC` + `licenseInfo: Apache-2.0` (detectado por GitHub automáticamente desde `LICENSE` en raíz). 0 archivos del producto tocados — fue operacional sobre GitHub API. | 2026-05-23 | Primera acción outward-facing real del proyecto. El guardrail del gitleaks scan se ejecutó ANTES de tocar visibilidad (anti-optimismo ilusorio: NO se asumió repo limpio — se verificó). La irreversibilidad parcial del cambio (clones/forks que ocurran durante la ventana pública sobreviven a un eventual `--visibility private`) está reconocida explícitamente por el flag `--accept-visibility-change-consequences` que gh CLI exige específicamente por esa razón. El homepage URL apunta al marketplace listing `GoLab.synaptic-sentinel` que **aún no existe** (.vsix v0.1.0 empaquetado pero no publicado; vsce publish es Phase 10) — el link va a 404 hasta entonces, declarativo de intención, no de estado completo |
| DG-069 | Phase 9 sub-increment 4 — release `v0.2.0` (**cierra Phase 9**) | **Option B** — bump `package.json` `0.1.0` → `0.2.0`; cut CHANGELOG date `[0.2.0] - 2026-05-23`; `pnpm verify` verde (302); `vsce package` produce `synaptic-sentinel-0.2.0.vsix` (429 archivos, 1.27 MB) con manifest validado (version 0.2.0, publisher GoLab, license Apache-2.0, description "The vibe-coding security sentinel...", keywords +`ai-generated-code`/`llm-security`); annotated tag `v0.2.0` pushed; `gh release create v0.2.0` con asset `.vsix` adjunto. Release público en https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.2.0; isDraft=false; isPrerelease=false; SHA-256 del asset expuesto por GitHub vía `digest` (`6d7c10b6ca6e5b9cd059b049f0d8475fef0e0e66cf4417ed18b0d4082a554be8`) — atiende el caveat de checksum público sin necesidad de un segundo asset. | 2026-05-23 | Cierre técnico de Phase 9 en 4 sub-increments balanceados: sustrato → texto → repo público → release. Opción B sobre A (sin GitHub Release) porque un tag sin asset es un release a medias en términos OSS; sobre C (B + `vsce publish` al marketplace) porque mezclaba el cierre técnico de Phase 9 con la publicación que merece Phase 10 propio. **Honesta nota anti-optimismo ilusorio: el `v0.2.0` es funcionalmente IDÉNTICO al `v0.1.0` — cero cambios de código en Phase 9; lo que cambia es legal + editorial + visibilidad + distribución como GitHub Release**. La página del marketplace `GoLab.synaptic-sentinel` sigue 404 hasta Phase 10 + PAT del usuario en Azure DevOps |
| DG-070 | **Apertura de Phase 11 — Multi-Provider Brain Layer** (bookkeeping puro) | **Option A** — apertura formal de Phase 11 tras viaje exploratorio extenso (2 rounds de discovery, 6 agentes web). El producto deja de ser Anthropic-only y se reposiciona como **provider-agnostic-by-design**. 10 decisiones consolidadas del usuario: provider-agnostic-by-design + provider-por-agente + Ollama recommended con XGrammar + **Modo D arquitectónico** (3 adapters: `AnthropicLlmClient` native + `OpenAiCompatibleLlmClient` genérico + `OllamaLlmClient` específico) + YAML `.sentinel/agents.yaml` versionable (Continue.dev pattern) + VSCode UI panel + CLI flags + `vscode.SecretStorage` namespaceado por provider + constrained decoding donde sea posible + extractor regex como fallback + benchmark empírico obligatorio (PromptBridge probó 20-30% degradation sin re-tuning) + marketplace listing debut con v0.3.0 multi-provider. Phase 10 (vsce publish v0.2.0 Anthropic-only) **DEFERIDA y renumerada como Phase 12**. Roadmap Phase 11: DG-070 (opener CERRADO) → DG-071 (`OpenAiCompatibleLlmClient`) → DG-072 (`OllamaLlmClient` con XGrammar) → DG-073 (provider registry + `.sentinel/agents.yaml`) → DG-074 (UI panel VSCode) → DG-075 (ground truth set manual sobre 11 fixtures) → DG-076 (empirical benchmark 495 calls) → DG-077 (per-provider prompt tuning si necesario) → DG-078 (cost visibility) → DG-079 (cierre Phase 11 con release v0.3.0). Este ciclo NO toca código del producto — solo bookkeeping en `.synaptic/`. | 2026-05-23 | Sub-increment 1 de 10 de Phase 11 — opener atómico solo bookkeeping, sin contaminar con código (patrón probado en DG-066 B). Anti-optimismo ilusoria explícita: NO declaramos el multi-provider hecho — declaramos solo la dirección estratégica formalizada. El producto sigue siendo el mismo `.vsix v0.2.0` ya publicado como GitHub Release; ningún feature user-visible cambia este ciclo. El valor es **arquitectónico-de-protocolo**: declarar el plan explícitamente antes de ejecutarlo, y dejar el contexto cargado para que los 9 ciclos siguientes (DG-071..DG-079) corran sobre base estable. Tres propósitos cumplidos: (a) registrar las 10 decisiones del usuario consolidadas tras discovery; (b) documentar públicamente la dirección en el repo público (DG-068 lo abrió — el roadmap ahora es signal de proyecto activo + commitment público al multi-provider); (c) anotar formalmente la renumeración Phase 10 → Phase 12 |
| DG-071 | Phase 11 sub-increment 2 — extraer `OpenAiCompatibleLlmClient` | **Option A** — adapter genérico OpenAI-compatible (~120 líneas + 8 tests unit con `fakeFetch`) que sirve a 14+ providers cambiando solo `baseURL` (OpenAI / Groq / DeepSeek / Mistral / Together / Fireworks / Perplexity / xAI Grok / Gemini-via-OpenAI-compat / AWS Bedrock Mantle / Azure OpenAI v1 / Ollama-sin-grammar / LM Studio / vLLM). Patrón replicado del `AnthropicLlmClient` (`#client` privado + helper parser puro + `temperature=0` hardcoded para determinism cross-provider). Cliente queda **dormant** (re-exportado del paquete `agents` pero ningún command del CLI lo invoca — el wiring viene en DG-073). Cero cambios en `AnthropicLlmClient` ni en el contrato `LlmClient`. Dep `openai@^6.18.0` agregada; `pnpm install` con `NODE_OPTIONS=--use-system-ca` (L-001). `pnpm verify` verde: 43 test files / 310 tests (+8 nuevos). | 2026-05-23 | Sub-increment 2 de Phase 11 — scope mínimo bounded (un solo archivo nuevo + un solo archivo de tests + 1 dep nueva) consistente con el patrón establecido en DG-066 B / DG-068 B. El cliente queda dormant deliberadamente para no introducir múltiples piezas movibles en un ciclo: NO declaramos multi-provider funcionando, declaramos sólo que el adapter genérico existe y está testeado con mocks deterministas. `temperature=0` hardcoded internamente (NO expone en el contrato) — decisión conservadora consistente con Sentinel como security tool que prefiere determinism; si DG-076 (benchmark empírico) revela que un provider necesita `temperature` distinta, se abre un sub-DG para exponerla. **Caveat conocido**: el bundle de la extensión necesitará `--external:openai` + agregar la clausura transitiva al `copy-cli-assets.mjs` cuando DG-073 wiree el cliente al CLI — anotado para no sorprendernos |
| DG-072 | Phase 11 sub-increment 3 — extraer `OllamaLlmClient` con XGrammar opt-in (cierra los 3 adapters del Modo D) | **Option B** — adapter Ollama-específico (~170 líneas + 15 unit tests con `fakeFetch`) que usa la **API NATIVA** de Ollama (`/api/chat`) en vez de la OpenAI-compat (`/v1/chat/completions`). La diferencia es el unlock real del posicionamiento "Ollama recommended con ~99% JSON validity" decidido en DG-070: sólo la API nativa soporta structured outputs constrained-by-grammar (XGrammar desde v0.5+) vía el param `format` (acepta JSON Schema object, `"json"` literal legacy, o undefined). Patrón replicado de los otros 2 adapters: `parseOllamaResponse` helper puro + clase `OllamaLlmClient implements LlmClient` + `temperature=0` hardcoded. Sin `apiKey` (Ollama local sin auth). Sin nueva dep (`fetch` global Node 20+) — el bundle de la extensión NO va a necesitar `--external:ollama` en DG-073. **Helpers de auto-discovery exportados** (Opción B sobre A): `isOllamaAvailable(baseUrl?, fetchImpl?)` con timeout 1s vía `AbortController`; `listOllamaModels(baseUrl?, fetchImpl?)` devuelve `readonly string[]` (vacío en cualquier error, no lanza). `pnpm verify` verde: 44 test files / 325 tests (+15). Cliente queda **dormant** (re-exportado pero ningún command lo invoca — wiring en DG-073). **3 de 3 adapters del Modo D extraídos**. | 2026-05-23 | Sub-increment 3 de Phase 11 — Opción B sobre A porque los helpers de auto-discovery son centrales al UX "Ollama recommended" que DG-070 ancló (badge "Found / Not found" en la UI de DG-074 + fallback en el CLI de DG-073). Cohesión arquitectónica: cliente + helpers de descubrimiento viven en el mismo archivo conceptual ("todo lo específico de Ollama"). Anti-optimismo ilusorio activo: cliente dormant, ningún feature user-visible cambia. **Caveat crítico** sobre XGrammar — en versiones de Ollama < v0.5 el campo `format` se ignora silenciosamente (modelo devuelve JSON pero NO constrained); esto es indetectable client-side; el benchmark DG-076 va a revelar empíricamente si funciona en la versión del usuario. **Caveat de timeout** — 1s puede dar false-negative bajo carga; la UI de DG-074 va a tener botón "Refresh" |
| DG-073 | Phase 11 sub-increment 4 — provider registry + `.sentinel/agents.yaml` + wiring runtime al CLI + SecretStorage namespaceado + bundle externals (**primer ciclo de Phase 11 con valor user-visible real**) | **Option B** — el sub-increment más grande de Phase 11. Compone los 3 adapters extraídos en DG-071/DG-072 en un **provider registry** (`createLlmClient` factory en `packages/agents/src/provider-registry.ts`) + schema YAML versionable `.sentinel/agents.yaml` (en `packages/core/src/config/agents-config.ts`) + 3 JSON Schemas hand-written para activar XGrammar en Ollama. **Wira el runtime al CLI**: `commands/triage.ts` refactored con `resolveAgentLlmClients` y precedencia `injected > CLI flag > yaml > Anthropic fallback > error`; nuevo flag `--agent-provider <agent>=<provider>/<model>` repeatable; los 3 agentes corren cada uno con su propio `LlmClient` (multi-provider end-to-end). **VSCode extension**: `secret-storage.ts` con slots namespaceados `sentinel.<provider>.apiKey` + migración automática del legacy `synaptic-sentinel.anthropicApiKey`; `cli-runner.ts` pasa `apiKeyEnv: Record<string,string>` (no más `apiKey: string` hardcoded a Anthropic); `index.ts` recolecta todas las keys con `collectAllApiKeysAsEnv`. **Bundle externals**: `--external:openai` en esbuild + clausura transitiva en `copy-cli-assets.mjs`. Dep `js-yaml@^4.1.0` en `core` + `openai@^6.18.0` en `vscode-extension`. **2 feat commits + 1 docs commit**: `5656e2d` (core+agents sin wiring) + `e9ca983` (cli+vscode-extension con wiring + bundle). `pnpm verify` verde: 48 test files / 375 tests (+50 nuevos funcionalmente). Smoke `vsce package` verde: `.vsix 1838 archivos / 3.08 MB` (vs 1.27 MB DG-072 B; +143% por clausura del SDK openai). Retro-compat v0.2.0 PRESERVADA: usuarios sin `agents.yaml` con `ANTHROPIC_API_KEY` setado siguen funcionando idéntico (fallback implícito a Anthropic Haiku para los 3 agentes). | 2026-05-23 | **Primer ciclo de Phase 11 con valor user-visible real** — tras 3 ciclos consecutivos de adapters DORMANT (DG-070/DG-071/DG-072), DG-073 compone todo. Opción B sobre A (sin wiring → 3 ciclos dormant seguidos diluirían la confianza) y sobre C (mergea DG-074 → high-risk con UI webview + multi-piece). Decisión arquitectónica clave: el contrato `LlmClient` **no se modifica**; `temperature=0` hardcoded en los adapters; precedencia de config sólo en `triage.ts` (no abstracción reutilizable, deliberadamente). El `llmClient` único legacy queda como `options.llmClient` para retro-compat de todos los tests existentes — cero refactor de tests previos. **Caveats honestos**: multi-provider funciona vía CLI pero NO via UI extension hasta DG-074 (la UI sigue siendo Anthropic-only); bundle size +143% (3.08 MB) por el SDK openai con su closure; NO se corrieron integration tests reales contra providers (DG-076 los cubre); XGrammar requiere Ollama ≥ v0.5 (DG-072 B caveat heredado); migración del legacy SecretStorage testeada con fake in-memory pero no manualmente con VSCode real (queda para F5 del usuario o DG-074). **Anti-optimismo ilusorio activo**: no declaramos quality cross-provider — declaramos solo plumbing end-to-end. La validación empírica de quality es DG-076 |
| DG-074 | Phase 11 sub-increment 5 — Settings panel multi-provider (Configure Brain Layer Providers) | **Option B** — webview panel del VSCode extension que cierra el gap UX dejado por DG-073 B: la extension UI era Anthropic-only. Con este DG el usuario puede configurar multi-provider **enteramente desde VSCode** sin tocar archivos. Comando nuevo `synaptic-sentinel.configureProviders` abre un webview con 3 secciones: **Active Configuration** (provider/modelo resuelto por agente, source = `agents.yaml` / fallback / none), **Managed Credentials** (12 rows, una por provider con auth: Anthropic + 11 OpenAI-compat; cada row con password input + Save/Delete/Test botones + badge `not set` / `configured` / `tested` / `error`), **Local Models (Auto-Discovery)** (ping a `localhost:11434/api/tags` + lista de modelos pulled + botón Refresh). Patrón replicado de DG-039 B "tomo vivo": **renderer puro testeable** (`settings-content.ts` ~280 líneas con CSP+nonce+escapeHtml para anti-inyección) + **provider con message passing** (`settings-view.ts` ~270 líneas, singleton con `resolveState` + `onMessage` handlers para `ready`/`set-key`/`delete-key`/`test-key`/`refresh-ollama`). **Las API keys NUNCA cruzan al webview en texto plano** — el state es `configured: boolean` por provider, no el valor real. **`agents-yaml-writer.ts`** (~75 líneas) con `agentsYamlHasComments` (detector con special-case del header autogenerado al inicio identificado por marcador "Generated by SYNAPTIC") + `renderAgentsYaml` (serializer con header documentado que NUNCA incluye keys). Deps workspace `@synaptic-sentinel/core` + `@synaptic-sentinel/agents` agregadas + `js-yaml@^4.1.0` explícito + tsconfig references. **9 archivos** (3 src + 2 tests + index.ts + package.json + tsconfig + lock). `pnpm verify` verde: 50 test files / **403 tests** (+28 nuevos: 14 settings-content + 9 agents-yaml-writer + 5 settings-view ya cubiertos por integration). Smoke `vsce package` verde: 1838 archivos / **3.16 MB** (+80 KB vs DG-073 B). | 2026-05-23 | Sub-increment 5 de Phase 11 — Opción B sobre A (read-only + 12 comandos individuales que pondrían ruido en Command Palette) y sobre C (B + dynamic model dropdown + cost preview duplica DG-078). **Decisión arquitectónica clave**: el renderer es PURO sin acceso a `SecretStorage` ni a `fetch` — todo el round-trip lo hace el provider. Las API keys NUNCA en el webview state. El botón "Test" es **placeholder UX** en este DG: confirma que la apiKey está en SecretStorage pero NO valida con ping real al provider — la validación cross-provider va a DG-076 (benchmark empírico). La extension NO escribe `agents.yaml` desde el panel en este DG (solo gestiona credenciales); el panel surfacea warning si detecta comentarios manuales (limitación `js-yaml@4` que no preserva comments al round-trip). **Anti-optimismo ilusorio**: el webview NO está ejercitado manualmente en CI — sólo el renderer puro + writer tienen unit tests; la interactividad la verifica el usuario con F5. 1 bug detectado durante el verify y corregido: `agentsYamlHasComments` trataba el header multi-línea como user comments — refactor para detectar el header como bloque consecutivo identificado por el marcador en la primera línea |
| DG-075 | Phase 11 sub-increment 6 — ground truth dataset (AI-draft) para benchmark cross-provider | **Option C (Híbrido)** — define + popula el dataset que DG-076 (benchmark empírico) va a consumir para puntuar cada `{provider, model}` en los 3 agentes del Brain Layer. **27 entries** cubriendo 13 fixtures deliberadamente vulnerables (9 OpenGrep SAST JS/TS + 7 SAST Python + 1 Gitleaks Secrets + 2 Checkov IaC + 4 Vibe-Detect VibeCoded). Todas las entries marcadas como `reviewedBy: "ai-draft"` — el dataset es un **borrador Claude** basado en lectura de los fixtures + el ruleset, NO autoritativo. **5 archivos**: schema Zod `BenchmarkGroundTruthSchema` en `packages/core/src/config/benchmark-ground-truth.ts` (~150 líneas con `TriageGroundTruthSchema` / `ContextGroundTruthSchema` / `RemediationGroundTruthSchema` y `REVIEW_STATUSES` enum + helper `countByReviewStatus`); JSON dataset en `tests/benchmark/ground-truth.json`; `tests/benchmark/README.md` documentando contrato de PASS por agente + modelo de revisor + procedimiento + limitaciones del AI-draft; 16 unit tests del schema + del dataset cargado desde disco. Las 3 capas de PASS: **Triage** = match exacto del classification + `confidence ≥ minConfidence` + `requiredKeywords` contained (case-insensitive); **Context** (solo si Triage=TP) = los 4 fields contienen sus respectivos `Keywords`; **Remediation** (solo si Triage=TP) = `summary` + `recommendation` contienen keywords + si hay `fixedSnippet`, NO contiene `forbiddenInSnippet` (**validación por NEGATIVA** — un buen fix elimina el sink). `pnpm verify` verde: 51 test files / **419 tests** (+16 nuevos). | 2026-05-23 | Opción C balanza velocidad con autoridad: yo lleno el borrador completo pero marco cada entry como `ai-draft` explícitamente; el usuario revisa cuando se acerque a release externo. **Anti-optimismo ilusorio explícito**: yo NO soy AppSec engineer; el dataset NO es autoritativo para claims externos; el README documenta que cualquier cita externa del benchmark debe filtrar a `human-confirmed` o ship con disclaimer. `requiredKeywords` pueden ser muy estrictos (sinónimos del LLM fallan) o muy laxos (LLM mediocre pasa) — DG-076 lo surfaceará empíricamente. **SCA (Trivy) deferido honestamente** — Trivy enumera 1 finding por CVE (potencialmente docenas en un solo package-lock.json); enumerar estáticamente sin correr el scanner sería optimismo ilusorio. DG-076 las descubre dinámicamente y el reporte las marca "no ground truth — best-effort scoring" |
| DG-080 | **Apertura de Phase 12 — Marketplace launch v0.3.0**: preparación marketplace (README polish + runbook + .vsix re-pack) · vsce publish diferido al usuario | **Option B (Polish README marketplace + vsce publish; usuario eligió ejecutar publish manual con runbook)** — abre Phase 12 con la preparación COMPLETA del lado código pero deja el `vsce publish` como step manual del usuario (sub-pregunta en el DG: el usuario eligió "runbook ahora + publish después yo mismo" por preferencia operacional, no por falta de PAT). **3 deliverables**: (1) `packages/vscode-extension/README.md` REESCRITO de v0.2.0-era a multi-provider explícito (~14 KB vs ~5 KB original) con tabla de 7 filas de providers soportados (Anthropic native + OpenAI + DeepSeek + Groq + Gemini + Mistral/Together/Fireworks/Perplexity/xAI + AWS Bedrock/Azure + Ollama/LMStudio/vLLM) + sección "Cost visibility" con ejemplo del summary block + sección "Configuring providers" con 3 paths equivalentes (IDE Settings panel / `.sentinel/agents.yaml` / CLI flag `--agent-provider`) + sección "Known Limitations" honesta con 4 items resumidos (gpt-5 reasoning tokens, Ollama RAM, free tier quotas, tokens proxy chars/4) que enlaza al CHANGELOG para el full set; command list actualizada incluyendo `Configure Brain Layer Providers`. (2) NEW `docs/PUBLISHING.md` runbook completo (~10 KB) con: prerequisitos one-time (Azure DevOps account + Marketplace publisher `GoLab` + PAT con scope `Marketplace > Manage` en "All accessible organizations" + verificación `vsce` installed), 4 steps de publish (set `VSCE_PAT` + `vsce publish --packagePath` + verify listing público + announce optional), guía para releases subsiguientes (`v0.4.0+`), tabla de 6 failure modes con causes + fixes, sección "Unpublishing emergency-only" con caveats de irreversibilidad parcial. (3) RE-EMPAQUETADO `synaptic-sentinel-0.3.0.vsix` (3.18 MB / 1838 archivos) con el README actualizado bundleado dentro — **SHA-256 nuevo `DA07CA79CB8632C19037C16AAA7175E9504EA727CBF223585AA35B0B29A22185`** DISTINTO del GitHub Release `v0.3.0` (`5EA050B1...`) que queda histórico inmutable — son artefactos divergentes por content del README, NO regresión. **`pnpm verify` verde 463 tests**. **DG-080 cierra PARCIAL**: preparación done, `vsce publish` pendiente. El listing `GoLab.synaptic-sentinel` SIGUE retornando 404 hasta que el usuario ejecute el runbook con su PAT. Cuando publique, follow-up Entry en BITACORA cierra el DG completamente (no requiere nuevo DG — es continuación operacional). | 2026-05-24 | Opción B sobre A (solo `vsce publish` sin polish — desperdiciaba la inversión de Phase 11 en el primer screenshot del marketplace que sería v0.2.0-era) y sobre C (B + announcement materials Show HN / X / LinkedIn / GitHub Discussions — anti-optimismo ilusorio: hype prematuro con 6 caveats abiertos es invitar a feedback negativo viral antes de resolver gpt-5 reasoning tokens / Ollama RAM / path leak; mejor publicar silenciosamente, arreglar caveats en sub-DGs, y hacer announcement cuando el producto esté listo para escrutinio público). El re-empaquetado con README distinto produce un SHA divergente — documentado explícitamente en bookkeeping y en runbook para evitar confusión usuario-side. **DG-080 modela un patrón nuevo en el proyecto: cierre PARCIAL con hand-off explícito al usuario**, distinto a los cierres atómicos previos. Cuando el usuario ejecute `vsce publish` y verifique el listing, una sola Entry follow-up en BITACORA cierra el ciclo completo |
| DG-079 | Phase 11 sub-increment 10 (último) — **CIERRE de Phase 11** con release v0.3.0 multi-provider | **Option A (Conservador: bump + tag + GitHub Release sin marketplace publish)** — replica el patrón de cierre de Phase 9 con DG-069 B. Bump `packages/vscode-extension/package.json` `0.2.0` → `0.3.0`. CHANGELOG nueva entrada `[0.3.0] - 2026-05-24` con headline **"Phase 11 closes: SYNAPTIC Sentinel is now provider-agnostic by design"** + 3 sub-secciones `### Added` (Multi-provider Brain Layer / Cost visibility / Cross-provider benchmark plumbing) + `### Changed` (4 items: OpenAI gpt-5\* quirks, DeepSeek default model, pricing table location, backward compatibility) + **`### Known Issues` (6 caveats honestos)** + `### Notes`. `pnpm verify` verde (56 test files / 463 tests). `pnpm -F synaptic-sentinel package` exitoso: **`synaptic-sentinel-0.3.0.vsix` (3.17 MB / 1838 archivos)**. Manifest validado al extraer: version 0.3.0 + publisher GoLab + license Apache-2.0 + description "vibe-coding security sentinel..." + engines.vscode ^1.95.0 + 12 keywords incluyendo `byok` y `llm-security`. **SHA-256**: `5EA050B10358534647C46E6E3B6E845F9174463D85DE2770CE334B669994AC29`. Annotated tag `v0.3.0` + push + `gh release create v0.3.0` con asset `.vsix` adjunto + release notes basadas en CHANGELOG. **Phase 11 CERRADA**. | 2026-05-24 | Cierre formal con valor incremental real al producto: BYOK any provider end-to-end + Settings panel in-IDE + ground truth + benchmark + cost visibility, todo Apache-2.0. **Anti-optimismo ilusorio MAXIMO en el CHANGELOG**: la sección "Known Issues" lista los 6 caveats abiertos heredados de DG-076/77/78 explícitamente — path leak en synthetic findings + gpt-5\* reasoning tokens consuming max_completion_tokens + Ollama RAM saturation con modelos pesados (Gemma 4 9.6 GB, gpt-oss:20b 13 GB) + free tier quotas de Groq+Gemini exhausted tras un benchmark completo + tokens proxy chars/4 (±15-20% vs facturado real) + ground truth ai-draft sin human-AppSec review. El producto se publica con estas limitaciones DOCUMENTADAS, no oculto problemas. **Opción A** sobre B (no agrego polish marketplace ahora; mejor hacerlo en Phase 12 cuando se publique) y sobre C (no incluyo `vsce publish` que requiere PAT del usuario y mezcla scope técnico con operacional — Phase 12 es el lugar adecuado). Replica DG-069 B paso a paso (patrón probado de cierre de Phase 9). |
| DG-078 | Phase 11 sub-increment 9 — cost visibility (tokens proxy + USD estimado) + persistencia colony.db v5 + sub-comando `cost-history` | **Option B (CLI summary + persistencia colony.db, balanced)** — materializa el value-prop multi-provider de Phase 11 en el CLI: el usuario ahora ve al final de cada `triage` cuántos tokens consumió y cuánto le costó estimadamente, agrupado por `(provider/model, agente)`. **Persistencia**: nueva tabla aditiva `triage_token_usage` (schema v5) con `triage_session_id` UUID + `scan_id` + `fingerprint` + `provider_label` + `agent_id` + tokens + cost + latencia. **Sub-comando nuevo** `synaptic-sentinel cost-history [--limit N]` consulta histórico agrupando por (provider, agent), default últimas 10 sesiones. **Single source of truth de pricing**: movida de `tests/benchmark/pricing.json` (eliminado) a `packages/core/src/config/pricing.ts` como const TS exportada (`PRICING_TABLE`) + función pura `estimateCostUsd(label, inputTokens, outputTokens, table?)`. El benchmark runner ahora importa de `@synaptic-sentinel/core` (mismo patrón que el manifest de scanners en DG-059 — evita ceremonial de copy JSON al bundle de la extension). **TokenTrackingLlmClient** decorator (`packages/agents/src/token-tracking-client.ts`) envuelve cualquier `LlmClient` y registra observations (`inputTokens` proxy + `outputTokens` proxy + `latencyMs` + `ok/errorMessage`) por call sin modificar el contrato. Wiring en `commands/triage.ts`: cada `runAgent` call drena la última observation, calcula cost USD con `estimateCostUsd`, y crea un `TokenUsageRecord` asociado al fingerprint actual. Persistencia final con `db.insertTokenUsages(records)`. Summary block coloreado post-triage con caveat explícito `~estimated`. **Caveat anti-optimismo ilusorio activo**: tokens son **proxies** `chars/4` (heurística ±15-20% vs `usage` real del provider). Cost USD puede divergir; documentado explícitamente en el summary post-triage, en el output de `cost-history`, y en `docs/colony-db.md`. **Tests**: 21 nuevos (14 pricing + 5 token-count + 7 token-tracking-client). `pnpm verify` verde 56 test files / 463 tests. Build CLI bundle 354.9 KB → 357.6 KB. Schema migration aditiva (`CREATE TABLE IF NOT EXISTS`) — usuarios con colony.db v4 no requieren reset. | 2026-05-24 | **Option B elegida sobre A** (CLI summary sin persistencia; perdíamos cost-history; histórico es lo que materializa "compare cross-provider sobre TU código real") y **sobre C** (B + sidebar webview en extension VSCode; agrega 1 ciclo más, posterga release v0.3.0; el sidebar puede esperar a post-Phase-11 polish). El value-prop multi-provider necesita ser **visible donde el usuario trabaja** — el CLI summary post-triage + pseudoterminal de la extension (que ya muestra el CLI output desde DG-038) cubren ambas superficies sin invertir en un webview dedicado. Refactor de pricing.json → pricing.ts hace el bundle de la extension más limpio (no requiere copia de assets adicionales). Decorator pattern para `TokenTrackingLlmClient` mantiene el contrato `LlmClient` intacto — los 3 agentes del Brain Layer NO se enteran de que están siendo observados. **Caveats activos heredados de DG-077**: path leak, gpt-5 reasoning tokens, Ollama RAM con modelos pesados, free tier quotas — NO bloquean DG-078; siguen como sub-DGs futuros |
| DG-077 | Phase 11 sub-increment 8 — post-mortem del primer benchmark real + 5 surgical fixes + verbose mode + ground truth recalibration con sinónimos | **Option A (Surgical fixes + ground truth recalibration; extendido con verbose mode pedido por el usuario)** — ciclo extenso que ejecutó el primer benchmark real con las 5 API keys del usuario (Anthropic + OpenAI + DeepSeek + Groq + Gemini), preservó la corrida PILOT como evidencia diagnóstica (`docs/benchmark/v0.3.0-PILOT.md`), aplicó 5 fixes técnicos contra los hallazgos empíricos del PILOT, y agregó instrumentación verbose al runner para debugging manual. **Fixes técnicos**: (1) `OpenAiCompatibleLlmClient` ahora hace switch automático `max_completion_tokens` para gpt-5* family (descubrió empíricamente que gpt-5-nano rechaza `max_tokens` clásico); (2) también OMITE `temperature` para gpt-5* (segundo descubrimiento iterativo: gpt-5* solo acepta default=1, rechaza 0); (3) `response_format: { type: 'json_object' }` agregado siempre — mejora Gemini JSON adherence dramáticamente sin dañar otros providers; (4) DeepSeek default model `v3.2` → `v4-flash` (modelo `v3.2` retornaba `400 deprecated`); (5) `BenchmarkGroundTruthSchema` extendido con `KeywordOrAlternativesSchema = string \| string[]` + scorer `containsAll` trata arrays como OR-groups de sinónimos. **Ground truth recalibrado**: 26 entries reescritas; keywords conceptuales (`code injection`, `XSS`, `SQL injection`, `command injection`, `deserialization`, etc) ahora arrays de sinónimos comunes (`RCE`, `cross-site scripting`, `SQLi`, `shell injection`, etc); keywords técnicos directos (`eval`, `document.write`, `shell=True`) quedan como strings. **Verbose mode**: 3 flags nuevos al runner — `--verbose` (1 línea de log por LLM call con provider/agent/ruleId/PASS-FAIL-ERR/latency/200-char raw sample), `--entries <ids>` (filtra ground truth por ruleId), `--providers <names>` (filtra providers configurados). Documentado en `tests/benchmark/README.md`. **Resultados empíricos (3rd run cloud-only post-fixes)**: Anthropic Haiku Context PASS 2.6%→**46.2%**, Remediation 1.3%→**25.6%** (recalibración funcionó). DeepSeek v4-flash debut: 25.6% Triage / 53.7% Context / 53.7% Remediation, JSON validity 85-100%, **$0.0092 USD** — best cost/performance cloud por amplio margen. Gemini cuota nueva: 52/52 runs / 24 errors (46% error rate vs 99% del PILOT). OpenAI gpt-5-nano sigue 100% errors PERO con error distinto (`content=null` por reasoning tokens consumiendo `max_completion_tokens=1024`). Groq 100K TPD exhausted (free tier). Ollama Gemma 4 9.6 GB saturó RAM del usuario tras 2 horas — caveat conocido para LLMs locales pesados. **Hallazgo de raíz no anticipado (--verbose probe)**: el LLM hace meta-razonamiento sobre `Finding.location.path` — ve `tests/.../fixtures/vulnerable/...` y clasifica como `inconclusive` (no `true_positive`). Eso explica el 1.3% Triage PASS persistente de Anthropic. **Path leak queda como sub-DG futuro** documentado en README. `pnpm verify` verde 53 test files / 442 tests. Commit feat consolidado + docs. | 2026-05-24 | Ciclo el más extenso de Phase 11 — 4 corridas reales del benchmark, 5 fixes técnicos iterativos, ground truth recalibración + verbose mode extension. Opción A elegida sobre B (solo fixes sin recalibration; el Anthropic 1.3% PASS hubiera quedado sin explicar) y sobre C (cierre conservador con reporte pilot; perdíamos la oportunidad de medir DeepSeek + Gemini reales). El usuario pivotó a verbose mode en medio del ciclo cuando el batch sin visibilidad saturó su equipo — buena decisión que reveló el path leak issue de raíz. **Anti-optimismo ilusorio MAXIMO en este DG**: NO declaramos cross-provider benchmark operativo, declaramos plumbing validado + 2 providers cloud medidos útilmente (Anthropic baseline + DeepSeek v4-flash) + verbose mode para debugging + 4 caveats abiertos documentados (path leak, gpt-5 reasoning tokens, free tier quotas, Ollama RAM con modelos pesados). El v0.3.0.md final lleva disclaimer fuerte. La cita externa de los números **debe** filtrar a `human-confirmed` y excluir Triage PASS rates hasta que el path leak se arregle |
| DG-076 | Phase 11 sub-increment 7 — cross-provider benchmark plumbing (runner + scorers + report) con `--dry-run` validado, ejecución real diferida al usuario | **Option B (Plumbing-only)** — implementa el benchmark **completo de extremo a extremo** pero lo entrega validado solo con mocks deterministas; la corrida real contra providers cloud queda diferida hasta que el usuario provea API keys y/o tenga Ollama corriendo (Gemma 4 estaba descargando con ~45 min ETA al momento del DG). **3 archivos `packages/cli/src/benchmark/`** + **2 archivos de tests** + **`tests/benchmark/pricing.json`** + **`docs/benchmark/v0.3.0-DRAFT.md`**: (1) `scoring.ts` (~200 líneas) — funciones puras `triagePass` / `contextPass` / `remediationPass` + `buildSyntheticFinding` (sintetiza un `Finding` desde la entry del ground truth, sin correr scouts reales) + `scoutIdFromCategory` con caso explícito para `BusinessLogic` (exhaustivo); (2) `report.ts` (~150 líneas) — tipos `BenchmarkReport` / `ProviderResult` / `AgentRollup` + `renderBenchmarkReport` (markdown con disclaimer fuerte si todas las entries son `ai-draft`, tablas por-provider con PASS rate / JSON validity / latency / p95 / cost / determinism / errors, sección "Providers not run" para los que faltó API key); (3) `run.ts` (~280 líneas) — script ejecutable con `--dry-run` que usa `MockLlmClient`s deterministas (devuelven la respuesta esperada según la entry para que el plumbing se valide al 100%) + resolución de providers desde env vars `SENTINEL_<PROVIDER>_API_KEY` con fallback al legacy `ANTHROPIC_API_KEY` + cálculo de costo USD desde `pricing.json` + token-count heurística chars/4 (proxy explícito con ±20% caveat documentado) + determinism cross-runs (% de pares de runs que coinciden en `classification`). `pnpm verify` verde: 53 test files / **442 tests** (+23 nuevos: 14 scoring + 9 report; las tests del runner cubren parsers individuales). Smoke `--dry-run` con 1 mock provider: 26 entries × 3 runs = 78 invocaciones por agente, 0 errores, <1s total, reporte generado en `docs/benchmark/v0.3.0-DRAFT.md`. Script raíz `benchmark:run` en `package.json`. | 2026-05-23 | Opción B (Plumbing-only) elegida sobre A (Plumbing + ejecución real con keys del usuario; bloqueada porque usuario no proveyó keys cloud y Gemma 4 aún descargando) y sobre C (Plumbing + ejecución solo con Ollama; misma blocker — modelo no listo). **Anti-optimismo ilusorio MÁXIMO en este DG**: el `100% PASS / 100% JSON validity / 0ms latency / $0 cost` que muestra el reporte `v0.3.0-DRAFT.md` **es matemáticamente correcto pero empíricamente vacío** — los `MockLlmClient`s devuelven la respuesta perfecta por construcción; estos números **NO son evidencia de calidad de ningún provider real**. El reporte es un smoke test del plumbing, no un benchmark. Eso queda documentado fuertemente en el archivo mismo (disclaimer "internal-comparative only / Do NOT cite externally") y en el README. **El benchmark real lo corre el usuario** cuando tenga keys + Ollama listo: `pnpm benchmark:run` con `SENTINEL_ANTHROPIC_API_KEY` + `SENTINEL_OPENAI_API_KEY` + `SENTINEL_DEEPSEEK_API_KEY` + `SENTINEL_GROQ_API_KEY` + `SENTINEL_OLLAMA_MODEL=gemma3:latest` setados. Bug interesante absorbido durante el ciclo: la tabla de pricing fue hand-curated (no auto-pulled de docs porque la web a este corte tendría precios distintos a los actuales del runtime); cualquier modelo `unknown` cae a `$0` y el reporte lo marca con asterisco. Caveat de token-count: el contrato `LlmClient` no expone `usage` del provider, así que se aproxima con chars/4 (~±20% según OpenAI tokenizer docs) — un sub-DG futuro podría exponerlo si DG-078 (cost visibility) lo necesita para producción |
| DG-101 | Sub-DG fix triage cap silencioso — setting `synaptic-sentinel.triageLimit` (default 25, configurable 1-10000) + comando interno `synaptic-sentinel.triageRemaining` + botón "Triage X untriaged" en summary card del sidebar. Cierra el último silent UX trap del backlog v0.3.x identificado empíricamente en discovery (Cycle 88) y reconfirmado en feedback v0.3.9 (el usuario tuvo que re-correr triage manualmente para obtener las 38 findings clasificadas, sin saber por qué el cap=25 los skipeaba) | **Option A** — Cross-file: (a) `packages/vscode-extension/package.json` NEW property `synaptic-sentinel.triageLimit` (type:number, default:25, min:1, max:10000); (b) `packages/vscode-extension/src/cli-runner.ts` NEW `limit?: number` en `RunCliTriageOptions` + pasa `--limit <N>` a child process si definido; (c) `packages/vscode-extension/src/index.ts` NEW `COMMAND_TRIAGE_REMAINING` + NEW `TRIAGE_REMAINING_LIMIT = 9999` const + NEW helper `readTriageLimitSetting()` (con defense-in-depth: type check + min check + Math.floor + undefined fallback) + extendido `triageWorkspace(...limitOverride?)` + NEW `registerCommand(triageRemaining)` que reusa el handler existente con limit alto; (d) `packages/vscode-extension/src/webview-content.ts` `renderSummary` extendido — emite `<button class="triage-remaining-btn" data-action="triage-remaining">Triage X untriaged</button>` solo si `untriagedCount > 0` + NEW CSS `.triage-remaining-btn` usando `var(--vscode-button-*)` vars para coherencia con cualquier theme + script del webview registra handler defensivo (solo si el botón existe); (e) `packages/vscode-extension/src/tomo-view.ts` `#onMessage` ahora reconoce postMessage `type: 'triage-remaining'` → ejecuta `vscode.commands.executeCommand('synaptic-sentinel.triageRemaining')` con early return; (f) `scripts/verify-extension-activate.mjs` `EXPECTED_COMMANDS` 7 → 8 + `EXPECTED_SUBSCRIPTIONS_COUNT` 13 → 14 (gate detectó el delta automáticamente — primer cycle desde DG-097 A que muta el gate cumulativo, confirma que funciona como diseñado). +3 unit tests (552 vs baseline 549). pnpm verify VERDE end-to-end + ambos gates OK. NO bump version del .vsix v0.3.9. | 2026-05-28 | Cierra el último silent UX trap del backlog v0.3.x. El cap=25 del CLI `triage` (`DEFAULT_TRIAGE_LIMIT = 25`) skipeaba findings sin avisar en el sidebar — solo el terminal mostraba `(capped at 25; use --limit to raise it)` que el usuario podría no leer. La captura v0.3.9 reconfirmó el trap: el usuario tuvo que re-correr triage manualmente sin saber por qué. Decisiones de diseño: (a) **setting con `minimum:1` + `maximum:10000`** previene valores tontos + excesos absurdos; (b) **comando `triageRemaining` NO en `contributes.commands`** → NO expuesto al Command Palette (el flow es "descubrir el botón en el sidebar contextualmente cuando aparece"); (c) **`TRIAGE_REMAINING_LIMIT = 9999` hardcoded** vs leer el setting: deliberado — el botón dice "Triage N untriaged" donde N es el count exacto del bucket; si limitamos al setting el botón engañaría; 9999 es "todos" para cualquier workspace razonable; (d) **postMessage `type: 'triage-remaining'`** sigue el patrón de `'reveal'` (DG-039 B) → handler en `#onMessage` delega via `vscode.commands.executeCommand`; (e) **CSS con `var(--vscode-button-*)`** = el botón se ve coherente con cualquier theme; (f) **botón aparece solo si `untriagedCount > 0`** — control invisible cuando no hay acción posible. **Anti-optimismo activo**: (1) IMPACTO REAL solo se valida cuando el usuario instale una nueva versión y reproduzca el cap (ej: bajar `triageLimit` a 5 en el dummy); (2) `TRIAGE_REMAINING_LIMIT = 9999` es límite artificial — workspaces con 10000+ findings pending genuinamente caerían en el cap; (3) wording "Triage N untriaged" podría ser ambiguo si el usuario nunca corrió triage antes — el botón aparecería como si fuera "retry" de algo previo, cuando es el primer triage; ajustable en sub-DG futuro si emerge feedback; (4) **synapticStrength alcanza 100** — techo simbólico del schema; 100 DGs desde DG-001 + 19 consecutivos sin failed cycle + 6 releases reales = milestone narrativo del proyecto |
| DG-100 | Sub-DG release v0.3.9 con el fix de DG-099 A (sidebar Cost Visibility cross-package) acumulado en `main`; bump version + CHANGELOG entry + vsce package + annotated tag + GitHub Release con asset .vsix + invitación composita a feedback empírico del usuario sobre AMBOS DG-097 A + DG-099 A (cierre PARCIAL — vsce publish queda al usuario, mismo patrón DG-089/DG-091/DG-094/DG-096/DG-098) | **Option A** — Bump `packages/vscode-extension/package.json` `0.3.8` → `0.3.9`. NEW CHANGELOG entry `[0.3.9] - 2026-05-26` con 4 secciones: Added (cost card in sidebar con tabla compacta per (provider/model, agent) + tabular-nums + total + caveat ~estimated USD + versión mínima si rows vacío); Added (CLI `cost-history --json` flag con typed JSON payload `CostHistoryJson` consumido por extension via Zod schema); Notes (scope only DG-099 A; tokens/cost ~estimated; provider-reported badge NO surfaceado per-row; cost card position fixed; anti-optimismo sobre theme + multi-provider scaling); Known Issues (1 caveat structurally closed, sin cambios). `pnpm verify` VERDE post-bump: 58 test files / 549 tests + ambos gates OK. `vsce package` exitoso: `synaptic-sentinel-0.3.9.vsix` (1838 archivos, 3.14 MB, SHA-256 `7dc1886477be1b16e6552380f65955e0c16298673aa96d32e387535bf52085fd`). Annotated tag `v0.3.9` + push origin + push tag. `gh release create v0.3.9` con asset .vsix descargable + release notes basadas en CHANGELOG + sección Milestone narrativa del DG-100. URL: https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.3.9. `vsce publish` al Marketplace **NO ejecutado** — queda al usuario con su PAT (cierre PARCIAL). **Invitación composita post-release**: presentar al usuario flow de 6 pasos (uninstall → download v0.3.9 → install --extension → reload → scan+triage en test_8d.../synaptic-sentinel-dummy → captura) para validar AMBOS DG-097 A + DG-099 A en una sola sesión empírica. | 2026-05-26 | **DG-100 marca el cycle numérico redondo del proyecto** — 100 Decision Gates desde Cycle 1 / DG-001 ("Estructura física del monorepo OSS/Pro" del 2026-05-20). En esos 100 DGs el proyecto: (a) construyó el monorepo + 5 scouts + Coordinator + CLI + reporters; (b) construyó el Brain Layer con 3 agentes + memoria del enjambre + UX verbose; (c) cerró FI-011 (inglés), FI-008 (.vsix packaging), FI-001 (sqlite-wasm), FI-003 (taint analysis), FI-009 (Anthropic SDK), Phase 9 (strategic pivot OSS Apache-2.0), Phase 11 (multi-provider Brain Layer), Phase 12 (Marketplace launch v0.3.3 publicado); (d) cerró el backlog v0.3.0 con 5 fixes estructurales + 1 caveat estructurado (DG-083..DG-095); (e) publicó 6 releases reales (v0.3.4 → v0.3.9); (f) cerró 2 mejoras UI primarias (sidebar triage state DG-097 A + sidebar Cost Visibility DG-099 A). 18 sub-DGs consecutivos sin failed cycle (DG-083 → DG-100). Mismo perfil operacional probado 6 veces consecutivas (DG-089/DG-091/DG-094/DG-096/DG-098/DG-100). **Composito clave**: cerrar TODA la deuda visual de feature (DG-097 A + DG-099 A) en un solo release + invitar feedback empírico del usuario en el mismo turno — patrón "publish + validate" para atacar la deuda de validación acumulada (17 DGs sin failed cycle pero sin validación empírica del usuario). **Anti-optimismo ilusorio activo TRIPLE**: (1) DG-100 publica DG-099 A pero el IMPACTO REAL sigue diferido hasta que el usuario complete el flow de install + smoke test + captura; la invitación es lo mejor que podemos hacer pero NO garantiza feedback; (2) 6 releases GitHub-only pendientes USER-side; el marketplace upload skip v0.3.3 → v0.3.9 es la distancia más grande del proyecto; si el usuario hace upload manual, el riesgo de descubrir clase de bug latente compose risk se mantiene (DG-082.1 lesson sigue válida); (3) el milestone narrativo DG-100 / Cycle 92 / 100 DGs es un hito legítimo pero NO es validación empírica — es validación de proceso (100 ciclos completados con compliance 100%); el producto es "shipped" técnicamente pero su valor real-world se mide solo con usuarios reales |
| DG-099 | Sub-DG sidebar webview Cost Visibility — cost card persistente en el sidebar tras triage. Materializa el value-prop multi-provider de Phase 11 en la UI primaria: la cost summary que el CLI venía mostrando solo en el terminal post-triage ahora aparece como card en el sidebar webview, al lado del summary card de findings del DG-097 A. Cierra el Option C de DG-078 deferido (Cycle 71) — la cost visibility salta del terminal a la UI persistente del IDE. Cross-package change: CLI gana flag `--json` al sub-comando `cost-history`, extension consume el JSON estructurado | **Option A** — Cross-package: (a) `packages/cli/src/commands/cost-history.ts` NEW option `json?: boolean` + NEW helper exportado `computeCostHistoryTotals(rows)` puro + NEW interface exportada `CostHistoryJson` como contrato CLI↔extension. Cuando `json===true` emite `JSON.stringify` del payload tipado a stdout. Comportamiento default intacto: tabla formateada. Si colony.db no existe en modo JSON emite payload vacío válido (no error). (b) `packages/cli/src/index.ts` registra `json: { type: 'boolean' }` en parseArgs + USAGE actualizada. (c) `packages/vscode-extension/src/tomo.ts` NEW `CostHistoryRowSchema` + `CostHistoryTotalsSchema` + `CostSummarySchema` (Zod) + `parseCostSummary(raw)` devuelve `CostSummary \| null` defensivamente. `agentId` restringido a enum `'triage'\|'context'\|'remediation'` como guardrail anti-drift. (d) `packages/vscode-extension/src/cli-runner.ts` NEW `onStdout?: (chunk) => void` en `SpawnCliOptions` — si presente, stdout va a onStdout y stderr a onOutput (separación limpia); si no, comportamiento legacy mezclado. NEW `runCliCostHistory(options)` spawn `cost-history --json --limit 1 --path <ws>`, captura stdout solo, parse defensivo. Devuelve `null` si exit code != 0 o si parse falla. (e) `packages/vscode-extension/src/webview-content.ts` NEW `renderCostCard(summary)`: HTML con clase cost-card + título "Brain Layer cost · last session" (o "last N sessions") + caveat "~estimated USD" + tabla por (provider/model, agent) con columnas calls + input + output + cost + avg lat (tabular-nums) + total row. Si rows vacío, versión mínima "run Triage Findings to start tracking cost". `renderTomoWebviewHtml` extendida con 3er parámetro opcional `costSummary?: CostSummary \| null`. Cost card va entre summary card de findings (DG-097 A) y sections (TP/INC/Untriaged/FP). (f) `packages/vscode-extension/src/tomo-view.ts` `update()` acepta `costSummary` opcional; preserva el previo si se llama sin (mark-fp NO trae costSummary, no es triage). (g) `packages/vscode-extension/src/index.ts` post-triage: tras runCliTriage + runCliScan silencioso, NEW llamada a `runCliCostHistory`, pasa resultado a `tomoView.update`. +11 unit tests (549 vs baseline 538): 3 de `renderTomoWebviewHtml` con costSummary + 4 de `renderCostCard` + 4 de `parseCostSummary` (anti-drift). pnpm verify VERDE end-to-end + ambos gates OK. NO bump version del .vsix v0.3.8. | 2026-05-26 | Cierra el Option C de DG-078 deferido (Cycle 71) — la cost visibility ya estaba en el CLI desde Phase 11 (summary post-triage + sub-comando `cost-history`), pero solo en el terminal; un usuario que cerraba el terminal perdía la visibilidad del costo. v0.3.8 le dio al sidebar structure rica (4 buckets + summary card del DG-097 A); ahora la cost card tiene un lugar natural arriba de las sections. Decisiones de diseño: (a) CLI `--json` flag opt-in (mantiene retro-compat 100%); (b) Schema Zod con `agentId` restringido a enum: si el agente devuelve algo nuevo, `parseCostSummary` devuelve null y la card no se renderea (preferimos no renderear vs renderear data incorrecta); (c) cost card POSICIÓN entre summary card y sections — el summary card del DG-097 A es el primary signal (qué arreglar), cost es secondary (cuánto cuesta); (d) `onStdout` opcional en SpawnCliOptions: NO refactorea los 4 callers existentes (scan/triage/mark-fp/install) — solo `runCliCostHistory` usa onStdout aislado; (e) `update()` preserva costSummary previo si no se pasa: mark-fp NO trae costSummary; (f) CSS tabular-nums + monospace para columnas numéricas; (g) border-left color (#6aa1d6 azul) coincide con `pill-fp` del summary card — coherencia visual "azul = info" en la paleta. **Anti-optimismo ilusorio activo QUÍNTUPLE**: (1) construyendo DG-099 A sobre DG-097 A SIN validación empírica del usuario — la deuda de validación se acumula (el usuario eligió A construir sobre mi recomendación C pausa empírica; decisión legítima pero compose risk aumenta); (2) IMPACTO REAL SOLO se ve cuando se publique v0.3.9 (v0.3.8 actual NO incluye); (3) los 11 unit tests cubren la lógica pura — NO cubren spawn real en VSCode extension host, ni cómo se ve la cost card con themes reales, ni cómo escala con 9 rows (3 providers × 3 agents); (4) tokens/cost son `~estimated` (caveat DG-078 B / DG-085 A); el badge "provider-reported" del cost summary del terminal NO está surfaceado en la cost card; (5) **compose risk**: este es el 17vo sub-DG consecutivo sin failed cycle (DG-083 → DG-099). El riesgo de un release-blocker latente acumulado por falta de feedback empírico aumenta cada DG construido sin validación. La próxima recomendación fuerte será la pausa empírica POST-release v0.3.9 |
| DG-098 | Sub-DG release v0.3.8 con el fix de DG-097 A (sidebar triage state visibility + DEP0040 suppression) acumulado en `main`; bump version + CHANGELOG entry + vsce package + annotated tag + GitHub Release con asset .vsix (vsce publish queda al usuario, cierre PARCIAL — mismo patrón DG-089/DG-091/DG-094/DG-096) | **Option A** — Bump `packages/vscode-extension/package.json` `0.3.7` → `0.3.8`. NEW CHANGELOG entry `[0.3.8] - 2026-05-26` con 4 secciones: Added (triage state grouping en sidebar con 4 buckets TP→INC→Untriaged→FP + section headers por bucket no vacío + heading icons CSS-styled ●/?/○/✓ / state badge en cada card con confidence porcentaje 'TP 95%' / 'INC 50%' / 'FP 95%' / 'NEW' / summary card al tope con N findings + X TP + Y INC + Z NEW + W FP pills coloreadas); Changed (DEP0040 punycode + otros Node deprecation warnings suppressed via `NODE_NO_WARNINGS=1` al CLI spawn — afecta los 4 callers scan/triage/mark-fp/cost-history); Notes (scope only DG-097 A; grouping fixed no toggle; anti-optimismo sobre theme rendering no cubierto por unit tests; 'NEW' badge vs 'Untriaged' heading); Known Issues (1 caveat structurally closed — sin cambios desde v0.3.7). `pnpm verify` VERDE post-bump: 58 test files / 538 tests + ambos gates OK (verify-extension-activate 7 commands + 13 subscriptions; verify-manifest 18 checks aceptó la nueva semver 0.3.8). `vsce package` exitoso: `synaptic-sentinel-0.3.8.vsix` (1838 archivos, 3.14 MB, SHA-256 `c36c3ae0385eadc8d840887703fadfa67204e58e3c22f69bde3b78b18d24a739`). Annotated tag `v0.3.8` + push origin + push tag. `gh release create v0.3.8` con asset .vsix descargable + release notes basadas en CHANGELOG. URL: https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.3.8. `vsce publish` al Marketplace **NO ejecutado** — queda al usuario con su PAT (cierre PARCIAL, mismo patrón de DG-080 B / DG-082 A / DG-089 A / DG-091 A / DG-094 A / DG-096 A). | 2026-05-26 | **PRIMER release UI-visible desde v0.3.0** (Phase 11) — los releases v0.3.1/v0.3.2/v0.3.3 fueron hotfixes runtime, v0.3.4 acumulación de 6 fixes estructurales sin UI visible, v0.3.5 provider selector solo en settings panel, v0.3.6/v0.3.7 backend + docs. v0.3.8 mueve la aguja UI principal del producto por primera vez en 8 releases. Sin él, un usuario instalado en v0.3.6/v0.3.7 NO recibe el sidebar reorganizado por triage state — el value-prop del Brain Layer queda invisible en la UI primaria. Mismo perfil operacional probado 4 veces consecutivas (DG-089/DG-091/DG-094/DG-096): scope acotado a versionado + CHANGELOG + .vsix + tag + GitHub Release. Decision cierre PARCIAL preserva separación de responsabilidades. Ahora hay **5 releases GitHub-only pendientes de marketplace upload** (v0.3.4 + v0.3.5 + v0.3.6 + v0.3.7 + v0.3.8) — el Marketplace puede saltar directamente de v0.3.3 a v0.3.8 (semver permite skip). **Anti-optimismo ilusorio activo TRIPLE**: (1) el marketplace upload tiene riesgo no-cero de descubrir clase de bug no cubierta por gates locales (DG-082.1 lesson sigue válida; los 2 gates current cubren activate runtime + manifest validity, NO cubren UI rendering real); (2) acumulación creciente de **5 releases GitHub-only** sin Marketplace sync amplía el rango de skip cuando el usuario haga upload — distancia mayor que cualquier upgrade previo del proyecto; (3) el fix de DG-097 A es UI-centric, su IMPACTO REAL (usuario abre sidebar y de un vistazo distingue TP de FP) sigue diferido honestamente hasta que un usuario real instale v0.3.8 — los 13 unit tests del renderer NO cubren cómo se ve realmente con theme dark/light real ni cómo escala visualmente con >50 findings ni si el untriaged-as-'NEW' resulta intuitivo en el panel real |
| DG-097 | Sub-DG triage state visibility en el sidebar webview de la extensión VSCode — el value-prop del Brain Layer (TP / FP / INC verdicts) era invisible en la UI primaria; todas las cards se rendereaban con el mismo peso visual. Refactor del renderer para agrupar por triage state primero (TP → INC → Untriaged → FP), section headers por bucket, state badges con confidence porcentaje, summary card con breakdown, FPs con opacity 0.55. Fix paralelo: `NODE_NO_WARNINGS=1` al spawn de la CLI para suprimir el DEP0040 punycode ruido | **Option A** — NEW type exportada `TriageState = 'tp' \| 'inc' \| 'untriaged' \| 'fp'` + helpers exportados `triageStateOf(finding)` (maps classification + defensivamente cae a untriaged si classification es unknown o undefined) + `groupByTriageState(findings)` (particiona en 4 buckets preservando severity orden interno) en `packages/vscode-extension/src/webview-content.ts`. `renderTomoWebviewHtml` refactorizado: order de buckets TP→INC→Untriaged→FP, section headers solo si bucket no vacio con clase `section-{state}` + heading icon CSS-styled (●/?/○/✓) + count, cada card lleva `state-{state}` class + state badge en el head con confidence porcentaje ('TP 95%' / 'INC 50%' / 'FP 95%' / 'NEW' sin porcentaje para untriaged). FPs llevan opacity 0.55 con hover full opacity. NEW summary card al tope reemplaza el meta line: 'N findings · X TP · Y INC · Z NEW · W FP' con pills coloreadas (solo aparecen pills de buckets con count > 0) + singular/plural correcto. `packages/vscode-extension/src/cli-runner.ts` 1-line tweak: `spawnCli` ahora SIEMPRE compone env con `NODE_NO_WARNINGS=1` (antes solo se pasaba env si options.env era undefined; el merge order garantiza que la flag siempre gana). +13 unit tests (538 vs baseline 525): 5 de `triageStateOf` (4 casos + classification desconocida defensivamente cae a untriaged) + 1 de `groupByTriageState` (severity preservado dentro del bucket) + 7 de `renderTomoWebviewHtml` (order independiente de severity / section headers solo si bucket no vacio / state-* class por card / state badge con porcentaje / 'NEW' sin porcentaje / summary card con breakdown / singular vs plural). pnpm verify VERDE end-to-end + ambos gates OK. NO bump version del .vsix v0.3.7. | 2026-05-26 | Sub-DG aislado que cierra el gap UX/UI #1 identificado en la sesión de discovery con dummy curado (38 findings con mix TP/INC/Untriaged/FP): el value-prop del Brain Layer (53 LLM calls reales por scan, $0.0518 USD invertidos en triage+context+remediation) era INVISIBLE en la UI primaria — el usuario veía 38 cards idénticas sin distinción entre lo que tenía que arreglar (TP) y lo que ya estaba descartado (FP). Decisiones de diseño: (a) **bucket order TP→INC→Untriaged→FP** alineado con el flujo del usuario (arreglar primero, FP abajo); (b) **state badge con confidence porcentaje** ('TP 95%') es scan-rápido, la rationale completa queda en el brain layer expandido del card; (c) **'NEW' para untriaged** comunica 'pending action' sin ser peyorativo y es más corto que 'UNTRIAGED'; (d) **heading icons como CSS-styled chars** (●/?/○/✓) en lugar de emoji para evitar font fallback issues cross-OS; (e) **NODE_NO_WARNINGS=1 siempre, no opt-in**: el extension host NO debe enseñar warnings de Node internals al usuario; aplica a los 4 callers de spawnCli (scan / triage / mark-fp / cost-history) sin discriminar; (f) **classification desconocida → untriaged**: anti-optimismo del schema — si el agente devuelve drift, NO clasificamos como TP/FP/INC sin certeza. **Anti-optimismo ilusorio activo**: (1) el IMPACTO REAL solo se valida cuando el usuario instale una nueva versión de la extensión (v0.3.7 NO incluye este fix); los 13 unit tests cubren la lógica pura del renderer (HTML producido es correcto) pero NO cubren cómo se ve realmente con el theme dark/light real, cómo mapean los CSS variables `--vscode-*` al theme del usuario, ni cómo escala visualmente con >50 findings; (2) decisión de scope explícita NO scope-creep: NO se implementa filtering chips ni grouping toggle ('Group by: severity / category / file / triage status') — el grouping es fijo; si el usuario lo pide tras instalar, abrir sub-DG con toggle; (3) FPs con opacity 0.55 podrían sentirse muy dim para themes claros — un sub-DG futuro podría preferir `<details>` collapsible para FPs |
| DG-096 | Sub-DG release v0.3.7 con el fix de DG-095 A (ground truth review structure) acumulado en `main`; bump version + CHANGELOG entry + vsce package + annotated tag + GitHub Release con asset .vsix (vsce publish queda al usuario, cierre PARCIAL — mismo patrón DG-089 A + DG-091 A + DG-094 A) | **Option A** — Bump `packages/vscode-extension/package.json` `0.3.6` → `0.3.7`. NEW CHANGELOG entry `[0.3.7] - 2026-05-26` con 3 secciones: Added (3-level disclaimer in the benchmark report con `HUMAN_REVIEW_THRESHOLD = 10` + operational flow for ground truth revision con 8 pasos + per-layer criteria + thresholds table); Notes (scope only DG-095 A; `HUMAN_REVIEW_THRESHOLD` exported; anti-optimismo sobre cierre estructural — la opacidad cierra, la deuda real sigue abierta hasta corpus ≥ 10 reviewed; no CLI helper, deliberate scope); Known Issues (1 caveat structurally closed — ground truth ai-draft DG-075 con DG-095 A structured path forward). `pnpm verify` VERDE post-bump: 58 test files / 525 tests + ambos gates OK (verify-extension-activate 7 commands + 13 subscriptions; verify-manifest 18 checks aceptó la nueva semver 0.3.7). `vsce package` exitoso: `synaptic-sentinel-0.3.7.vsix` (1838 archivos, 3.14 MB, SHA-256 `d01ade1a08d66f49d1174be9d145dd0397c3eb6c3cfdb5d50357947af77a7122`). Annotated tag `v0.3.7` + push origin + push tag. `gh release create v0.3.7` con asset .vsix descargable + release notes basadas en CHANGELOG. URL: https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.3.7. `vsce publish` al Marketplace **NO ejecutado** — queda al usuario con su PAT (cierre PARCIAL, mismo patrón de DG-080 B / DG-082 A / DG-089 A / DG-091 A / DG-094 A). | 2026-05-26 | Cierre del "loop publicar" del DG-095 A. Sin él, un usuario instalando v0.3.6 NO recibe el escalado del disclaimer ni el README actualizado de ground truth review. Mismo perfil operacional que DG-089 A (release v0.3.4) + DG-091 A (release v0.3.5) + DG-094 A (release v0.3.6): scope acotado a versionado + CHANGELOG + .vsix + tag + GitHub Release. Decision cierre PARCIAL preserva separación de responsabilidades (yo no tengo credenciales del usuario). Ahora hay **4 releases GitHub-only pendientes de marketplace upload** (v0.3.4 + v0.3.5 + v0.3.6 + v0.3.7) — el Marketplace puede saltar directamente de v0.3.3 a v0.3.7 (semver permite skip). **Marca el cierre completo del backlog v0.3.0**: con v0.3.7 publicado, los 5 caveats cerrados técnicamente + el 1 estructurado están todos en un release real descargable. **Anti-optimismo ilusorio activo TRIPLE**: (1) el marketplace upload tiene riesgo no-cero de descubrir clase de bug no cubierta por gates locales (DG-082.1 lesson sigue válida); (2) acumulación creciente de 4 releases GitHub-only sin Marketplace sync amplía el rango de skip cuando el usuario haga upload — distancia mayor que cualquier upgrade previo del proyecto; (3) el fix de DG-095 A es structural/documentation-centric, su IMPACTO REAL (revisor humano usando "How to revise"; reporter mostrando disclaimer según review progress) sigue diferido hasta que un AppSec engineer real se sume |
| DG-095 | Sub-DG ground truth review structure — disclaimer escalado 3 niveles en `renderBenchmarkReport` (0 reviewed → "Anti-optimismo ilusorio" / 0 < reviewed < 10 → "Limited human review" + ratio + threshold / ≥ 10 → "acceptable for external citation; mention the mix") + flujo operacional documentado en `tests/benchmark/README.md` con criterios per-capa Triage/Context/Remediation; cierra el caveat heredado "ground truth ai-draft" estructuralmente sin pretender que AI puede hacer el review humano | **Option A** — NEW const exportada `HUMAN_REVIEW_THRESHOLD = 10` en `packages/cli/src/benchmark/report.ts`. Lógica de disclaimer reescrita en 3 ramas según `humanReviewed = confirmedCount + correctedCount`: (1) `=== 0` mantiene el disclaimer fuerte "Anti-optimismo ilusorio: all N entries are ai-draft (NO human-AppSec review); internal-comparative only — do NOT cite externally"; (2) `< HUMAN_REVIEW_THRESHOLD` agrega un nuevo disclaimer medio "⚠️ Limited human review (N of M entries reviewed; threshold for high-confidence external citation: 10)" + line "Ground truth review status: X confirmed + Y corrected + Z draft"; (3) `>= HUMAN_REVIEW_THRESHOLD` elimina el disclaimer fuerte y emite solo "Ground truth review status: X confirmed + Y corrected + Z draft (W ≥ 10 threshold — aggregate numbers are acceptable for external citation; mention the mix when reporting)". `tests/benchmark/README.md` sección "How to revise" reescrita como "How to revise (DG-095 A — full operational flow)" con 8 pasos (clone → branch → pick entry → open fixture → apply criteria → update reviewedBy + humanNotes → bump reviewedAt → pnpm test:unit → PR), subsección "What to validate, per capa" con criterios detallados para Triage / Context / Remediation, y "Disclaimer thresholds" table. +2 tests nuevos en `report.test.ts`: (1) "imprime disclaimer suave cuando humanReviewed ≥ 10 (DG-095 A threshold)" reemplaza el viejo "omite el disclaimer fuerte cuando hay revision humana"; (2) "imprime disclaimer medio Limited human review cuando 0 < humanReviewed < 10 (DG-095 A)" con fixture `{ai-draft: 22, confirmed: 3, corrected: 2}` verificando `Limited human review`, `5 of 27 entries reviewed`, `threshold for high-confidence external citation: 10`. `pnpm verify` VERDE 525 tests (+1 vs baseline 524) + ambos gates OK. NO bump version del .vsix v0.3.6. NO CLI helper (decisión anti-optimismo de scope — el editor manual del JSON + tests + PR es suficiente y evita un sub-DG aparte). | 2026-05-26 | Sub-DG aislado que cierra el último caveat heredado de v0.3.x Known Issues ESTRUCTURALMENTE — la deuda real (AppSec engineer revisando 27 entries) NO se puede cerrar por código, pero el caveat se vuelve un caveat CON FLUJO documentado y CON UMBRAL operacional medible. 5 caveats cerrados + 1 estructurado = **100% del backlog v0.3.0 tratado** (no resuelto: el ground truth ai-draft sigue abierto en lo concreto, pero ya no es opaque — tiene flujo claro de cómo bajarlo). Decisiones de diseño: (a) **threshold 10 (no 5 ni 27)**: 5 sería estadísticamente insuficiente para "acceptable for external citation"; 27 (100%) sería excelente pero sin progresividad reconocible; 10 = ~37% del corpus + n suficiente para hablar honestamente de "majority human-reviewed"; (b) **3 niveles vs 2** (binary actual era 0 vs >0): el caveat "Limited human review" entre 1 y 9 entries es honesto — el reporte deja ver que HUBO esfuerzo de review pero NO hace marketing falso de "ground truth validado"; (c) **operational flow en README.md, no CLI helper**: cada entry es un JSON file con `reviewedBy`/`reviewedAt`/`humanNotes` — un editor de texto + `pnpm test:unit` es suficiente; un CLI helper sería over-engineering (sub-DG aparte si emerge demanda real) — anti-optimismo de scope; (d) **HUMAN_REVIEW_THRESHOLD exportado** (no inline number): el threshold puede cambiar legítimamente con más data empírica; tests lo importan para no hardcodearlo dos veces; (e) **criterios per-capa documentados** (Triage / Context / Remediation): explican QUÉ validar en cada categoría (`is_true_positive`, `keywords_required`, `risk_score`, etc) — convierte una tarea opaca ("revisar el ground truth") en una checklist accionable. **Anti-optimismo ilusorio MAXIMO**: este DG NO cierra el caveat real (corpus 100% ai-draft sigue siéndolo); LO QUE cierra es la opacidad — antes "ground truth ai-draft" era un caveat sin path forward; ahora es un caveat con threshold + flow + criterios. La cita externa SIGUE bloqueada hasta que el corpus llegue a ≥ 10 human-reviewed reales. Lo afirmable ahora: los 525 tests verifican que el reporter rinde correctamente los 3 niveles de disclaimer según los counts del corpus |
| DG-094 | Sub-DG release v0.3.6 con los 2 sub-DGs estructurales acumulados (DG-092 A diagnóstico defensivo + DG-093 A unificación naming `.sentinel/`); bump version + CHANGELOG entry + vsce package + annotated tag + GitHub Release con asset .vsix (vsce publish queda al usuario, cierre PARCIAL — mismo patrón DG-089 A + DG-091 A) | **Option A** — Bump `packages/vscode-extension/package.json` `0.3.5` → `0.3.6`. NEW CHANGELOG entry `[0.3.6] - 2026-05-26` con 4 secciones: Added (ColonyDb.open actionable error messages — DG-092 A: pre-flight de directorio parent + wrap del SQLite error con las 3 causas comunes observadas + original error preservado); Changed (workspace data directory unified to `.sentinel/` — DG-093 A: colony.db ahora en `<workspace>/.sentinel/colony.db`; backward-compat dual-read sin migrar archivo; per-user scanner cache `~/.synaptic-sentinel/scanners/` queda intacto); Notes (helper `resolveColonyDbPath` exportado para integraciones third-party; `.gitignore` updated en docs/colony-db.md; anti-optimismo sobre helper puro 100% covered vs integration test sólo en mark-fp); Known Issues (1 abierto: ground truth ai-draft — sin cambios desde v0.3.5). `pnpm verify` VERDE post-bump (524 tests + verify-manifest aceptó la nueva semver 0.3.6). `vsce package` produce `synaptic-sentinel-0.3.6.vsix` (1838 archivos, 3.14 MB, SHA-256 `f99bf452c02b5d1b3bb7d1dbbb9f441363c447308b8790addef77af8c021eb08`). Annotated tag `v0.3.6` + push origin + push tag. `gh release create v0.3.6` con asset .vsix + release notes basadas en CHANGELOG entry. URL: https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.3.6. `vsce publish` al Marketplace **NO ejecutado** — queda al usuario con su PAT (cierre PARCIAL, mismo patrón de DG-080 B / DG-082 A / DG-089 A / DG-091 A). | 2026-05-26 | Cierre del "loop publicar" del feedback del usuario que originó esta cadena: DG-092 A vino de un bug real reportado (`unable to open database file` opaco), DG-093 A de una pregunta UX legítima (inconsistencia naming `.sentinel/agents.yaml` vs `.synaptic-sentinel/colony.db`). v0.3.6 empaqueta ambos en un release real visible. Mismo perfil operacional que DG-089 A (release v0.3.4) + DG-091 A (release v0.3.5): scope acotado a versionado + CHANGELOG + .vsix + tag + GitHub Release. Decisión cierre PARCIAL preserva separación de responsabilidades (yo no tengo credenciales del usuario). Ahora hay **3 releases GitHub-only pendientes de marketplace upload** (v0.3.4 + v0.3.5 + v0.3.6) — el Marketplace puede saltar directamente de v0.3.3 a v0.3.6 (semver permite skip). **Anti-optimismo ilusorio activo TRIPLE**: (1) el marketplace upload tiene un riesgo no-cero de descubrir una clase de bug no cubierta por los gates locales (DG-082.1 demostró que el upload manual puede revelar mismatches); (2) acumulación de 3 releases GitHub-only sin Marketplace sync amplía el rango de "skip" cuando el usuario haga upload; (3) la migración a `.sentinel/` del DG-093 A sólo se valida end-to-end con instalación local del v0.3.6 — los unit tests cubren el helper puro pero los 3 commands distintos a mark-fp NO tienen integration test específico del dual-read |
| DG-093 | Sub-DG unificación de naming workspace-level a `.sentinel/` con dual-read backward-compat — cierra la deuda de diseño descubierta en DG-092 A (la inconsistencia `.sentinel/agents.yaml` vs `.synaptic-sentinel/colony.db` que el usuario reportó usando v0.3.5) | **Option A** — NEW archivo `packages/core/src/colony/db-path.ts` con 3 const exportadas (`COLONY_DB_DIRNAME = '.sentinel'`, `COLONY_DB_DIRNAME_LEGACY = '.synaptic-sentinel'`, `COLONY_DB_FILENAME = 'colony.db'` — single source of truth) + interface exportada `ColonyDbPathResolution { path, dir, isLegacy }` + función exportada `resolveColonyDbPath(projectRoot)` con preferencia 4-rama: (1) `.sentinel/colony.db` existe → isLegacy=false; (2) sólo legacy existe → usa legacy isLegacy=true; (3) ninguno (workspace nuevo) → defaultea a `.sentinel/` isLegacy=false; (4) ambos existen (anómalo) → gana el nuevo. Barrel `packages/core/src/index.ts` agrega `export * from './colony/db-path.js'`. Los 4 CLI commands (scan, triage, mark-fp, cost-history) reemplazan el hardcoded `join(projectRoot, '.synaptic-sentinel', 'colony.db')` por el helper + log informativo cuando `isLegacy === true`. Imports `join` removidos donde quedaron unused. +6 unit tests (524 vs baseline 518): 5 del helper cubriendo las 4 ramas + regresión guard de const names; 1 nuevo en mark-fp.test.ts cubriendo el dual-read end-to-end (`lee del legacy .synaptic-sentinel/colony.db si es el unico presente`). Tests existentes refactorizados con `seedDb` helper que acepta `{legacy?: boolean}`. `cli-runner.integration.test.ts` limpia ambos paths en afterAll() por si quedó residual. **Cache global per-user `~/.synaptic-sentinel/scanners/` NO se toca** (decisión user-side AskUserQuestion: workspace y home son ámbitos distintos). Documentación actualizada: extension README (2 menciones + nota dual-read), root README (1 mención + nota dual-read), ONBOARDING.md, docs/colony-db.md (intro reescrito + sección `.gitignore` lista ambos paths). `pnpm verify` VERDE 524 tests + ambos gates OK. NO bump version del .vsix v0.3.5. | 2026-05-26 | Cierra la deuda de diseño confirmada en DG-092 A. Antes de implementar: AskUserQuestion para 2 sub-decisiones grandes — (1) cache global per-user: mantener legacy (decisión del usuario); (2) backward-compat workspace DB: dual-read sin migrar archivo (decisión del usuario). Decisiones de diseño: (a) **helper centralizado** `resolveColonyDbPath` en core (single source of truth); (b) **dual-read sin migración automática** preserva data existente con cero riesgo — el usuario decide cuándo mover el archivo manualmente; (c) **caso anómalo "ambos existen" gana el nuevo**: comportamiento predecible y consistente con "nuevo path tiene precedencia"; (d) **3 const exportadas** (`COLONY_DB_DIRNAME` + `COLONY_DB_DIRNAME_LEGACY` + `COLONY_DB_FILENAME`) para que tests no hardcodeen strings y resistir a refactors futuros del path; (e) **log informativo** (no error) cuando se lee legacy — no interrumpe el flujo, solo sugiere migración. **Anti-optimismo ilusorio activo**: el IMPACTO REAL (usuario antiguo conserva data, usuario nuevo escribe en `.sentinel/`) sólo se valida con corrida en dev host F5; el helper puro tiene cobertura completa de tests; el dual-read end-to-end está cubierto por 1 integration test en mark-fp pero NO en los otros 3 commands. Riesgo bajo asumido — todos usan el mismo helper |
| DG-092 | Sub-DG diagnóstico defensivo en `ColonyDb.open()` — convierte el error opaco `unable to open database file` de SQLite WASM en un mensaje accionable con las 3 causas comunes (lockfile residual / Norton AV / permisos) + el original SQLite error. Bug reportado por el usuario al abrir v0.3.5 en un workspace de SYNAPTIC Expert; el error fue transitorio (log posterior mostró sistema funcionando end-to-end) pero el fix asegura diagnóstico cuando reaparezca | **Option A** — NEW import `dirname` de `node:path` en `packages/core/src/colony/colony-db.ts`. `ColonyDb.open()` ahora: (1) **pre-flight**: si `path !== ':memory:'`, valida que `dirname(path)` existe; si no, throw Error con `parent directory "<dir>" does not exist...` citando el path absoluto solicitado. (2) **wrapped error**: try/catch sobre `new sqliteWasm.Database(path)`; si falla, throw Error con mensaje multi-línea listando las 3 causas comunes observadas/teóricas + el original SQLite error: (a) file lockeado por otro Sentinel CLI run; (b) Norton/Defender/AV bloqueando write (agregar workspace a exclusions); (c) workspace read-only / sin write permissions. Defensa centralizada en `ColonyDb.open` → beneficia los 4 callers (scan, triage, mark-fp, cost-history) sin duplicar. +3 unit tests (518 vs baseline 515) en `colony-db.test.ts`: (1) `lanza un error accionable cuando el directorio parent no existe` (regex match `parent directory.*does not exist`); (2) el mensaje incluye el path absoluto; (3) `:memory:` sigue funcionando sin pre-flight (regresión guard). `pnpm verify` VERDE 518 tests + ambos gates OK. NO bump version del .vsix v0.3.5. | 2026-05-26 | Bug reportado por el usuario abriendo v0.3.5 en un workspace ajeno (`D:\GoLAB\PROYECTOS\SYNAPTIC_EXPERT\packages\agent\workspaces\Robalito\`). El error original fue **transitorio**: el log completo posterior mostró Scan + 2x Triage + provider selector UI del DG-090 A operando end-to-end (`provider summary` cambió de `triage=OpenAiCompatible` a `triage=Ollama` entre corridas — validación empírica accidental del DG-090 A). Causas probables del error transitorio (no diagnosticables sin stack trace SQLite original): lockfile residual de proceso muerto / Norton AV bloqueando / permisos. DG-092 A asegura que cuando reaparezca, el mensaje sea diagnosticable y accionable. Decisiones de diseño: (a) **defensa centralizada en `ColonyDb.open`** (no en cada CLI command): single source of truth, callers nuevos heredan la defensa sin duplicar; (b) **NO auto-fix** (mkdir/retry/delete lockfile): cada causa requiere acción del usuario distinta — el mensaje accionable es el contrato; (c) **pre-flight de dirname() existe** (no permissions check): el caller hace `mkdirSync({recursive:true})` antes de open, así que dir-inexistente solo se ve en tests con paths inválidos; permissions check es noisy en Windows (ACLs); (d) mensaje en INGLÉS consistente con FI-011; (e) las 3 causas vienen de observación empírica + L-002 documented. **Hallazgo lateral confirmado durante el diagnóstico (NO atacado en este DG)**: inconsistencia de naming en main `.sentinel/agents.yaml` (DG-073 B Cycle 66) vs `.synaptic-sentinel/colony.db` + `~/.synaptic-sentinel/scanners/` (todo lo demás). Deuda de diseño real documentada como sub-DG candidato — probable DG-093. **Anti-optimismo ilusorio activo**: el IMPACTO REAL (cuando vuelva a ocurrir, el usuario ve el mensaje accionable) sólo se valida cuando se reproduzca; el wrap del SQLite error NO está bajo unit test (mock del driver = low-ROI vs la observabilidad de un error real) |
| DG-091 | Sub-DG release v0.3.5 con el provider selector UI de DG-090 A; bump version + CHANGELOG entry + vsce package + annotated tag + GitHub Release con asset .vsix (vsce publish queda al usuario, cierre PARCIAL — mismo patrón DG-089 A) | **Option A** — Bump `packages/vscode-extension/package.json` `0.3.4` → `0.3.5`. NEW CHANGELOG entry `[0.3.5] - 2026-05-26` con 3 secciones: Added (Editable provider/model selector per agent — `<select>` filtrado + `<input>` model prefilled + Apply button + helpful hint cuando no hay providers ready); Notes (only DG-090 A feature; yaml format unchanged from v0.3.0 / DG-073 B; mergeAgentConfig validation defensiva; anti-optimismo sobre tests vs flow stateful end-to-end); Known Issues (1 abierto: ground truth ai-draft — sin cambios desde v0.3.4). `pnpm verify` VERDE post-bump (515 tests + verify-manifest aceptó la nueva semver 0.3.5). `vsce package` produce `synaptic-sentinel-0.3.5.vsix` (1838 archivos, 3.14 MB, SHA-256 `89c749d15f09c476c76fe222c746d6057c6ffe2d57e6c58f10968883886ff4b2`). Annotated tag `v0.3.5` + push origin + push tag. `gh release create v0.3.5` con asset .vsix + release notes basadas en CHANGELOG entry. URL: https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.3.5. `vsce publish` al Marketplace **NO ejecutado** — queda al usuario con su PAT (cierre PARCIAL, mismo patrón de DG-080 B / DG-082 A / DG-089 A). | 2026-05-26 | Cierre del "loop publicar" del feedback del usuario: el reporte UX vino directamente de usar v0.3.4 instalada (DG-090 A), y v0.3.5 empaqueta el fix en un release real para que sea visible externamente y otros usuarios lo reciban. Mismo perfil operacional que DG-089 A (release v0.3.4 con los 6 fixes acumulados DG-083 → DG-088): scope acotado a versionado + CHANGELOG + .vsix + tag + GitHub Release. El verify gate cumulativo (DG-081 B activate runtime + DG-083 A manifest validity) confirma la viabilidad del release; el `verify-manifest` específicamente aceptó la nueva semver 0.3.5. Decisión cierre PARCIAL (igual que DG-089 A): el GitHub Release queda público con el asset .vsix descargable e instalable manualmente, pero `vsce publish` al Marketplace queda al usuario con su PAT — preserva la separación de responsabilidades (yo no tengo credenciales del usuario). Ahora hay **2 releases GitHub-only pendientes de marketplace upload** (v0.3.4 + v0.3.5) — el Marketplace puede saltar directamente de v0.3.3 a v0.3.5 (semver permite skip de versiones intermedias en marketplace listings). **Anti-optimismo ilusorio activo doble**: (1) el marketplace upload tiene un riesgo no-cero de descubrir una clase de bug no cubierta por los gates locales (DG-082.1 demostró que el upload manual puede revelar mismatches no detectables localmente); (2) el flow stateful end-to-end del DG-090 A (usuario abre panel → Apply → yaml escrito → next triage usa nuevo provider) sólo se valida con la instalación local del .vsix v0.3.5 — los 13 unit tests del DG-090 A cubren el renderer + el helper puro `mergeAgentConfig`, no la integración `writeAgentsYamlFromUI` desde el message handler |
| DG-090 | Sub-DG provider selector UI editable en el panel "Configure Brain Layer Providers" — la sección "Active Configuration" deja de ser read-only; cada agente gana dropdown de provider + input de model + botón "Apply" que escribe `.sentinel/agents.yaml` (UX deuda descubierta por el usuario usando v0.3.4 instalada) | **Option A** — Renderer `renderAgentRow` reescrita en `packages/vscode-extension/src/settings-content.ts`: `<select data-agent-field="provider">` con providers filtrados (configured + Ollama available + currentProvider siempre incluido aunque NO configured, para reflejar state real) + `<input type="text" data-agent-field="model">` prefilled con escape + `<button data-action="set-agent-provider">Apply</button>`. NEW helper privado `availableProvidersForSelector(state, currentProvider)`. NEW archivo `packages/vscode-extension/src/agents-config-merge.ts` con `mergeAgentConfig(current, agentId, provider, model): AgentsConfig \| null` puro testeable (validación defensiva: agentId en triage/context/remediation, provider en PROVIDER_NAMES, model non-empty tras trim — null si invalido); separado de `settings-view.ts` para evitar dependencia transitiva en `vscode` que rompe vitest unit (misma estrategia que `agents-yaml-writer.ts` desde DG-074 B). `settings-view.ts`: NEW method `#handleSetAgentProvider` (valida workspace + llama mergeAgentConfig + writeAgentsYamlFromUI + showInformationMessage) + NEW method `#loadCurrentAgentsConfig` (yaml o fallback Anthropic) + handler `set-agent-provider` en `#onMessage` ANTES del guard `provider === undefined` (porque ese guard es para SecretProviderName, no LLM ProviderName). Script del webview gana handler `set-agent-provider` que lee `<select>` + `<input>` del row clickeado y envía postMessage. README de la extensión actualizada: path A (panel IDE) ahora documenta "pick a provider + model per agent from a dropdown, then click Apply ... writes .sentinel/agents.yaml". +13 unit tests (515 vs baseline 502): 6 del renderer (dropdowns + filtering + selected + escape + hint) + 7 del helper puro (merge isolation + inmutabilidad + trim + 4 casos de validación inválidos + soporte 13 providers). `pnpm verify` VERDE 515 tests + ambos gates OK. NO bump version del .vsix v0.3.4. | 2026-05-26 | UX deuda descubierta por el usuario usando v0.3.4 instalada localmente: el panel mostraba "Active Configuration" como display read-only y guardaba API keys en SecretStorage, pero NO ofrecía forma de elegir DESDE LA UI cuál provider/model corre cada agente — sólo vía `.sentinel/agents.yaml` manual o flag CLI `--agent-provider`. La pregunta UX legítima del usuario abrió este sub-DG fuera del backlog v0.3.x Known Issues (este fix NO viene del backlog heredado — es UX nueva). Decisiones de diseño: (a) helper puro `mergeAgentConfig` en archivo separado para evitar dependencia transitiva en `vscode` (rompe vitest unit); (b) dropdown filtra a providers `configured` + Ollama `available` — evita confusión "selecciono mistral pero no tengo key → siguiente triage falla con error opaco"; (c) currentProvider siempre se incluye aunque NO configured — para que el usuario vea EXACTAMENTE qué provider el sistema usaría ahora; (d) input de model como text plano (no dropdown) — la lista de models de cada provider cambia con cada release; un dropdown hardcoded se desactualiza; (e) validación en `mergeAgentConfig` defensiva (devuelve null vs throw) — el webview puede mandar cualquier cosa via postMessage; el caller transforma null en `showWarningMessage`. **Anti-optimismo ilusorio activo**: el IMPACTO REAL (usuario abre panel, clickea Apply, yaml se escribe, siguiente triage usa nuevo provider) sólo se valida con dev host F5; los 13 unit tests cubren render + helper puro; la parte stateful (`writeAgentsYamlFromUI` llamado desde el handler) queda diferida — alta complejidad para unit test con mocks de vscode + fs + workspaceRootProvider, low ROI |
| DG-089 | Sub-DG release v0.3.4 con los 6 fixes acumulados DG-083 A → DG-088 A; bump version + CHANGELOG entry + vsce package + annotated tag + GitHub Release con asset .vsix (vsce publish queda al usuario, cierre PARCIAL) | **Option A** — Bump `packages/vscode-extension/package.json` `0.3.3` → `0.3.4`. NEW CHANGELOG entry `[0.3.4] - 2026-05-26` con 4 secciones (Added: verify-manifest gate DG-083 A + Ollama heavy warning DG-087 A + real LLM usage DG-085 A + QuotaExhaustedError DG-088 A; Changed: gpt-5* reasoning tokens DG-086 A + benchmark anonymized paths DG-084 A; Known Issues: 1 abierto — ground truth ai-draft; Notes: verify gate cumulativo). `pnpm verify` VERDE post-bump (502 tests + verify-manifest aceptó la nueva semver). `vsce package` produce `synaptic-sentinel-0.3.4.vsix` (1838 archivos, 3.13 MB, SHA-256 `c00cf2cddf5b05c9c40bc19191aa2aa421ed48283ca08100ea553732de16bce0`). Annotated tag `v0.3.4` + push origin + push tag. `gh release create v0.3.4` con asset .vsix + release notes basadas en CHANGELOG entry. URL: https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.3.4. `vsce publish` al Marketplace **NO ejecutado** — queda al usuario con su PAT (mismo patrón cierre PARCIAL de DG-080 B / DG-082 A). | 2026-05-26 | Cierre del "loop publicar" tras 6 sub-DGs consecutivos cerrando caveats (DG-083 → DG-088). Antes del DG-089 el `main` tenía 6 fixes que el `v0.3.3` del marketplace no incluía — deuda visible que confunde reportes de bugs. El verify gate cumulativo (DG-081 B activate runtime + DG-083 A manifest validity) específicamente fue construido para hacer este release seguro: si el gate pasa, el bundle activa y el manifest es válido. 5 de los 6 caveats originales heredados de v0.3.0 ahora cerrados (path leak / tokens proxy / gpt-5 reasoning / Ollama RAM / quota handling) = **83% del backlog resuelto**. Decisión cierre PARCIAL (igual que DG-080 B / DG-082 A): el GitHub Release queda público (asset descargable, instalable via `code --install-extension <ruta>.vsix`), pero `vsce publish` al Marketplace queda al usuario con su PAT — preserva la separación de responsabilidades (yo no tengo credenciales del usuario). Cuando ejecute el upload, una sola Entry follow-up cierra el ciclo completamente (continuación operacional, no nuevo DG). **Anti-optimismo ilusorio activo**: el marketplace upload tiene un riesgo no-cero de descubrir una clase 3 de bug no cubierta por los gates locales (DG-082.1 demostró que el upload manual puede revelar mismatches no detectables localmente). Si el upload acepta, follow-up Entry cierra; si no, nuevo sub-DG diagnostic |
| DG-088 | Sub-DG free tier quotas handling — `QuotaExhaustedError` tipado en el contrato + wrap en los 3 adapters + skip provider tras 2 consecutivos en el benchmark runner + sección "Providers throttled this run" en el reporter (cierra 1 de los 2 caveats heredados restantes de v0.3.x Known Issues) | **Option A** — NEW clase exportada `QuotaExhaustedError extends Error` en `packages/agents/src/llm-client.ts` con campos `providerLabel: string`, `httpStatus: number`, `retryAfterSeconds: number \| null`. Los 3 adapters wrappean errores quota: `openai-compatible-client.ts` helper `quotaErrorFromOpenAi` (status=429 OR regex `rate_limit\|quota\|insufficient_quota\|tokens_per_day\|requests_per_minute\|too_many_requests`); `anthropic-client.ts` helper `quotaErrorFromAnthropic` (incluye `overloaded` para 529); `ollama-client.ts` chequeo simple `response.status === 429` para Ollama Cloud/gateways managed. `try/catch` en el `chat.completions.create` / `messages.create` re-lanza como `QuotaExhaustedError` solo cuando aplica, preserva otros errores. Benchmark runner (`packages/cli/src/benchmark/run.ts`): `measureOnce` retorna `quotaExhausted?: boolean` cuando atrapa la excepción tipada; loop principal trackea `consecutiveQuotaErrors`; 2 consecutivos → set flag `quotaExhausted=true`, push error explicativo, break del loop interno + outer. `ProviderResult` gana campo opcional `quotaExhausted?: boolean`. Reporter (`packages/cli/src/benchmark/report.ts`): nueva sección `## Providers throttled this run` con tabla `Provider \| Completed runs before skip`; badge `⚠️ throttled` en el row del summary table del provider quota-exhausted. +8 unit tests (502 vs baseline 494) cross-package: 5 de adapters (429 → typed; non-quota error preservado) + 3 de reporter (sección renderea con flag; badge en summary; sección NO renderea sin flag). `pnpm verify` VERDE + ambos gates OK. NO bump version del .vsix v0.3.3. | 2026-05-26 | Sub-DG aislado que cierra el caveat 'free tier quotas Groq+Gemini exhaustion' descubierto en DG-077 (Groq agotó 100K TPD tras 2 corridas, Gemini RPM tras la 2da, dejando un 46% error rate opaco). Decisiones de diseño: (a) clase `Error` tipada (no plain Error + substring matching downstream) — facilita detection robusto en el runner via `instanceof`; (b) threshold 2 consecutivos para skip (no 1 — podría ser retry transitorio del SDK; no 5 — desperdicia 5 calls que pegan quota) — "evidencia razonable de quota persistente"; (c) contador NO se resetea con errores no-quota — si el provider intercala rate-limits con timeouts/parse errors sigue throttled funcionalmente, solo una run exitosa real lo libera; (d) regex permisivo de detection (múltiples substring patterns) — los providers cambian sus textos de error sin previo aviso; mejor false-positive ocasional (Error tratado como quota) que false-negative (quota tratado como error genérico); (e) Anthropic incluye `overloaded` en regex (529 con type='overloaded_error' bajo carga global — mismo skip behavior aplica); (f) badge `⚠️ throttled` en summary table + sección separada para diagnóstico: permite al lector distinguir "quota agotada" de "modelo roto" SIN ir a buscar la sección 'Providers throttled'. **Anti-optimismo ilusorio activo**: el IMPACTO REAL (usuario corriendo el benchmark con keys free-tier ve el badge + sección throttled en su reporte) sólo se valida con corrida real contra quotas que se agoten — diferido honestamente. Lo afirmable ahora: los 8 unit tests cubren extraction del 429 desde cada adapter + propagación via QuotaExhaustedError + render del badge y de la sección. 1 caveat heredado de v0.3.x sigue abierto: ground truth ai-draft (requiere AppSec engineer humano); 5 cerrados = 83% del backlog resuelto |
| DG-087 | Sub-DG Ollama heavy-model warning + "Don't remind me again" toggle en el Settings panel del VSCode extension (cierra 1 de los 3 caveats heredados restantes de v0.3.x Known Issues; add-on solicitado por el usuario) | **Option A** — NEW `listOllamaModelsWithInfo(): Promise<readonly OllamaModelInfo[]>` en `packages/agents/src/ollama-client.ts` retorna `{ name, sizeBytes }` por modelo del payload `/api/tags`; legacy `listOllamaModels` delega (cero break del API público). `settings-content.ts`: NEW `OllamaModelEntry` exportada, campo opcional `modelsInfo?` en `OllamaStatus` (fallback al render legacy si ausente), const exportada `HEAVY_MODEL_THRESHOLD_BYTES = 5 * 1024 * 1024 * 1024`, campo `suppressHeavyModelWarning: boolean` en `SettingsViewState`, helper `formatGb`, badge `⚠ heavy` cuando sizeBytes > umbral Y !suppress, panel-level warning con botón "Don't remind me again" cuando aplica. `settings-view.ts`: NEW const `SUPPRESS_HEAVY_MODEL_WARNING_KEY = 'synaptic-sentinel.suppressHeavyModelWarning'`, constructor gana 3er parámetro opcional `globalState?: vscode.Memento`, handler nuevo `dismiss-heavy-warning` que persiste cross-workspace en globalState. README de la extensión item 2 de Known Limitations reescrito con tabla recomendada de modelos ≤3 GB (gemma3:4b / qwen2.5-coder:7b / llama3.2:3b). +9 unit tests (494 vs baseline 485): 4 tests de `listOllamaModelsWithInfo` (size del payload, sizeBytes=0 defensivo, [] cuando Ollama no responde, listOllamaModels legacy intacto) + 5 tests del renderer (heavy badge + warning + botón cuando >5GB; size visible pero sin badge cuando ≤5GB; suppress=true oculta badge+warning; fallback legacy sin modelsInfo; sizeBytes=0 no renderea GB). `pnpm verify` VERDE 494 tests + ambos gates OK. NO bump version del .vsix v0.3.3. | 2026-05-26 | Sub-DG aislado que cierra el caveat 'Ollama RAM con modelos pesados' descubierto en DG-077 (Gemma 4 9.6 GB y gpt-oss:20b 13 GB saturaron RAM del usuario tras 2 horas). Add-on `Don't remind me again` implementado tal como el usuario lo pidió en el prompt del DG. Decisiones de diseño: (a) threshold 5 GB (no 7 ni 10) — excluye qwen2.5-coder:7b (~4.4 GB) y mistral-7b sin disparar warning, captura Gemma4 y arriba; (b) persistencia globalState (no workspaceState) — un usuario que entendió el caveat no quiere oírlo de nuevo en cada workspace; (c) constructor del provider acepta Memento como OPCIONAL preservando 100% backward-compat de tests legacy (mismo patrón que DG-085 A); (d) `modelsInfo` opcional en `OllamaStatus` con fallback al render legacy basado en `models: string[]` — preserva tests existentes; (e) `sizeBytes=0` se trata como "no info" en vez de renderear "0.0 GB" — protege contra payloads antiguos de Ollama. **Anti-optimismo ilusorio activo**: el IMPACTO REAL (interacción usuario-Settings panel) sólo se valida con dev host F5 / marketplace install; lo afirmable ahora son los 9 unit tests cubriendo 5 escenarios (heavy/light/suppressed/legacy/zero-size). 2 de los 6 caveats heredados de v0.3.x siguen abiertos (free tier quotas, ground truth ai-draft); 4 cerrados (DG-084 A path leak + DG-085 A tokens proxy + DG-086 A gpt-5 reasoning tokens + DG-087 A Ollama heavy warning) = 67% del backlog de Known Issues resuelto |
| DG-086 | Sub-DG gpt-5* reasoning tokens budget fix — bump del default `max_completion_tokens` de 1024 a 8192 SOLO para la familia gpt-5* en `OpenAiCompatibleLlmClient` (cierra 1 de los 4 caveats heredados restantes de v0.3.x Known Issues) | **Option A** — NEW constante `DEFAULT_MAX_TOKENS_GPT5 = 8192` en `packages/agents/src/openai-compatible-client.ts`. Lógica del `completeWithUsage` actualizada: `const tokens = request.maxTokens ?? (isGpt5 ? DEFAULT_MAX_TOKENS_GPT5 : DEFAULT_MAX_TOKENS)`. Override del caller via `request.maxTokens` SIEMPRE se respeta sin importar el model; non-gpt-5* models siguen con el default cross-provider de 1024 (regresión guard testeado). Comment del adapter actualizado a documentar TRES quirks de gpt-5*: (1) `max_completion_tokens` vs `max_tokens`; (2) rechazo de `temperature=0`; (3) reasoning tokens internos consumen el budget. Test existente cambia assert de 1024 → 8192 con comentario citando DG-086 A + root cause; +2 tests nuevos (anti-regresión override caller respeta `maxTokens: 512` aún para gpt-5*; regresión guard que gpt-4o-mini sigue con `max_tokens: 1024`). `pnpm verify` VERDE 485 tests (+2 vs baseline 483) + ambos gates OK. NO bump version del .vsix v0.3.3. | 2026-05-26 | Sub-DG aislado que cierra el caveat 'gpt-5* reasoning tokens consumen el budget' descubierto en DG-077 (3rd benchmark run cloud-only mostró 100% errors con `content=null` en gpt-5-nano). Root cause empírica: los reasoning tokens INTERNOS de gpt-5* (Chain-of-Thought elaboración interna, no visible al caller) consumen el techo de `max_completion_tokens` antes de emitir texto visible — con 1024 default, 1024 tokens enteros se evaporan en reasoning, dejando 0 para content. Decisión de diseño: **8192 como sweet spot empírico-defensivo** (4096 ya supera el techo observado pero ofrece menos margen; 16384 sería over-allocation que amplía el peor caso anti-rogue agent). Aplicar SOLO a gpt-5* porque el problema es específico de la familia (otros providers no tienen reasoning interno significativo at this date). Cost implication documentada: el cap **NO duplica el cost real** — el provider cobra por tokens GENERADOS, no por el cap configurado; el cap solo amplía el peor caso. Decisión deliberada de NO agregar flag CLI `--max-tokens` en este DG (anti-optimismo: cerrar lo prometido sin inflar scope; futuro sub-DG si emerge necesidad empírica). **Anti-optimismo ilusorio activo**: el IMPACTO REAL (gpt-5-nano deja de devolver content=null) sólo se puede medir corriendo el benchmark contra OpenAI con la API key del usuario — diferido honestamente. Lo afirmable ahora: el bug-cause documentado (reasoning consume el budget) se elimina al ampliar el budget. 3 de los 6 caveats heredados de v0.3.x siguen abiertos (Ollama RAM, free tier quotas, ground truth ai-draft); 3 cerrados (DG-084 A path leak, DG-085 A tokens proxy, DG-086 A gpt-5 reasoning tokens) |
| DG-085 | Sub-DG expose real LLM usage en el contrato `LlmClient` — reemplazar la proxy heurística `chars/4` (±15-20%) por los counts REALES de tokens que cada provider expone en su response (cierra 1 de los 5 caveats heredados de v0.3.x Known Issues) | **Option A** — método OPCIONAL `completeWithUsage(req): Promise<LlmCompletionResult>` agregado al contrato `LlmClient` con tipos nuevos `TokenUsage { inputTokens, outputTokens }` + `LlmCompletionResult { text, usage }`. Decisión clave de no-invasión: como método OPCIONAL del contrato (no cambio del return de `complete`), preserva 100% backward-compat de los 10+ fakeLlmClient existentes en tests (siguen implementando solo `complete`). Los 3 adapters reales implementan ambos métodos; `complete` queda como wrapper sobre `completeWithUsage`. NEW helpers exportados `parseAnthropicUsage` (extrae `usage.input_tokens` + `output_tokens` del SDK Anthropic), `parseOpenAiCompatibleUsage` (`choices.usage.prompt_tokens` + `completion_tokens` del protocolo OpenAI Chat Completions — sirve 14+ providers), `parseOllamaUsage` (`prompt_eval_count` + `eval_count` del `/api/chat` nativo). Cada helper devuelve `null` defensivamente si el shape no coincide. `TokenTrackingLlmClient` decorator agrega type guard `hasCompleteWithUsage(client)` + campo `usageSource: 'provider'`/`'proxy'` en `TokenUsageObservation`; prefiere usage real cuando disponible, fallback a proxy `chars/4` con `usageSource='proxy'` en 3 casos (legacy client sin método, provider sin usage en response, call que lanza). CLI `triage` rastrea `usageSourcesSeen: Set` y `renderCostSummary(tokenUsages, usageSources)` emite caveat condicional: 'provider-reported' / '~estimated chars/4 proxy' / 'mixed'. Persistencia `colony.db` tabla `triage_token_usage` NO cambia (cero schema migration; los counts almacenados son ahora reales cuando el provider los expone). +15 unit tests cubriendo extracción de usage en cada adapter + preferencia provider/proxy en el decorator. `pnpm verify` VERDE 483 tests (+15 vs baseline 468) + ambos gates OK. NO bump version del .vsix v0.3.3. | 2026-05-26 | Sub-DG aislado que cierra el caveat 'tokens proxy chars/4 ±15-20% vs facturado real' de v0.3.x Known Issues. Diseño no-invasivo (método opcional vs cambiar el return type del contrato): preserva los 10+ fakes existentes en tests sin tocarlos, mantiene la separación entre `complete` (consumido por los 3 agentes — solo necesita `text`) y `completeWithUsage` (consumido por el decorator de cost visibility). El campo `usage` nullable permite distinguir explícitamente "el provider no expuso usage" de "no había proxy fallback aplicable" — los helpers son defensivos contra drift del API. La decisión de NO persistir `usageSource` en `colony.db` evita una migración aditiva v5→v6 y mantiene el alcance: la información es metadata UX/runtime, no business data del registro; los counts almacenados ya son reales cuando el provider los expuso. **Anti-optimismo ilusorio activo**: el IMPACTO EMPÍRICO REAL (cost USD alineado <5% con la facturación del provider) solo se puede medir corriendo `triage` real contra providers reales con keys del usuario — diferido honestamente. Lo afirmable ahora: los 3 adapters devuelven `usage` real del payload (verificable por unit tests con payloads sintéticos idénticos a los del API real). 4 de los 6 caveats heredados de v0.3.x siguen abiertos (gpt-5* reasoning tokens, Ollama RAM, free tier quotas, ground truth ai-draft) como sub-DGs futuros candidatos |
| DG-084 | Sub-DG path leak fix en `buildSyntheticFinding` del cross-provider benchmark — anonimizar `Finding.location.path` antes de pasarlo al Brain Layer (cierra 1 de los 6 caveats heredados de v0.3.x Known Issues) | **Option A** — NEW función exportada `anonymizeFixturePath(fixturePath)` en `packages/cli/src/benchmark/scoring.ts` con heurística: split en segmentos, tomar el último (filename) + el penúltimo si está en `KNOWN_LANGS = {javascript, typescript, python}`, prefijar con `src/`. Resultados: `packages/scouts/tests/.../fixtures/vulnerable/javascript/eval.js` → `src/javascript/eval.js`; `packages/scouts/tests/gitleaks/fixtures/secrets/leaked.js` → `src/leaked.js`; `packages/scouts/tests/checkov/fixtures/iac/Dockerfile` → `src/Dockerfile`. `buildSyntheticFinding` ahora usa el path anonimizado en `location.path` Y en `fingerprint` (consistencia interna; el fingerprint sintético no se persiste cross-runs porque `scanId='benchmark'` — Findings reales no se ven afectados). Test existente que assertaba el path completo actualizado a versión anonimizada con comentario referenciando DG-084 A; 5 tests nuevos del helper (lang preservation javascript/typescript/python, lang stripping secrets/iac/vibe-coded, assertions negativos NO contiene 'tests'/'fixtures'/'vulnerable', edge cases path corto/vacío). `pnpm verify` VERDE 468 tests (+5 vs baseline 463) + ambos gates OK (verify-extension-activate + verify-manifest). NO bump version del .vsix v0.3.3. | 2026-05-25 | Sub-DG aislado que cierra el caveat 'path leak en synthetic findings' descubierto en DG-077 vía `--verbose` probe (root cause del 1.3% Triage PASS persistente de Anthropic: el LLM ve `tests/.../fixtures/vulnerable/` y razona "esto es código de prueba → inconclusive"). `src/` como prefijo es la convención más neutral en proyectos JS/TS/Python; preservar el lenguaje (cuando es claro) le da al LLM contexto sintáctico legítimo sin meta-razonamiento sobre 'test fixture'; descartar segmentos categoría-de-finding (secrets/iac/vibe-coded) porque `Finding.category` ya transmite la categoría legítimamente. **Anti-optimismo ilusorio activo**: el IMPACTO EMPÍRICO REAL (mejora del Triage PASS rate cross-provider) sólo se puede medir corriendo el benchmark contra providers reales con keys del usuario — eso queda diferido honestamente. Lo afirmable ahora: el path delator desaparece del prompt del LLM (verificable por unit tests + assertions negativos). El caveat del CHANGELOG se reescribirá a "path leak fix landed in DG-084 A; empirical validation deferred" en el próximo release. 5 de los 6 caveats heredados siguen abiertos (tokens proxy, gpt-5 reasoning tokens, Ollama RAM, free tier quotas, ground truth ai-draft) como sub-DGs futuros candidatos |
| DG-083 | Sub-DG `verify-manifest.mjs` — destila la lesson de DG-082.1 (publisher mismatch que el marketplace rechazó al subir v0.3.2) a un step PERMANENTE del verify gate | **Option A** — NEW `scripts/verify-manifest.mjs` (228 líneas) con 18 assertions sobre `packages/vscode-extension/package.json` (source, no .vsix): publisher='RealGoLab' (diagnostic cita DG-082.1), name='synaptic-sentinel', license='Apache-2.0', main='./dist/extension.cjs', version semver, engines.vscode='^1.95.0', icon field + file exists, displayName, description >10 chars, categories array, keywords array, repository.url contains 'github.com/golab-arch/synaptic-sentinel', homepage URL, bugs.url URL, activationEvents array, contributes.commands ≥ 5. Wireado al `pnpm verify` del root (`format:check && lint && build && test:unit && verify:extension-activate && verify:manifest`). Smoke test PASSED 18 checks. Adversarial test PASSED (corromper publisher 'RealGoLab' → 'GoLab' temporalmente → exit code 1 con diagnostic correcto citando DG-082.1, restore desde backup). No bump version del .vsix v0.3.3 (sigue siendo el release público) — cambio de tooling, no de producto. | 2026-05-25 | Sub-DG aislado destinado a fortalecer el verify gate como step PERMANENTE post-Phase-12. Mismo patrón que DG-081 B (destila lesson DG-079.1+.2 a verify-extension-activate.mjs). Validar el SOURCE package.json es suficiente para esta clase de bug (vsce copia el manifest tal cual al .vsix, sin mutarlo) y evita overhead de `vsce package` en cada verify. Cada EXPECTED value es una const TS al inicio del script — si necesita cambiar legítimamente (ej. bump engines.vscode minimum), modificar conscientemente; el commit hace el cambio auditable. **Anti-optimismo lesson v3 consolidada**: el verify gate es CUMULATIVO, no PREVENTIVO completo — cada release-blocker descubierto por acción humana real distinta agrega un step (activate runtime DG-081 B + manifest validity DG-083 A). Siempre puede existir una nueva clase de bug no descubierta hasta que una acción humana real la exponga |
| Q1 | Package manager / tooling de monorepo | **pnpm workspaces** (v10.33.0) | 2026-05-20 | Ya instalado; preferencia v0.4 §9.5; sin overhead |

**Discovery cerrado. Scaffolding generado, verificado y commiteado** (`f0b5202`, 54 archivos). **Cycle 2 CERRADO.** Siguiente: PASO 4 — Scout Layer.

---

## Technical Notes

- Entorno verificado (2026-05-20): Node v24.11.1, npm 11.6.2, pnpm 10.33.0, git 2.51.0, Python 3.14.0, Docker 28.4.0, WSL2 v2.7.3.0, `gh` CLI v2.88.1 (cuenta `golab-arch`).
- **Antivirus activo: Norton 360** (real-time Auto-Protect/SONAR). Windows Defender está en modo pasivo.
- Scaffolding generado: monorepo pnpm con 7 paquetes; build via `tsc -b` (project references); ESLint 9 flat + Prettier; Vitest 3. Verificado: `pnpm install/build/lint/test` todos en verde.
- Schema `colony.db` incluye 2 mejoras sobre v0.4 §3.5: tabla `meta` (`schema_version`) + `CHECK` constraints (defensa anti Memory Poisoning, §9.6).
- ⚠️ **Hallazgo de entorno — Norton 360 interfiere con el desarrollo**. Dos síntomas:
  - **L-001 — Inspección TLS**: `pnpm install` falla con `UNABLE_TO_VERIFY_LEAF_SIGNATURE`. Workaround permanente: `NODE_OPTIONS=--use-system-ca`.
  - **L-002 — Bloqueo de escritura git**: `git add`/`commit` fallan con `Permission denied` en `.git/objects/` (Auto-Protect de Norton). Fix raíz (DG-007 A): excluir `D:\GoLAB\PROYECTOS\SENTINEL` de Auto-Protect/SONAR en Norton 360.
  - Impacto futuro: `scripts/install-scanners.ts` (DG-003 A) sufriría lo mismo — la exclusión lo previene.
- ✅ Observación resuelta (DG-031 A): Python 3.14 vs Checkov — Checkov se integró vía su binario standalone (PyInstaller onefile); no requiere un intérprete de Python en el cliente.
- ✅ Verificado (DG-032, BITACORA Entry #34): la llamada LLM real a Anthropic — los 2 tests de integración del Brain Layer (Triage/Context) pasaron contra la Messages API (Haiku 4.5) con una API key BYOK provista por el usuario. El round-trip `AnthropicLlmClient`→API→parseo→validación `zod` queda verificado.
- ⚠️ Observación: `better-sqlite3` es módulo nativo — evaluar `node:sqlite` por ABI con Electron de VSCode.
- ⚠️ Observación: `exactOptionalPropertyTypes` (tsconfig) es muy estricto — relajable puntualmente si genera fricción.

---

## Architecture Changes

- 2026-05-20 — Cycle 1: arquitectura física fijada como monorepo único pnpm (DG-001 B), split OSS/Pro por workspace marcado.
- 2026-05-20 — Cycle 2: scaffolding generado — 7 paquetes (shared, core, scouts, cli, reporters, vscode-extension, agents). Build con `tsc -b` project references. Primer commit atómico `f0b5202` en `main` (54 archivos) tras resolver el bloqueo de Norton 360 (DG-007 A).
- 2026-05-20 — Cycle 3: PASO 4 Increment 1 — tipos compartidos (`Finding`, `Pheromone`, `Scan`, severidades) con `zod`, e interfaz `ScoutAgent`. 28 tests verdes.
- 2026-05-20 — Cycle 4: PASO 4 Increment 2 — `scripts/install-scanners.ts` con manifest pinneado y checksums SHA-256. OpenGrep v1.22.0 descargado y verificado. 32 tests verdes.
- 2026-05-21 — Cycle 5: PASO 4 Increment 3 — `OpenGrepScout` (implementa `ScoutAgent`) + normalizer (OpenGrep JSON → `Finding`). 48 tests verdes.
- 2026-05-21 — Cycle 6: PASO 4 Increment 4 — ruleset baseline + fixtures vulnerables + tests de integración con OpenGrep real. **PASO 4 COMPLETO.** 51 tests verdes.
- 2026-05-21 — Cycle 7: capa `colony.db` (clase `ColonyDb` sobre `node:sqlite`) — DG-012 B.1. 59 tests verdes.
- 2026-05-21 — Cycle 8: contrato `ScoutAgent` movido a `core`; `Coordinator` stage 1 (orquesta scouts → feromonas en `colony.db`) — DG-012 B.2. **DG-012 B COMPLETO.** 63 tests verdes.
- 2026-05-21 — Cycle 9: CLI `synaptic-sentinel scan` (DG-015 A) — primer comando ejecutable; scan end-to-end desde terminal. 67 tests verdes.
- 2026-05-21 — Cycle 10: paquete `reporters` — modelo del tomo + export JSON con firma SHA-256 (DG-016 B); `scan --export`. 73 tests verdes.
- 2026-05-21 — Cycle 11: `install-scanners` soporta assets comprimidos (extracción via `tar`); Gitleaks v8.30.1 instalable (DG-017 A.1).
- 2026-05-21 — Cycle 12: `GitleaksScout` de punta a punta (DG-018 B) — wrapper de Gitleaks (categoría Secrets, `--redact`) + normalizer + integración real; `runProcess` extraído a módulo compartido; el CLI `scan` corre OpenGrep **y** Gitleaks. 86 tests verdes. **Cobertura de secrets operativa.**
- 2026-05-21 — Cycle 13: `Coordinator` stage 2 (DG-019 A) — dedup por `fingerprint`, supresión de falsos positivos confirmados (`fp_known`) y clasificación de ciclo de vida (`new`/`known`) en re-scans. Nuevo contrato `FpKnownPayload` + `buildFpKnownPheromone`; `ColonyDb.getKnownFingerprints`; `ScanOutcome.suppressedCount`. 94 tests verdes.
- 2026-05-21 — Cycle 14: extensión VSCode MVP (DG-020 B / DG-021 A) — **arquitectura spawn-CLI**: la extensión lanza la CLI como child process, lee el tomo exportado y pinta los hallazgos como diagnostics inline. Capa UX delgada (módulos puros desacoplados de la API `vscode`); bundle `esbuild` → `dist/extension.cjs`, **sin `node:sqlite`** en el extension host. Fix de la CLI: `findScannersRoot` resuelve `.scanners/` sin depender del `cwd`. **Inicio de la fase Inline UX.** 110 tests verdes.
- 2026-05-21 — Cycle 15: cierre del lazo Inline UX (DG-022 B) — comando CLI `mark-fp` (registra una feromona `fp_known`, idempotente) + Code Action "marcar falso positivo" en la extensión + status bar. El Code Action dispara `mark-fp` → `fp_known` → el `Coordinator` stage 2 lo suprime en el próximo scan. Cierra FI-007. 119 tests verdes.
- 2026-05-21 — Cycle 16: reporter HTML del tomo (DG-023 A) — `renderTomoHtml` produce un informe HTML autocontenido (todo el contenido dinámico escapado); `TomoSummary` gana `suppressedCount` (FI-006); el CLI gana `--export-html`. El tomo es exportable a JSON **y** HTML. Cierra FI-006. 123 tests verdes.
- 2026-05-21 — Cycle 17: Brain Layer increment 1 (DG-024 B) — paquete `agents` (capa Cerebro, Pro): contrato `BrainAgent` (prompt + parser, **no microservicio**), frontera `LlmClient`, `AnthropicLlmClient` (BYOK, cliente `fetch` propio) y un **Triage Agent** mínimo (`Finding` → `TriageVerdict`, validado con zod). La respuesta del LLM se valida como entrada no confiable (Memory Poisoning). **Inicio de la fase Brain Layer.** 145 tests verdes + 1 skipped.
- 2026-05-21 — Cycle 18: Brain Layer increment 2 (DG-025 A) — comando CLI `triage` que corre el Triage Agent sobre los hallazgos del último scan (BYOK), salta `fp_known` y los ya triados (economía de tokens, v0.4 §187) y persiste los veredictos. **Schema v2**: nueva tabla `triage_verdicts` (cambio aditivo via `CREATE TABLE IF NOT EXISTS`); los tipos de triage se movieron a `core`. El paquete `cli` ahora depende de `agents` (Pro). 161 tests verdes + 1 skipped.
- 2026-05-21 — Cycle 19: surface del triage en el tomo (DG-026 A) — `TomoFinding` = `Finding` + `triage` opcional; `buildTomo` hace join por `fingerprint` con los veredictos de `colony.db`; el resumen gana `byTriage` y el HTML muestra el veredicto por hallazgo. El tomo pasa de "hallazgos crudos" a "hallazgos triados". 165 tests verdes + 1 skipped.
- 2026-05-21 — Cycle 20: triage en la extensión VSCode (DG-027 B) — comandos `Triage Findings` y `Set Anthropic API Key`; **BYOK vía `vscode.SecretStorage`** (key cifrada por el SO, pasada al child process por entorno, nunca por argumentos); los veredictos se anotan en los diagnostics del IDE. El Brain Layer es alcanzable desde la superficie primaria. 171 tests verdes + 1 skipped.
- 2026-05-21 — Cycle 21: Context Agent (DG-028 B) — 2.º agente del Brain Layer: `ContextAgent` (`BrainAgent<Finding, ContextExplanation>`) explica la cadena de explotabilidad (entrada → sink → exposición) de un hallazgo confirmado. `ContextExplanation` en `core`; `extractJsonObject` reubicado a `brain-agent.ts` (util compartido). Sin wiring al pipeline aún. 180 tests verdes + 2 skipped.
- 2026-05-21 — Cycle 22: wire del Context Agent (DG-029 A) — el comando `triage` corre el Context Agent sobre los true positives; las explicaciones se persisten (**schema v3**, tabla `context_explanations`) y se surfacean en el tomo (JSON + HTML). `buildTomo` refactorizado a un objeto `TomoEnrichment` (extensible para futuros agentes). 187 tests verdes + 2 skipped.
- 2026-05-21 — Cycle 23: `TrivyScout` (DG-030 A) — cuarto scout: **Trivy v0.70.0** (SCA, dependencias vulnerables). `scouts/trivy/` (output schema + normalizer Trivy→`Finding` categoría `SCA` + `TrivyScout`); el CLI corre 3 scouts. Fix de `install-scanners`: extracción de `.zip` en Windows vía PowerShell `Expand-Archive` (el `tar` de Windows interpretaba `D:\...` como host remoto). 197 tests verdes + 2 skipped.
- 2026-05-21 — Cycle 24: `CheckovScout` (DG-031 A) — quinto scout: **Checkov 3.2.529** (IaC, misconfiguraciones en Dockerfile/Terraform/k8s). `scouts/checkov/` (output schema unión objeto|array + normalizer Checkov→`Finding` categoría `IaC`, severidad `null` de Checkov OSS → `medium` + `CheckovScout`); el CLI corre 4 scouts. Fix de `install-scanners`: extracción de `.zip` en Unix vía `unzip` + campo `archiveDir` que aplana el binario empaquetado bajo `dist/`. Checkov se integra como binario standalone (sin intérprete Python). 208 tests verdes + 2 skipped.
- 2026-05-21 — Cycle 25: `VibeDetectScout` (DG-032 B) — scout de detección de código *vibe-coded* (categoría `VibeCoded`), el scout firma del producto vibe-coding-native. `scouts/vibe-detect/` (`detectors.ts` — catálogo curado de 6 detectores heurísticos regex; `detect.ts` — `runVibeDetectors` función pura; `vibe-detect-scout.ts` — `VibeDetectScout` con walker propio del árbol de archivos). Detección **nativa en TypeScript, sin binario OSS**: siempre disponible, 100% determinista. El CLI corre 5 scouts. Además — gap histórico cerrado: los 2 tests de integración del Brain Layer (Triage/Context) se corrieron con una API key BYOK del usuario y **pasaron contra la API real de Anthropic** (Haiku 4.5). `cli-runner.test` timeout 60s→120s (FI-002). 221 tests verdes + 2 gated.
- 2026-05-21 — Cycle 26: `Remediation Agent` (DG-033 A) — 3.er y último agente del Brain Layer: propone cómo corregir un verdadero positivo. `core/types/remediation.ts` (`RemediationSuggestion` + `Record`); **colony.db schema v4** (tabla aditiva `remediation_suggestions`); `agents/remediation-agent.ts` (`RemediationAgent` — `BrainAgent<Finding, RemediationSuggestion>`); wiring en el comando `triage` (corre sobre los TP junto a Context) y surface en el tomo JSON/HTML. Completa el trío Triage→Context→Remediation. RemediationAgent verificado contra la API real de Anthropic; E2E scan→triage→export con el bloque de remediación en el HTML. 241 tests verdes + 3 gated.
- 2026-05-21 — Cycle 27: surface del Brain Layer en la extensión (DG-034 B) — la extensión VSCode muestra el Brain Layer completo: `HoverProvider` con triage + cadena de contexto + remediación sobre los hallazgos bajo el cursor, y una Code Action "copiar remediación sugerida". `vscode-extension/` (`tomo.ts` — `ExtensionFinding` gana context/remediation; `diagnostics.ts` — `findingHoverMarkdown` + `remediationClipboardText` puras; `index.ts` — HoverProvider + comando + Code Action). **`.vscode/launch.json`** creado: la extensión ya es F5-testeable. Decisión honesta: la Code Action copia al portapapeles, no inserta en el buffer (la remediación es orientativa, no un patch). 250 tests verdes + 3 gated.
- 2026-05-21 — Cycle 28: documento de onboarding (DG-035 A) — `ONBOARDING.md` (guía completa: instalación, uso de la CLI y la extensión, arquitectura, troubleshooting); `README.md` al día (5 scouts, Brain Layer con 3 agentes, licencias reales); `docs/colony-db.md` con las tablas v2-v4. Hallazgo honesto: `format:check` —fuera del gate de verificación por ciclo— revela drift de Prettier preexistente en ~41 archivos → **FI-010**. Ciclo de documentación, sin cambios de código: 250 tests verdes + 3 gated.
- 2026-05-21 — Cycle 29: kill-switch del Coordinator (DG-036 B) — presupuesto de tiempo por scout (v0.4 §9.6 "Rogue Agents"). `ScanOptions.scoutTimeoutMs` (default 5 min); cada scout corre con una `AbortSignal` propia —enlazada al `signal` del llamante— y su ejecución compite (`Promise.race`) contra la cancelación: un scout colgado que ignora su signal se cancela igual y se reporta `failed`, sin colgar el scan. Sin cambios en el CLI (usa el presupuesto por defecto). 253 tests verdes + 3 gated.
- 2026-05-21 — Cycle 30: salida verbose y dinámica de la CLI (DG-037 B, increment 1/3) — tras la prueba F5 del usuario (Entry #39, gap "extensión en vivo" cerrado). `reporters/console-reporter.ts` (render puro: banner, glifos/colores por severidad v0.4 §4.3, `renderScanReveal`); `Coordinator.onScoutSettled` (callback de progreso); `cli/spinner.ts` (spinner braille); el comando `scan` imprime banner → drip en vivo por scout → reveal coloreado, con `--no-color` / `shouldUseColor`. Arquitectura "un show, dos superficies": el motor vive en la CLI, la extensión lo reusará (increment 2). 267 tests verdes + 3 gated.
- 2026-05-21 — Cycle 31: pseudoterminal verbose en la extensión (DG-038 B, increment 2/3) — la extensión transmite el "show" de la CLI a un Pseudoterminal nativo de VSCode. `vscode-extension/terminal.ts` (`SentinelTerminal`), `terminal-format.ts` (`toCrlf`), `cli-runner.ts` (streaming vía `onOutput` + `FORCE_COLOR=1`); los comandos Scan y Triage abren la terminal "Synaptic Sentinel". `cli/triage.ts` enriquecido (banner + `renderTriageTag`). `.gitignore` ignora los dirs extraños de subpaquetes. Cierra "un show, dos superficies" con API nativa (sin webview). 273 tests verdes + 3 gated.
- 2026-05-21 — Cycle 32: webview "tomo vivo" (DG-039 B, increment 3/3 — cierra DG-037 B) — panel lateral con los hallazgos del último scan, agrupados por severidad y clickeables para saltar al código (v0.4 §4.3). `vscode-extension/webview-content.ts` (`renderTomoWebviewHtml`, render puro con CSP+nonce y `escapeHtml`), `tomo-view.ts` (`SentinelTomoViewProvider`), `package.json` (`contributes.views`). Arquitectura spawn-CLI intacta (`escapeHtml` propio, no se importa el motor). **UX verbose completa**: CLI → pseudoterminal → webview. 279 tests verdes + 3 gated.
- 2026-05-21 — Cycle 33: la colonia aprende — `learning_records` increment 1 (DG-040 B) — activa la tabla `learning_records`, inerte desde v1 (v0.4 §3.5). `core/types/learning.ts` (`LearningRecord`, `patternSignature` = `${category}:${ruleId}`, `triageClassificationToLearning`); `colony-db.ts` (`recordLearningBatch` — upsert por `(pattern_signature, classification)`; `getLearningRecords`); `cli/triage.ts` alimenta `learning_records` con los veredictos decisivos. `learning_records` generaliza por patrón (cross-scan/cross-ubicación), distinto de `triage_verdicts` (por `fingerprint` exacto). Sin cambio de schema. E2E verificado. 289 tests verdes + 3 gated.
- 2026-05-21 — Cycle 34: la colonia aprende — lado lectura (DG-041 B, cierra "la colonia aprende") — `core/types/learning.ts` (`deriveFromLearning`, umbral de evidencia 3): el `triage` consulta `learning_records` y pre-clasifica los patrones conocidos —evidencia fuerte y consistente— sin gastar una llamada LLM (economía de tokens, v0.4 §187). El veredicto derivado se persiste con `agentId` `colony-learning` y nunca se realimenta (sin bucle). E2E sin costo de API: 7 hallazgos pre-clasificados desde la memoria, 0 llamadas LLM. 294 tests verdes + 3 gated.
- 2026-05-21 — Cycle 35: higiene del repo (DG-042 A, cierra FI-010) — `pnpm format` sobre 44 archivos con drift (cambios puramente de estilo) + nuevo script raíz `verify` (`format:check && lint && build && test`) que codifica el gate por ciclo como un único comando. Hallazgo de fondo: un comentario de `cli-runner.test.ts` había quedado colapsado en una línea con `//` embebidos y en posición colgante entre los argumentos de `it(...)` — Prettier no podía estabilizarlo (`prettier(input) !== prettier(prettier(input))`, verificado con `--debug-check`); reescrito como bloque de líneas independientes movido antes de la llamada. `pnpm verify` completo en verde. 294 tests verdes + 3 gated.
- 2026-05-22 — Cycle 36: ampliación del catálogo SAST (DG-043 B, atiende FI-003) — `sentinel-baseline.yaml` pasa de 4 a 11 reglas pattern-based deterministas. 7 reglas nuevas — JS/TS: `setTimeout`/`setInterval` con string (CWE-95), `document.write`/`writeln` (CWE-79), asignación dinámica a `innerHTML` (CWE-79, excluye literales con `pattern-not`); Python: `os.system` (CWE-78), `yaml.load` sin SafeLoader (CWE-502), `pickle.load`/`loads` (CWE-502), `hashlib.md5` (CWE-327). Cobertura: ejecución dinámica de código, inyección de comandos, XSS, deserialización insegura y criptografía débil, con refs CWE + OWASP 2021. 2 fixtures deliberadamente vulnerables + 2 tests de integración contra el binario real de OpenGrep. El taint analysis queda diferido (FI-003 sigue abierto, acotado a taint). `pnpm verify` completo en verde. 296 tests verdes + 3 gated.
- 2026-05-22 — Cycle 37: exportador SARIF 2.1.0 del tomo (DG-044 B) — nuevo reporter `reporters/src/sarif-reporter.ts` (`renderTomoSarif`, puro y determinista) + flag CLI `--export-sarif`. SARIF (OASIS Static Analysis Results Interchange Format) es el estándar de intercambio de hallazgos de seguridad: vuelve la salida de Sentinel CI-native (GitHub Code Scanning, Azure DevOps). Reglas (`reportingDescriptor`) deduplicadas por `ruleId` y referenciadas por `ruleIndex`; severidad→`level` (critical/high=error, medium=warning, low/info=note) y →`security-severity` (0-10, que GitHub lee de la regla); el `fingerprint` estable de cada hallazgo se publica como `partialFingerprints`. SARIF ya estaba previsto como formato OSS en el `index.ts` de `reporters`. 8 tests unitarios que parsean la salida. `pnpm verify` completo en verde (~152 s, dominado por integración — FI-002 gana urgencia). 304 tests verdes + 3 gated.
- 2026-05-22 — Cycle 38: política de exit code `--fail-on` para CI (DG-045 B) — el comando `scan` gana el flag `--fail-on <severidad>`: termina con **exit code 2** si hay hallazgos de severidad ≥ umbral, completando la historia CI-native que abrió SARIF (DG-044) — SARIF reporta, el exit code bloquea el pipeline. `countBlockingFindings` es un helper puro y exportado (verificable sin correr un scan); reúsa `severityAtLeast` del core. El exit 2 es distinto del 1 (error de ejecución): un consumidor de CI los distingue. `--fail-on` es **opt-in** — sin el flag, `scan` sigue en exit 0, así que la extensión VSCode (`cli-runner`/`assertCliOk`, que nunca pasa `--fail-on`) no se ve afectada; riesgo señalado en el DG y verificado. Verificación: 5 tests unitarios + e2e real con la CLI construida (4 exit codes exactos: `--fail-on high`→2, `critical`→0, sin flag→0, inválido→1). `pnpm verify` completo en verde. 309 tests verdes + 3 gated. Instrucción permanente del usuario desde este DG: cada DG futuro debe incluir la recomendación explícita del asistente sobre las opciones.
- 2026-05-22 — Cycle 39: separación `test:unit` / `test:integration` (DG-046 A, cierra FI-002) — la suite Vitest se divide en dos proyectos vía `test.projects`: **`unit`** (lógica pura, ~7 s) e **`integration`** (binarios reales de los escáneres, red, CLI construida — lento). El gate por ciclo (`verify`) pasa de `pnpm test` a `pnpm test:unit`; `test:integration` se invoca explícitamente ante un commit de feature o un release. Split **por proyecto Vitest** (`--project <name>`), no por glob en los scripts — robusto cross-platform (sin problemas de quoting entre cmd y bash). Los tests de integración se identifican por el sufijo `*integration.test.ts`; `cli-runner.test.ts` era mixto y se separó en `cli-runner.test.ts` (unit) + `cli-runner.integration.test.ts`. Verificado **sin pérdida de tests**: 300 unit + 9 + 3 gated integration = 309 + 3, idéntico al pre-split. `pnpm verify` completo en verde. 309 tests verdes + 3 gated.
- 2026-05-22 — Cycle 40: `ruleId` canónico de OpenGrep (DG-047 A, cierra FI-005) — al correr OpenGrep con `--config <archivo>`, el `check_id` viene prefijado con los segmentos de la ruta de ese archivo (`d.tmp.probe.js-eval-usage`) y el `Finding.ruleId` lo heredaba. `deriveTitle` se renombró a `canonicalRuleId` y normaliza el `ruleId` al último segmento (el id real de la regla); `title` usa el mismo valor. Beneficio: `ruleId` estable independiente de la ubicación del archivo de reglas → `patternSignature` de la memoria del enjambre y `rule.id` del SARIF limpios. Verificado e2e contra OpenGrep real (`ruleId === 'sentinel-js-eval-usage'`). 310 tests verdes + 3 gated. **Nuevo requisito del usuario** abierto en este ciclo: toda la salida del producto al usuario final debe migrar a inglés — registrado como **FI-011** (5 etapas), a abordar en DGs futuros; desde ya, todo string de salida nuevo se escribe en inglés.
- 2026-05-22 — Cycle 41: salida de la CLI en inglés (DG-048 A, FI-011 etapa 1) — primer increment de la migración a inglés: toda la salida de la CLI al usuario final pasa a inglés. `console-reporter.ts` (banner, `renderScoutLine`, `renderScanReveal` — el `console-reporter` es el renderer de la CLI), `index.ts` (`USAGE`/ayuda completa y mensajes de error), `scan.ts` (aviso de scanners, spinner, `Tome exported`, mensajes de `--fail-on`), `triage.ts` (encabezado, tags, contexto/remediación, resumen, `rationale` de los veredictos derivados de la memoria), `mark-fp.ts` (mensajes de resultado y error). Los prompts de los agentes (etapa 5) no se tocan. Verificado e2e (`--help` y `scan --fail-on`). 310 tests verdes + 3 gated. Restan las etapas 2-5 de FI-011.
- 2026-05-22 — Cycle 42: mensajes de los scouts en inglés (DG-049 A, FI-011 etapa 2) — se migran a inglés todos los strings que un scout propio pone en un `Finding`: los 11 `message` de `sentinel-baseline.yaml` (reglas SAST de OpenGrep), el `title`+`message` de los 6 detectores de Vibe-Detect (`detectors.ts`), y el glue propio de los normalizers de Trivy (`is vulnerable`, `fixed in`, `no fixed version available`) y Checkov (`Guideline:`). Gitleaks/Trivy/Checkov emiten el grueso de su descripción en inglés nativamente — solo se traduce el texto propio. Efecto multiplicador: esos mensajes se propagan al tomo, al SARIF y al hover de la extensión. Verificado e2e (el `message` del `Finding` en el tomo exportado sale en inglés) y con `test:integration` (el YAML traducido sigue válido para OpenGrep). 310 tests verdes + 3 gated. Restan las etapas 3-5 de FI-011.
- 2026-05-22 — Cycle 43: reporter HTML del tomo en inglés (DG-050 A, FI-011 etapa 3) — los labels del reporter HTML (`html-reporter.ts`) migran a inglés: `lang="en"`, título/`h1` "Audit tome", encabezados (Summary, Methodology, Findings), labels de triage/contexto/remediación (true/false positive, inconclusive, Context, Entry point, Exposure, Remediation), columnas de la tabla (Status, Findings), línea de resumen, "By severity/category/triage", "No findings", pie de integridad ("Integrity", "Generated by … tome"). El CSS y la estructura no cambian. `html-reporter.test.ts`: 6 aserciones de strings actualizadas. Verificado e2e (el tomo HTML exportado sale íntegramente en inglés). 310 tests verdes + 3 gated. Restan las etapas 4-5 de FI-011.
- 2026-05-22 — Cycle 44: extensión VSCode en inglés (DG-051 A, FI-011 etapa 4) — la UI de la extensión —superficie primaria del producto (v0.4 VSCode-primary)— migra a inglés: `package.json` (`description`, descripción del setting `cliPath`), `index.ts` (status bar, títulos de Code Actions, prompts y notificaciones de scan/triage/mark-fp/API key), `diagnostics.ts` (`triageLabel`, labels del hover, texto del portapapeles), `webview-content.ts` (estado vacío, labels, `lang="en"`), `cli-runner.ts` (error de `assertCliOk`), `tomo-view.ts` (warning). Solo strings; sin cambios de lógica. `diagnostics.test.ts` y `webview-content.test.ts`: aserciones actualizadas. Verificado por los unit tests de los renderers + `pnpm verify` + `test:integration`. 310 tests verdes + 3 gated. Resta solo la etapa 5 de FI-011 (prompts del Brain Layer).
- 2026-05-22 — Cycle 45: prompts del Brain Layer en inglés (DG-052 A, FI-011 etapa 5 — **cierra FI-011**) — los `SYSTEM_PROMPT` y user prompts de los 3 agentes (`triage-agent.ts`, `context-agent.ts`, `remediation-agent.ts`) migran a inglés, de modo que el LLM produzca veredictos/contexto/remediaciones en inglés; los mensajes de error de `parseResponse` y de `extractJsonObject` (`brain-agent.ts`) también. La forma JSON pedida **no cambia** (field names idénticos) — el parser y los schemas zod no se ven afectados. El `fakeLlm` del test de `triage` discriminaba el Context Agent por una frase del system prompt; reanclado a `exploitability chain`. **FI-011 queda cerrado**: las 5 etapas (CLI, mensajes de scouts, reporter HTML, extensión, Brain Layer) están migradas — el producto habla inglés de punta a punta. Verificado con `pnpm verify` (test:unit 301; los unit tests de los 3 agentes con la forma JSON intacta). 310 tests verdes + 3 gated.
- 2026-05-22 — Cycle 46: **abre Phase 8 (Distribución)** — cache de scanners global por usuario (DG-053 C, FI-004 sub-increment 1) — `resolveScannerBinary` resuelve los binarios de los scanners también desde una cache global por usuario (`~/.synaptic-sentinel/scanners`), no solo desde la cache `.scanners/` del repo en dev. Nuevos `globalScannerCacheDir()` y `findBinaryInCache()`; orden de resolución: explícito → env → `.scanners/` del repo → cache global (la del repo mantiene prioridad → sin cambio de comportamiento en dev). Es el prerequisito de FI-008 (`.vsix`): un producto enviado no tiene el repo. Alcance acotado a la **resolución**; la auto-instalación en la cache global (descarga de red on-demand) es el sub-increment 2 de FI-004. 3 tests nuevos; el test de "no está en ninguna cache" pasa a ser determinista (recibe un `globalCacheDir` inyectado). `pnpm verify` verde (test:unit 304) + `test:integration` verde (9 + 3 gated). 313 tests verdes + 3 gated.
- 2026-05-22 — Cycle 47: modo `--global` de `install-scanners` (DG-054 A, FI-004 sub-increment 2 — **cierra FI-004**) — `scripts/install-scanners.ts` gana un modo `--global` que instala los binarios de los scanners en la cache global por usuario (`~/.synaptic-sentinel/scanners`), el layout que DG-053 ya resuelve. Cierra el lazo de FI-004: resolver (DG-053) + instalar (esto) → la cache global queda operativa. `installScanner` no cambió (ya parametrizaba `installDir`); el cambio es solo de `main()`, que elige el destino según el flag. Nuevo `globalScannerCacheDir()` (espeja la definición de `cli/scan.ts`). De paso, la salida del script —antes en español— se migró a inglés (consistencia con el producto; el script no era parte de las 5 etapas de FI-011, pero se alinea con la directiva). Verificación: `pnpm verify` verde (test:unit 304) + **e2e real** del modo `--global` sin red (cache global poblada con una copia de `.scanners/` → los 4 scanners "cache OK"). El disparo on-demand del install (cuando falta un scanner) es parte del empaquetado FI-008. 313 tests verdes + 3 gated.
- 2026-05-22 — Cycle 48: la CLI corre con el Node del extension host (DG-055 A, FI-008 sub-increment 1) — `spawnCli` (`cli-runner.ts`) lanza la CLI como child process con `process.execPath` —el Node con el que VSCode corre el extension host— por defecto, en vez de `node` del `PATH`. Una extensión empaquetada (`.vsix`) instalada por un tercero no puede asumir que el usuario tenga `node` en el `PATH`. `nodePath` sigue siendo inyectable (override en tests). Cambio de un solo default; sin cambio de comportamiento en dev. `pnpm verify` verde (test:unit 304) + `test:integration` verde (9 + 3 gated). 313 tests verdes + 3 gated. **Instrucción del usuario** en este ciclo: hacer commit **y push** tras cada ciclo. El repo no tenía remoto (DG-002 A lo difirió); como el monorepo contiene código Pro (`packages/agents`, LICENSE-PRO), el remoto de desarrollo debe ser **privado** — la configuración del remoto y el primer push se elevan al usuario.
- 2026-05-22 — Cycle 48 (infra, Entry #59): **remoto git configurado** — `origin` → `github.com/golab-arch/synaptic-sentinel`, repo **privado** (el monorepo contiene código Pro). Cierra el diferimiento de DG-002 A; desde ahora cada cierre de ciclo hace `git push`. Secret scan previo: sin secretos reales en el historial.
- 2026-05-22 — Cycle 49: manifest de la extensión preparado para `vsce` (DG-056 A, FI-008) — `packages/vscode-extension/package.json` gana `repository` (con `directory`, apuntando al monorepo recién publicado), `homepage`, `bugs` y `keywords`; se agregan `.vscodeignore` (el `.vsix` shippea solo `dist/`, no `src/`/`tests/`/`node_modules`), `README.md` (página de marketplace, en inglés) y `LICENSE` (copia del Apache-2.0 raíz). Solo metadata, sin tocar lógica. El tooling `@vscode/vsce` y el script `vscode:prepublish` son del próximo sub-increment (empaquetado). **Limitación honesta:** la validación `vsce ls` del manifest no se pudo correr — `npx @vscode/vsce` se cuelga en la descarga en este entorno (Norton/TLS, 2 intentos sin output); queda para el ciclo de empaquetado. `pnpm verify` verde (test:unit 304). 313 tests verdes + 3 gated.
- 2026-05-22 — Cycle 50: la CLI bundleada dentro de la extensión (DG-057 B, FI-008) — **último ciclo del Tomo 001.** El script `bundle` de `vscode-extension` produce ahora dos artefactos esbuild: `extension.cjs` (extension host, CJS) y `cli.mjs` (la CLI, **ESM**). La CLI bundleada queda dentro de la extensión (`dist/cli.mjs`); `defaultCliEntry` la resuelve ahí. esbuild **no** procesa `new URL(…, import.meta.url)` (es un patrón de webpack, no de esbuild): los dos assets que la CLI lee en runtime —`schema.sql` (core), `sentinel-baseline.yaml` (scouts)— se copian junto al bundle con un script post-bundle nuevo (`scripts/copy-cli-assets.mjs`); `colony-db.ts` y `rules.ts` resuelven el path canónico de `src/` primero y, si no existe (caso bundle), caen al asset hermano del bundle. La CLI bundleada va en ESM porque `import.meta.url` —usado para resolver los assets— solo existe en ESM. Verificación: `pnpm verify` verde (test:unit 304) + **e2e real** de la CLI bundleada (`node dist/cli.mjs scan` → 1 finding, `colony.db` creada) + `test:integration` verde (9 passed, incluido `cli-runner` contra el bundle). 313 tests verdes + 3 gated. Caveat honesto: la CLI bundleada usa `node:sqlite` vía el Node del extension host — si ese Node es < 22.5, el `.vsix` no correrá end-to-end hasta resolver FI-001.
- 2026-05-22 — **Tomo 001 CERRADO** (Cycles 1-50, 2026-05-20 → 2026-05-22; 50 ciclos, 100% de éxito). Archivado en `.synaptic/tomes/tome-001.{json,md}`. **Tomo 002 abierto** desde el Cycle 51.
- 2026-05-22 — Cycle 58: marketplace polish v0.1.0 (DG-065 A) — primer ciclo post-cero-deuda; alineación con la familia SYNAPTIC (sibling SYNAPTIC Expert revisado en `D:\GoLAB\PROYECTOS\SYNAPTIC_VSC_EXTENSION` por instrucción del usuario). `package.json`: `version` `0.0.0`→**`0.1.0`**, `publisher` `golab`→**`RealGoLab`** (mismo namespace que el sibling; id de marketplace pasa a `RealGoLab.synaptic-sentinel`), `+icon: 'media/icon.png'`, `+galleryBanner: {color:'#1a1a2e', theme:'dark'}` (mismo color que el sibling), `+activationEvents: ['onStartupFinished']`, `description` endurecida con value-prop concreto (5 scouts + Brain Layer + IDE + BYOK), `categories` `+'Testing'`, `keywords` `+'synaptic'` `+'taint-analysis'` `+'ai-coding'` `+'byok'`. Assets: `media/icon.svg` con un diseño **Sentinel-específico que reusa el glifo de neurona de la familia** (4 nodos + 3 conectores, `#a8c5ff` sobre `#1a1a2e`) y suma un contorno tipo escudo de 4 trazos (`#6c7fbd`) que lo distingue visualmente del sibling. `media/icon.png` 128×128 RGBA generado por `scripts/render-icon.mjs` — un **encoder PNG hecho a mano** (`zlib` + `Buffer` + Bresenham para líneas, fill-rect/circle por test de distancia) **porque no había `magick`/`inkscape`/`rsvg-convert` en el entorno y agregar `sharp`/`resvg-js` habría reintroducido el riesgo de ABI nativa que FI-001 acaba de cerrar** — coherente con el principio de cero-binarios-nativos que motivó el pivot a WASM en DG-062. 653 bytes; magic/IHDR/IDAT/IEND validados (`file` reporta `PNG image data, 128x128, 8-bit/color RGBA, non-interlaced`). `CHANGELOG.md` NUEVO en formato Keep a Changelog, entry 0.1.0 con el cuerpo completo de capacidades (los 5 scouts, las 3 agencias del Brain Layer, la memoria del enjambre, la UX inline, la pseudoterminal, la turnkey "Install Scanners", los exports CI-native), notas de arquitectura (spawn-CLI, `node-sqlite3-wasm`, SDK oficial), privacy/data flow, known constraints, licencias (Apache-2.0 OSS + LICENSE-PRO para el Brain Layer). `README.md` rewritten como **página de marketplace** al estilo del sibling: pitch + tabla de los 5 scouts con CWEs concretos + detalle del Brain Layer + 4 pasos turnkey + CI-native + privacy + licencias + link a la familia. Verificación: `pnpm verify` verde (test:unit 302; un lint error inicial por `console` no-undef en `render-icon.mjs` se arregló removiendo el diagnóstico). `vsce package` produjo `synaptic-sentinel-0.1.0.vsix` (430 archivos, **1.26 MB**) con `icon.png` y `media/icon.svg` shippeados y todos los campos marketplace validados al extraer y inspeccionar el `.vsix`. **El `.vsix` queda listo para `vsce publish`** — el timing y el PAT del marketplace son del usuario.
- 2026-05-22 — Cycle 57: migración del cliente LLM al SDK oficial (DG-064 A, **cierra FI-009**) — `AnthropicLlmClient` deja el cliente mínimo `fetch` propio (desviación informada del v0.4 línea 695, abierta desde DG-024) y pasa a usar `@anthropic-ai/sdk@^0.40` detrás del mismo contrato `LlmClient` que tenía desde el inicio. Beneficios: retries automáticos en 429/5xx (`maxRetries` configurable), manejo nativo de rate-limiting, soporte de streaming (no usado aún, disponible). El refactor está acotado por el contrato — **cero cambios en los 3 agentes** (Triage / Context / Remediation): siguen viendo solo `complete(request): Promise<string>`. `buildAnthropicRequest` (función pura de construcción HTTP) se elimina porque el SDK la encapsula; `parseAnthropicResponse` se mantiene intacto porque la forma de `Message.content` del SDK es idéntica al JSON crudo. Tests: 2 tests de `buildAnthropicRequest` eliminados, 4 de `parseAnthropicResponse` intactos, 2 de `complete` adaptados (la `fakeFetch` se pasa al constructor del SDK + body verificado del `init.body` capturado; `maxRetries: 0` para evitar esperas). **Dos gotchas absorbidos honestamente durante la migración:** (1) **bundle inline falla en runtime** — esbuild inlining el SDK + `node-fetch` produce un `__require2` que rompe la interop CJS↔ESM al cargar `node-fetch`'s `lib/index.js`. Fix: `--external:@anthropic-ai/sdk` (mismo patrón que `node-sqlite3-wasm`); (2) **`require.resolve('@anthropic-ai/sdk/package.json')` falla** por el campo `exports` del SDK. Fix: helper `findPackageRoot` que resuelve el main entry y camina hacia arriba buscando `package.json` cuyo `name` coincida — robusto contra cualquier paquete con `exports`. `copy-cli-assets.mjs` refactorizado para resolver la clausura transitiva con `createRequire` chains desde cada parent. **14 paquetes copiados** planos en `dist/node_modules/`: `@anthropic-ai/sdk` + `abort-controller` + `agentkeepalive` + `event-target-shim` + `form-data-encoder` + `formdata-node` + `humanize-ms` + `ms` + `node-domexception` + `node-fetch` + `tr46` + `web-streams-polyfill` + `webidl-conversions` + `whatwg-url`. `.vsix`: 621 KB → **1.25 MB** (427 archivos), costo aceptable por el valor. Verificación: `pnpm verify` verde (test:unit 302; −2 por el drop de `buildAnthropicRequest`) + `pnpm test:integration` verde (11 passed) + **e2e doble** (CLI bundleada + `.vsix` extraído a una ruta externa). **Hito: la lista de mejoras futuras (`futureImprovements`) queda vacía por primera vez en 57 ciclos** — el producto no tiene deuda técnica registrada.
- 2026-05-22 — Cycle 56: taint analysis Python (DG-063 B, **cierra FI-003**) — el ruleset baseline de OpenGrep gana **3 reglas `mode: taint` para Python**, replicando el patrón validado en DG-061 (JS/TS). `sentinel-py-taint-command-injection` (CWE-78): sources Flask (`request.args`/`form`/`json`/`values`/`headers`/`cookies`), Django (`request.GET`/`POST`/`body`), CLI/env (`sys.argv`, `os.environ`); sinks `subprocess.run`/`call`/`check_call`/`check_output`/`Popen` + `os.system`/`os.popen`. `sentinel-py-taint-sql-injection` (CWE-89): sinks `$CURSOR.execute`/`executemany`/`executescript` — el metavariable `$CURSOR` cubre cualquier cursor de DB-API 2.0. `sentinel-py-taint-path-traversal` (CWE-22 — sink nuevo no probado en JS/TS): sinks `open(...)`, `pathlib.Path(...)`, `os.remove`/`os.unlink`, `shutil.rmtree`/`shutil.copy`; sanitizers `secure_filename`/`werkzeug.utils.secure_filename`/`os.path.basename`. Fixture `tests/opengrep/fixtures/vulnerable/python/taint-vuln.py` (4 funciones cubriendo los 3 flujos: `sys.argv → subprocess.run`, `request.form → os.system`, `request.args → cursor.execute`, `request.args → open`). Test de integración nuevo: las 3 reglas dispararon contra OpenGrep real en los lugares correctos (líneas 24, 30, 36, 42). Cobertura del ruleset: **14 → 17 reglas** (11 pattern-based + 6 taint cubriendo CWE-22/78/79/89). Las pattern-based existentes (`sentinel-py-subprocess-shell`, `sentinel-py-os-system`) dispararon en complemento — esperado (recall + precisión). `pnpm verify` verde (test:unit 304) + `pnpm test:integration` verde (11 passed) + e2e con la CLI bundleada (12 hallazgos en el fixture, sin FPs detectados). `.vsix` repackagea a 621.59 KB. **FI-003 cerrado al 100%** (etapa 1 DG-061 + etapa 2 DG-063 + fix DG-062). Único frente OPEN remanente: FI-009 (cliente LLM). 315 tests verdes + 3 gated.
- 2026-05-22 — Cycle 55: pivot del driver SQLite a WASM + fix FP SQL (DG-062 B) — la prueba del usuario en el `.vsix` post-DG-061 reveló **dos bugs**: (a) `NODE_MODULE_VERSION 137 vs 140` al cargar `better-sqlite3` en el extension host de Electron — confirmación práctica de que los prebuilts NO eran NAPI universales como reclamé en DG-060; (b) **falso positivo** de la regla `sentinel-js-taint-sql-injection` matcheando `child_process.exec(...)` (el sink `$DB.exec` capturaba el destructured `exec`). DG-062 cierra los dos: **(1)** drop del sink `$DB.exec`, regresión `sqlFindings.length === 1` asserted en el test taint; **(2)** pivot a `node-sqlite3-wasm@^0.8` — SQLite compilado a WebAssembly puro, sin `.node` binary, sin ABI. ColonyDb absorbe los tres caveats reales de la librería que ningún reclamo previo prometió: **interop CJS** (`createRequire` porque Node ESM no extrae los named exports de este CJS), **binds via array** (`stmt.run([a, b, c])` no `stmt.run(a, b, c)`), **statement finalization explícita** (`db.close()` no la hace; helper `withStmt` para batches + `db.run/get/all` para one-shots — sin esto los reopens fallan con `database is locked`, 4 tests de triage lo confirmaron). Drop de `PRAGMA journal_mode = WAL` del schema (CLI single-process no la necesitaba y agravaba los locks). Bundle/`.vsix`: `--external:node-sqlite3-wasm` + el script de copia post-bundle ahora copia un único paquete sin transitive deps; `.vsix` pasa de **3.65 MB a 620 KB**. Verificación: `pnpm verify` verde (test:unit 304) + `pnpm test:integration` verde (10 passed) + **e2e doble** (CLI bundleada + `.vsix` extraído a una ruta externa). 314 tests verdes + 3 gated. **Caveat honesto:** el `.vsix` no se reverificó en VSCode en este entorno, pero la clase de bug ABI desaparece estructuralmente — WASM no tiene ABI. El usuario reverificará.
- 2026-05-22 — Cycle 54: taint analysis JS/TS (DG-061 B, FI-003 etapa 1) — el ruleset baseline de OpenGrep gana **3 reglas `mode: taint`** para JS/TS, complementando (no reemplazando) las 11 pattern-based. `sentinel-js-taint-command-injection` (CWE-78) sigue `req.body`/`req.query`/`req.params`/`req.headers`/`req.cookies`/`process.argv`/`process.env` hacia `exec`/`execSync`/`execFile`/`spawn`/`spawnSync`. `sentinel-js-taint-xss` (CWE-79) suma sources de browser (`window.location.search`/`hash`, `document.URL`, `document.referrer`) hacia `innerHTML`/`outerHTML` y `document.write`/`writeln`, con sanitizers `DOMPurify.sanitize`/`escapeHtml`/`sanitizeHtml`. `sentinel-js-taint-sql-injection` (CWE-89) sigue las sources de Express hacia `$DB.query`/`execute`/`exec` y `knex.raw`. Source patterns LITERALES (`req.body` en vez de `$REQ.body`) para evitar over-tainting de cualquier `.body`. Fixture **deliberadamente vulnerable** en `tests/opengrep/fixtures/vulnerable/javascript/taint-vuln.js` (4 funciones cubriendo los 3 flujos). Test de integración nuevo: el motor taint de OpenGrep dispara las 3 reglas contra el fixture — **prueba empírica** de que `mode: taint`, `pattern-sources`/`pattern-sinks` y la propagación intra-procedural funcionan en este binario, lo que era el único unknown real al entrar al ciclo. Cobertura del ruleset: **11 → 14 reglas** (11 pattern-based + 3 taint). Python diferido a etapa 2 (mismo patrón, sub-increment de bajo riesgo). Verificación: `pnpm verify` verde (test:unit 304) + `pnpm test:integration` verde (10 passed; era 9). 314 tests verdes + 3 gated. **Pausa explícita del usuario:** se prueba el producto con las nuevas reglas antes de presentar DG-062.
- 2026-05-22 — Cycle 53: migración del driver SQLite a `better-sqlite3` (DG-060 B, **cierra FI-001**) — `ColonyDb` pasa de `node:sqlite` (módulo experimental, requiere Node ≥ 22.5) a `better-sqlite3@^12` (módulo nativo con prebuilts NAPI). El cambio es estrictamente local al wrapper porque la persistencia ya estaba aislada desde DG-013: `DatabaseSync` → `Database` (default) + `Database.Database` como tipo de instancia; las firmas (`.exec` / `.prepare` / `.run` / `.get` / `.all` / `.close`) son idénticas. Primer intento con `^11` falló (sólo prebuilts hasta Node 22; el entorno dev en Node 24 no tiene Visual Studio para compilar) → bump a `^12` que trae prebuilts NAPI para Node 24 (unblock real, anti optimismo ilusorio). esbuild **no** puede bundlear `.node` binarios → `--external:better-sqlite3` + un copy step que mete `better-sqlite3` + `bindings` + `file-uri-to-path` en `dist/node_modules/` (navegando la cadena de `createRequire` de cada parent para sortear la aislación estricta de pnpm). `runtime-check.ts` y su test ELIMINADOS (band-aid de DG-058 que detectaba Node < 22.5 con `node:sqlite` — ya no aplica). El `.vsix` shippea ahora el binario nativo + sus deps, crece de **87 KB a 3.65 MB**. La `ExperimentalWarning: SQLite is an experimental feature` desaparece. Verificación: `pnpm verify` verde (test:unit 304, -7 por la eliminación del runtime-check) + `test:integration` verde (9 passed) + **e2e doble**: la CLI bundleada en `dist/` corre fin a fin contra el probe vulnerable, y un `.vsix` **extraído a una ruta externa** (`d:/tmp/`) también corre fin a fin (prueba que la layout es autocontenida). 313 tests verdes + 3 gated. **Caveat honesto:** el binario NAPI es ABI-estable cross-version y cross-Electron según la documentación oficial de `better-sqlite3 v12`, pero la carga real en el extension host de VSCode (Electron en modo Node) no se verificó — requiere cargar el `.vsix` manualmente en VSCode.
- 2026-05-22 — Cycle 52: sub-comando `scanners install` turnkey (DG-059 A, **cierra FI-008**) — la CLI gana `synaptic-sentinel scanners install [--global]`. La lógica de instalación se mueve de `scripts/install-scanners.ts` a `packages/cli/src/scanners/install.ts` para que viva DENTRO del bundle de la extensión; el manifest pasa de `.json` a un const TS tipado (`scanners.manifest.ts`) para evitar el ceremonial de copiar el JSON al `dist/` y al bundle. La extensión contribuye el comando "Synaptic Sentinel: Install Scanners" que invoca el sub-comando con `--global` (instala en `~/.synaptic-sentinel/scanners`) y transmite el show al pseudoterminal verbose; el child Node hereda `NODE_OPTIONS=--use-system-ca` para sortear la inspección TLS de Norton (L-001). Los 4 tests de integración de scouts (opengrep/gitleaks/trivy/checkov) importan ahora `SCANNERS_MANIFEST` en vez de leer el JSON con `readFileSync`; los 5 unit tests de `install-scanners` se movieron a `packages/cli/tests/scanners/`; `scripts/install-scanners.{ts,test.ts}`, `scripts/scanners.manifest.json` y `scripts/tsconfig.json` eliminados. Verificación: `pnpm verify` verde (test:unit 311) + `test:integration` verde (9 passed) + **e2e real con red**: `node dist/cli.mjs scanners install --global` descargó los 4 scanners desde GitHub Releases, verificó SHA-256, extrajo (`Expand-Archive` en Windows) e instaló en `~/.synaptic-sentinel/scanners`; segundo run = `cache OK` para los 4 (idempotente). El `.vsix` repackagea limpio (9 archivos, 87 KB) con el sub-comando dentro del bundle. **FI-008 CERRADO → Phase 8 (Distribución) COMPLETA.** Experiencia turnkey de punta a punta: instalar el `.vsix` → "Install Scanners" (una vez) → "Scan Workspace". 320 tests verdes + 3 gated.
- 2026-05-22 — Cycle 51: el `.vsix` instalable de la extensión (DG-058 A, FI-008) — **primer ciclo del Tomo 002.** `@vscode/vsce` se agrega como **devDependency** (se instala con `pnpm install` — evita el cuelgue de `npx @vscode/vsce` que bloqueó la validación en DG-056) + scripts `vscode:prepublish` (build) y `package` (`vsce package --no-dependencies`). El `name` del paquete pasa de `@synaptic-sentinel/vscode-extension` a `synaptic-sentinel` — `vsce` rechaza un `name` con scope; el identificador de marketplace queda `golab.synaptic-sentinel` (filtro del `build` raíz actualizado). `.vscodeignore` endurecido: el `.vsix` shippea solo el bundle (`dist/extension.cjs`, `cli.mjs` + los 2 assets de runtime) y `package.json`/`LICENSE`/`README` — se excluyen `.synaptic/`, `.vscode/`, `context/`, `tsbuildinfo` y los `.js`/`.d.ts` sueltos de `tsc`. Mitigación de FI-001: `src/runtime-check.ts` (`checkExtensionHostRuntime`, puro y unit-tested) — `activate()` avisa con un mensaje accionable si el Node del extension host es < 22.5 (lo necesita `node:sqlite` de la CLI bundleada). **Desviación honesta** del texto de DG-058 A: no se elevó `engines.vscode` a un valor adivinado (el mapeo VSCode↔Node no se pudo verificar offline — sería una falsa garantía); el chequeo en runtime es la mitigación precisa e independiente de ese mapeo. Verificación: `pnpm verify` verde (test:unit 311, +7 de `runtime-check`) + **`vsce package` produjo `synaptic-sentinel-0.0.0.vsix`** (9 archivos, 83 KB) — lo que **valida el manifest**, la limitación que arrastraba DG-056 — + e2e real (el `.vsix` se extrajo y `node extension/dist/cli.mjs scan` corrió de punta a punta). 320 tests verdes + 3 gated. FI-008 casi cerrado: resta solo la auto-instalación on-demand de scanners.

---

## Mejoras Futuras / Deuda Técnica

Items identificados para mejorar más adelante. No bloquean el MVP.

| ID | Tema | Detalle |
|----|------|---------|
| FI-001 | Driver SQLite | ✅ **Resuelto en dos pasos (DG-060 B + DG-062 B pivot)** — DG-060 migró a `better-sqlite3@^12` confiando en sus prebuilts NAPI; la prueba real del usuario reveló `NODE_MODULE_VERSION` mismatch en Electron (los prebuilts eran ABI-específicos, no NAPI universales). DG-062 pivot a `node-sqlite3-wasm@^0.8` — SQLite a WebAssembly puro, sin `.node` binary, sin ABI. `.vsix`: 3.65 MB → 620 KB. ColonyDb absorbe los tres caveats reales de la lib (CJS interop vía `createRequire`, binds via array, statement finalization explícita). `runtime-check.ts` (band-aid de DG-058) eliminado en DG-060. |
| FI-002 | Suite de tests | ✅ **Resuelto (DG-046 A)** — la suite Vitest se dividió en dos proyectos (`test.projects` `unit` / `integration`); el gate por ciclo corre solo `test:unit` (~7 s). `test:integration` se invoca explícitamente. Integración = sufijo `*integration.test.ts`. |
| FI-003 | Catálogo de reglas / taint analysis | ✅ **Resuelto entero (DG-043 B + DG-061 B + DG-062 B + DG-063 B)** — DG-043 sentó los 11 patrones deterministas (CWE-95/78/79/502/327). Etapa 1 (DG-061): 3 reglas `mode: taint` para JS/TS — `taint-command-injection` (CWE-78), `taint-xss` (CWE-79, con sanitizers `DOMPurify`/`escapeHtml`/`sanitizeHtml`), `taint-sql-injection` (CWE-89). Fix DG-062: drop del sink `$DB.exec` que falsificaba contra `child_process.exec`, regresión asserted. Etapa 2 (DG-063): 3 reglas `mode: taint` para Python — `py-taint-command-injection` (CWE-78), `py-taint-sql-injection` (CWE-89), `py-taint-path-traversal` (CWE-22, con sanitizers `secure_filename`/`os.path.basename`). **Cobertura final del ruleset: 17 reglas** (11 pattern-based + 6 taint cubriendo CWE-22/78/79/89). Verificado e2e con OpenGrep real en ambos lenguajes. |
| FI-004 | Cache de scanners del producto | ✅ **Resuelto (DG-053 C + DG-054 A)** — cache de scanners global por usuario (`~/.synaptic-sentinel/scanners`): la CLI la resuelve (`resolveScannerBinary`) y `install-scanners --global` la puebla. El disparo on-demand del install desde un `.vsix` (cuando falta un scanner) es parte del empaquetado FI-008, no de la cache en sí. |
| FI-005 | `ruleId` de OpenGrep | ✅ **Resuelto (DG-047 A)** — `canonicalRuleId` normaliza el `Finding.ruleId` al último segmento del `check_id`, descartando el prefijo de ruta que OpenGrep agrega con `--config <archivo>`. |
| FI-006 | `suppressedCount` en el tomo | ✅ **Resuelto (DG-023 A)** — `TomoSummary` incluye `suppressedCount`, tomado de `outcome.suppressedCount`. |
| FI-007 | Creación de `fp_known` | ✅ **Resuelto (DG-022 B)** — el comando CLI `mark-fp` y el Code Action "marcar falso positivo" de la extensión crean feromonas `fp_known`. |
| FI-008 | Empaquetado de la extensión (`.vsix`) | ✅ **Resuelto (DG-055 A + DG-056 A + DG-057 B + DG-058 A + DG-059 A)** — cerrado en 5 sub-increments: Node del extension host vía `process.execPath` (DG-055); metadata del manifest para `vsce` (DG-056); CLI bundleada como `dist/cli.mjs` ESM con sus assets de runtime al lado (DG-057); `.vsix` instalable producido y validado con `vsce package` = `synaptic-sentinel-0.0.0.vsix`, 9 archivos, 87 KB (DG-058); sub-comando turnkey `synaptic-sentinel scanners install [--global]` (dentro del bundle) + comando de la extensión "Synaptic Sentinel: Install Scanners" (DG-059, e2e real con descarga desde GitHub Releases verificado + idempotente). Experiencia turnkey: instalar el `.vsix` → "Install Scanners" una vez → "Scan Workspace". FI-001 sigue mitigada (no cerrada) con `runtime-check.ts`. |
| FI-009 | Cliente LLM | ✅ **Resuelto (DG-064 A)** — `AnthropicLlmClient` migrado del cliente `fetch` propio al SDK oficial `@anthropic-ai/sdk@^0.40` detrás del contrato `LlmClient` (de DG-024 B, que aisló el cliente desde el inicio). Gana retries automáticos en 429/5xx (`maxRetries` configurable), rate-limiting nativo, soporte de streaming. Cero cambios en los 3 agentes consumidores. Bundle: `--external:@anthropic-ai/sdk` + `findPackageRoot` walk-up para resolver la clausura transitiva (14 paquetes shippeados en el `.vsix`). |
| FI-010 | Drift de Prettier | ✅ **Resuelto (DG-042 A)** — `pnpm format` sobre 44 archivos con drift; `format:check` incorporado al gate por ciclo via el nuevo script raíz `verify`. Se corrigió además un comentario inestable de `cli-runner.test.ts` que Prettier no podía estabilizar. |
| FI-011 | Salida del producto en inglés | ✅ **Resuelto (DG-048…DG-052, 5 etapas)** — toda la salida del producto al usuario final está en inglés: **(1)** CLI (DG-048), **(2)** mensajes de los scouts (DG-049), **(3)** reporter HTML del tomo (DG-050), **(4)** extensión VSCode (DG-051), **(5)** prompts del Brain Layer (DG-052). Principio en vigor: todo string de salida **nuevo** se escribe en inglés. Fuera de alcance (siguen en español): el bookkeeping de `.synaptic/`, los comentarios de código y los mensajes de commit. |

---

## Technology Stack

- **Lenguaje**: TypeScript 5.9 estricto — LOCKED (v0.4 §9)
- **Runtime**: Node.js 20+ — verificado v24.11.1
- **Package manager**: pnpm workspaces v10.33.0 — DECIDIDO (Q1)
- **Estructura**: monorepo único OSS/Pro — DECIDIDO (DG-001 B)
- **Build**: `tsc -b` con project references
- **Tests**: Vitest 3
- **Linter/formatter**: ESLint 9 flat (typescript-eslint 8) + Prettier 3 — DECIDIDO (DG-006 B)
- **Superficie**: VSCode Extension API — LOCKED
- **DB local**: SQLite (`colony.db`) — driver `node-sqlite3-wasm` (DG-062 B, WASM puro tras pivot desde `better-sqlite3`); capa `ColonyDb`
- **LLM**: `@anthropic-ai/sdk` con BYOK — LOCKED · cliente migrado al SDK oficial (DG-064 A)
- **Licencia**: Apache-2.0 **íntegra** (incluido `packages/agents`) tras DG-066 B; `LICENSE-PRO` eliminado
- **Scanners**: binarios nativos pinneados descargados on-demand — DECIDIDO (DG-003 A)

---

## Project Overview

- **Name**: SENTINEL (Synaptic Sentinel)
- **Description**: Toolkit Apache-2.0 de auditoría agéntica de seguridad — Scout Layer + Brain Layer (LLM agents, BYOK Anthropic) — vibe-coding-native, posicionado como "the world's best security sentinel for vibe-coded projects" (giro estratégico DG-066 B).
- **Phase**: Cycle 75 / **🏁 Phase 12 — Marketplace launch CERRADA en Cycle 74 con DG-082 A final (Entry #92)**: `SYNAPTIC Sentinel v0.3.3` PUBLICADO en el Visual Studio Marketplace en https://marketplace.visualstudio.com/items?itemName=RealGoLab.synaptic-sentinel tras 4 release-blockers consecutivos resueltos (v0.3.0 inlined SDKs / v0.3.1 createRequire undefined / v0.3.2 publisher mismatch / v0.3.3 OK). Verify gate fortalecido con `scripts/verify-extension-activate.mjs` (DG-081 B) cubre la clase activate() runtime; sub-DG futuro abierto para `verify-manifest.mjs` que cubrirá la clase manifest validity (publisher / name / license / engines.vscode). Sin Phases abiertas. **Phase 12 cubrió Cycles 73-74** con 8 sub-increments (DG-080 B + DG-079.1 + DG-079.2 + DG-079.2 follow-up + DG-081 B + DG-082.1 + DG-082.1 follow-up + DG-082 A final). El producto está en su primer estado 'fully shipped' del proyecto: GitHub Release + Marketplace public listing operativos. Originalmente Phase 12 — Marketplace launch ABIERTA con DG-080 B PARCIAL + hotfix DG-079.1 + segundo hotfix DG-079.2 APLICADOS**. Phase 11 (Multi-Provider Brain Layer) CERRADA en Cycle 72. Phase 9 (Strategic Pivot) CERRADA en Cycle 62. Phase 8 (Distribución) sigue COMPLETA funcionalmente. **DG-079.1 HOTFIX (Entry #86) APLICADO en Cycle 73**: bug crítico descubierto al validar localmente el `.vsix v0.3.0` antes del marketplace publish — TODOS los comandos de la extensión fallaban con `Command not found` porque el bundle script solo marcaba `--external:vscode`, dejando que esbuild **inlineara `@anthropic-ai/sdk` + `openai` + `node-sqlite3-wasm`** (arrastrados transitivamente vía `@synaptic-sentinel/agents` desde `settings-view.ts` agregado en DG-074 B); esos SDKs contienen `require()` dinámicos que **matan `activate()` silenciosamente** antes de registrar comandos. Fix: agregar `--external:@anthropic-ai/sdk --external:openai --external:node-sqlite3-wasm` al bundle script (paquetes ya copiados a `dist/node_modules/` por `copy-cli-assets.mjs`). Bundle pasa de 615 KB → 280 KB (-54%); refs a `@anthropic-ai/sdk` en el bundle bajan de 178 → 2 (solo el `require()` runtime). Bump `0.3.0` → `0.3.1` + CHANGELOG entry hotfix con anti-optimismo lesson explícita. **`synaptic-sentinel-0.3.1.vsix` (3.12 MB / 1838 archivos)** producido — SHA-256 `0450DF44C4E21170FE7F6F706B4836619C26655CD691C3F0EDF63286AF6348E4`. `docs/PUBLISHING.md` actualizado para apuntar a v0.3.1. **v0.3.0 SUPERSEDED** (ambas variantes: GitHub Release SHA `5EA050B1...` y re-pack DG-080 SHA `DA07CA79...` están afectadas; queda como artefacto histórico). `pnpm verify` verde 56 test files / 463 tests. **DG-080 B sigue PARCIAL** (preparación marketplace done en Entry #85, ahora con v0.3.1 como artifact); `vsce publish` PENDING hasta que el usuario valide v0.3.1 localmente — pasos en Entry #86 `pending_user_validation`. **Anti-optimismo lesson capturada**: 463 unit tests + verify gate + `vsce package` validation no atajaron 2 ciclos seguidos un bug que el smoke test del usuario detectó en minutos. CLASE de bug: bundling de deps con `require()` dinámicos. Sub-DG futuro a registrar: vscode-test integration en verify gate. **DG-079.2 SECOND HOTFIX (Entry #87) APLICADO** tras descubrir que v0.3.1 era insuficiente: el usuario instaló v0.3.1, los 5 comandos aparecían en `Ctrl+Shift+P` (paso 4 OK), pero al ejecutar `Install Scanners` (paso 6) volvía el mismo popup `Command not found`. Diagnóstico con **headless extension-host simulator** (`node -e` con mock del módulo `vscode` + `require('dist/extension.cjs')` + invocar `activate()`) reveló el root cause real: `packages/core/src/colony/colony-db.ts:10` usa `createRequire(import.meta.url)` (patrón ESM válido en cli.mjs pero `import.meta.url` es `undefined` cuando esbuild bundlea a CJS en extension.cjs). `createRequire(undefined)` lanza `TypeError: The argument 'filename' must be a file URL object...`. La excepción mata `activate()` al cargar el módulo. Fix: nuevo helper `bundleSafeModuleUrl()` en `colony-db.ts` que intenta `import.meta.url` (ESM) y cae a `eval('typeof __filename === "string" ? __filename : null')` (CJS bundle — eval es la única forma de acceder al `__filename` del wrapper CJS desde código bundleado). El mismo helper se usa para `createRequire` y para `new URL('./schema.sql', ...)`. Bump `0.3.1` → `0.3.2`. CHANGELOG entry `[0.3.2]` con anti-optimismo lesson v2. **`synaptic-sentinel-0.3.2.vsix` (3.13 MB / 1838 archivos)** producido — SHA-256 `84411A07C4F6C68D9C8CD9ECA973020F183D62949ECE45C326CB7D0A943D39C0`. `pnpm verify` verde 56 test files / 463 tests. **Headless simulator ahora REPORTA SUCCESS**: registra los 7 comandos (scanWorkspace, markFalsePositive, copyRemediation, triageWorkspace, setAnthropicApiKey, **installScanners**, configureProviders) + 13 subscriptions + `activate()` completed sin errores. `docs/PUBLISHING.md` actualizado a v0.3.2. **v0.3.0 y v0.3.1 ambos SUPERSEDED**. **Anti-optimismo lesson v2 capturada**: DG-079.1 reclamó el fix sin headless simulator; bundle size 615→280 KB pareció evidencia de fix pero era evidencia parcial (refs a SDKs externos removidas, pero el bug `createRequire(undefined)` era independiente). Sub-DG futuro **MÁS CRÍTICO QUE NUNCA**: vscode-test integration en el verify gate (o como mínimo el headless simulator one-liner como pre-publish step). **DG-081 B (Entry #89): vscode-test/headless integration en verify gate APLICADO en Cycle 73**. Lesson capturada en DG-079.1 + DG-079.2 + Entry #88 honrada explícitamente: el verify gate del proyecto NO era suficiente para extension activation. **Fix**: NEW `scripts/verify-extension-activate.mjs` (~280 líneas) destila el headless one-liner que descubrió DG-079.2 a un script reusable con assertions estrictas — verifica que (a) `extension.cjs` carga sin throw, (b) `activate` es función, (c) `activate(fakeContext)` completa, (d) los 7 comandos esperados (`scanWorkspace`, `markFalsePositive`, `copyRemediation`, `triageWorkspace`, `setAnthropicApiKey`, `installScanners`, `configureProviders`) quedan registrados, (e) `subscriptions.length === 13`. **Wirea al `verify` script root**: `format:check && lint && build && test:unit && verify:extension-activate`. Soporte mecánico: nueva entrada `verify:extension-activate` en `package.json` + bloque `globals` Node para `scripts/**.{js,mjs}` en `eslint.config.mjs` (ESLint 9 flat config no infiere de extension) + micro-cleanup `eslint-disable no-eval` unused en `colony-db.ts`. **`pnpm verify` VERDE end-to-end**: 463 tests + ✅ verify-extension-activate OK con 7 comandos + 13 subscriptions registrados. **Valor empírico demostrado**: este simulator en su forma one-liner fue el que detectó DG-079.2 + validó v0.3.2 — prueba retrospectiva contra los dos bugs reales. NO es panacea (no cubre runtime behavior de comandos ni UI del webview ni interacción real con SecretStorage); cubre la clase de bug `activate() lanza al cargar el bundle CJS`. **NO bump de version** — el cambio es de tooling/gate, no de producto. v0.3.2 sigue siendo el release público en GitHub Release. **Sub-DG futuro** que queda abierto: `@vscode/test-electron` framework completo para cubrir runtime behavior (no urgente). **Phase 12 publish ahora protegido**: el siguiente release de extension va a tener que pasar este gate antes de llegar a `vsce package`.

---

**DG-079.2 follow-up (Entry #88): v0.3.2 VALIDATED por el usuario en VSCode real**. Test path completo: desinstaló v0.3.1, instaló v0.3.2 via 'Install from VSIX...', reload window, los 5 comandos SYNAPTIC aparecen, **`Install Scanners` ejecuta correctamente** y descarga los binarios end-to-end. Confirma que el headless simulator era diagnóstico válido. **Tag annotated `v0.3.2` pushed + `gh release create v0.3.2`** publicado en https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.3.2 (Latest, isDraft:false, asset .vsix descargable, SHA-256 `84411A07C4F6C68D9C8CD9ECA973020F183D62949ECE45C326CB7D0A943D39C0`). **GitHub Release v0.3.0 retro-actualizada** con nota fuerte de superseded para evitar que usuarios lo instalen (queda como evidencia diagnóstica histórica). v0.3.1 nunca llegó a release público (DG-079.1 cerró sin tag esperando user validation que descubrió el segundo bug). **Siguiente**: (a) Phase 12 publish — ejecutar `docs/PUBLISHING.md` con PAT del usuario para `vsce publish v0.3.2`; (b) sub-DG mientras tanto — prioridad elevada: **vscode-test/headless integration en verify gate** (previene la clase de bugs que casi rompe el marketplace publish dos veces). Sub-DGs futuros heredados: path leak fix, gpt-5 reasoning tokens, Ollama batching, exponer usage real, sidebar webview Cost Visibility, ground truth human review.

---

*Created: 2026-05-20T19:09:00.816Z*
*Last Updated: 2026-05-25T00:45:00.000Z*
*SYNAPTIC Protocol v3.0*
