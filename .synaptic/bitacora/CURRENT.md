# BITACORA - SENTINEL

## Active Tome

**Tomo 002** — Cycles 51-100 — abierto 2026-05-22 · Tomo 001 (Cycles 1-50) CERRADO, archivado en `tomes/tome-001.{json,md}`

## Current Cycle

- **Cycle:** 79 — **DG-086 A CERRADO en Cycle 78 (Entry #96)**: gpt-5* reasoning tokens budget bump (1024 → 8192). Cierra **1 de los 4 caveats heredados** restantes de v0.3.x Known Issues (3 de 6 originales cerrados ya). Producto sigue live en Marketplace, sin Phases abiertas. Awaiting próximo DG (Ollama RAM warning / free tier quotas / pausa)
- **Phase:** **🏁 Phase 12 CERRADA en Cycle 74** · 🏁 Phase 11 (Multi-Provider Brain Layer) CERRADA en Cycle 72 (10/10 sub-increments) · Phase 9 (Strategic Pivot) CERRADA en Cycle 62 · Phase 8 (Distribución) COMPLETA · **Sin Phases abiertas. Verify gate CUMULATIVO con 2 steps: verify-extension-activate.mjs + verify-manifest.mjs. Caveats heredados de v0.3.x: 3 abiertos (era 6; DG-084 A path leak, DG-085 A tokens proxy, DG-086 A gpt-5 reasoning tokens — todos cerrados)**
- **Status:** Cycle 78 con DG-086 A CERRADO (Entry #96): NEW constante `DEFAULT_MAX_TOKENS_GPT5 = 8192` en `packages/agents/src/openai-compatible-client.ts`. Lógica del `completeWithUsage` actualizada: `const tokens = request.maxTokens ?? (isGpt5 ? DEFAULT_MAX_TOKENS_GPT5 : DEFAULT_MAX_TOKENS)`. Override del caller via `request.maxTokens` SIEMPRE se respeta; non-gpt-5* models siguen en 1024 default. Comment del adapter actualizado: ahora documenta TRES quirks de gpt-5* (max_completion_tokens vs max_tokens; rechazo de temperature=0; reasoning tokens consumen el budget). Test existente actualizado de assert 1024 → 8192 + 2 tests nuevos (anti-regresion override caller + regresion guard non-gpt-5). `pnpm verify` VERDE 485 tests (+2 vs baseline 483) + 2 gates OK. Anti-optimismo: el impacto empírico (gpt-5-nano deja de devolver content=null) sólo se mide corriendo el benchmark contra OpenAI con keys del usuario — diferido honestamente. Lo afirmable: el bug-cause documentado (reasoning consume el budget) se elimina al ampliar el cap a 8192. Cost implication documentada: el cap NO duplica el cost real — provider cobra por tokens generados, no por el cap. NO bump version del .vsix v0.3.3
- **Compliance:** 100%
- **Synaptic Strength:** 85

## Cycles cerrados

- **Cycle 1-13** — discovery → scaffolding → PASO 4 → colony.db → Coordinator → CLI → reporters → GitleaksScout ✅
- **Cycle 14-26** — extensión VSCode MVP, Brain Layer (Triage/Context/Remediation), scouts Trivy/Checkov/Vibe-Detect ✅
- **Cycle 27-29** — surface del Brain Layer en la extensión · `ONBOARDING.md` · kill-switch del Coordinator ✅
- **Cycle 30-32** — UX verbose: CLI (DG-037) → pseudoterminal (DG-038) → webview "tomo vivo" (DG-039) ✅
- **Cycle 33-34** — la colonia aprende: `learning_records` escritura (DG-040) + lectura (DG-041) ✅
- **Cycle 35-41** — higiene/Prettier, SAST 11 reglas, SARIF, `--fail-on`, test split, `ruleId` canónico, CLI en inglés ✅
- **Cycle 42-45** — migración a inglés FI-011 (cerrado): scouts · reporter HTML · extensión · prompts del Brain Layer ✅
- **Cycle 46-47** — Phase 8 abierta: cache de scanners global por usuario (FI-004 cerrado) ✅
- **Cycle 48-50** — FI-008: Node del extension host (DG-055) · manifest vsce (DG-056) · CLI bundleada (DG-057) ✅
- **Cycle 51-52** — FI-008 cerrado: `.vsix` producido (DG-058) + sub-comando turnkey "Install Scanners" (DG-059) → **Phase 8 COMPLETA** ✅
- **Cycle 53** — FI-001 migración inicial a `better-sqlite3` NAPI (DG-060 B) — corregida en Cycle 55 ✅
- **Cycle 54** — FI-003 etapa 1 (JS/TS): 3 reglas `mode: taint` (DG-061 B) ✅
- **Cycle 55** — Pivot a `node-sqlite3-wasm` + fix FP SQL (DG-062 B): cierra FI-001 de raíz tras la prueba del usuario ✅
- **Cycle 56** — FI-003 etapa 2 (Python): 3 reglas `mode: taint` (DG-063 B) → **FI-003 cerrado entero** ✅
- **Cycle 57** — FI-009: `AnthropicLlmClient` → `@anthropic-ai/sdk` oficial (DG-064 A) → 🏁 **`futureImprovements` vacía por primera vez** ✅
- **Cycle 58** — Marketplace polish v0.1.0 alineado con la familia SYNAPTIC (DG-065 A): publisher, icono, galleryBanner, CHANGELOG, README marketplace-ready ✅ · _follow-up Entry #70 (commit `9f44a82`): tras feedback visual del usuario — logo OFICIAL de la familia GoLab (sustituye al Sentinel-específico, removidos `icon.svg` + `render-icon.mjs`), publisher `RealGoLab`→`GoLab` (id `golab.synaptic-sentinel`), casing `Synaptic Sentinel`→`SYNAPTIC Sentinel` en toda la superficie user-visible_
- **Cycle 59** — 🔀 **Apertura de Phase 9 — Strategic Pivot** (DG-066 B): giro estratégico ratificado por el usuario a **OSS full Apache-2.0** ("lanzar full capabilities como el mejor sentinela del mundo enfocado en vibe-coding"). Sub-increment atómico de sustrato legal: `LICENSE-PRO` eliminado, `packages/agents` re-licenciado a `Apache-2.0`, header `[PRO]` retirado. DG-001 B **amendado** (`publish-oss.ts` allowlist obsoleto bajo el giro) ✅
- **Cycle 60** — **Phase 9 sub-increment 2: re-positioning textual** (DG-067 B): tagline elegida por el usuario "**The vibe-coding security sentinel**" (tone conservador — no "world's best"). 5 archivos user-visible reescritos: `README.md` raíz, `packages/vscode-extension/README.md`, `CHANGELOG.md` (nueva entrada `[0.2.0] - _Unreleased — to be cut in DG-069_`), `ONBOARDING.md`, `packages/vscode-extension/package.json` (description + keywords `ai-generated-code`/`llm-security`). Apache-2.0 declarado en todas las superficies user-visible; Brain Layer sin "Pro/proprietary" ✅
- **Cycle 61** — 🌐 **Phase 9 sub-increment 3: repo PÚBLICO** (DG-068 B): primera acción outward-facing real del proyecto. Pre-flight `gitleaks` sobre historia completa PASS (único hit en fixture deliberadamente vulnerable de `tests/.../fixtures/secrets/`, excluido por guardrail). Metadata refresh (description + homepage al marketplace listing + 10 topics: `vibe-coding`/`security`/`sast`/`taint-analysis`/`ai-coding`/`ai-generated-code`/`llm-security`/`byok`/`vscode-extension`/`synaptic`). `gh repo edit --visibility public --accept-visibility-change-consequences` ejecutado; `gh repo view` confirma `visibility: PUBLIC` + `licenseInfo: Apache-2.0` (detectado automáticamente por GitHub desde `LICENSE` en raíz) ✅
- **Cycle 62** — 🚀 **Phase 9 sub-increment 4 (cierra Phase 9): release `v0.2.0`** (DG-069 B): bump `package.json` `0.1.0` → `0.2.0`; cut CHANGELOG date `[0.2.0] - 2026-05-23`; `pnpm verify` verde; `vsce package` → `synaptic-sentinel-0.2.0.vsix` (429 archivos, 1.27 MB, manifest validado). Annotated tag `v0.2.0` push. `gh release create v0.2.0` con asset `.vsix` descargable: [github.com/golab-arch/synaptic-sentinel/releases/tag/v0.2.0](https://github.com/golab-arch/synaptic-sentinel/releases/tag/v0.2.0) · `isDraft=false` · SHA-256 expuesto por GitHub vía `digest` ✅ — **Phase 9 CERRADA**
- **Cycle 63** — 🧭 **Apertura de Phase 11 — Multi-Provider Brain Layer** (DG-070 A): bookkeeping puro tras viaje exploratorio extenso (2 rounds de discovery, 6 agentes web cubriendo librerías de abstracción + landscape de providers + protocolo OpenAI-compatible + benchmarks externos + Ollama deep dive + UX patterns). El producto se reposiciona como **provider-agnostic-by-design**. 10 decisiones consolidadas del usuario: Modo D arquitectónico (3 adapters: Anthropic native + OpenAI-compat genérico + Ollama-específico con XGrammar), YAML `.sentinel/agents.yaml` (Continue.dev pattern), provider-por-agente, benchmark empírico obligatorio antes de v0.3.0. **Phase 10 (vsce publish v0.2.0) DEFERIDA y renumerada como Phase 12**. Roadmap Phase 11: 10 sub-increments DG-070..DG-079. NO toca código ✅
- **Cycle 64** — 🧩 **Phase 11 sub-increment 2: `OpenAiCompatibleLlmClient` extraído** (DG-071 A): adapter genérico (~120 líneas + 8 unit tests con `fakeFetch`) que sirve a **14+ providers** vía `baseURL` override (OpenAI / Groq / DeepSeek / Mistral / Together / Fireworks / Perplexity / xAI Grok / Gemini-via-OpenAI-compat / AWS Bedrock Mantle / Azure OpenAI v1 / Ollama-sin-grammar / LM Studio / vLLM). Patrón replicado del `AnthropicLlmClient`: `#client` privado + helper parser puro + `temperature=0` hardcoded para determinism cross-provider. Dep `openai@^6.18.0` agregada; `pnpm install` con `NODE_OPTIONS=--use-system-ca` (L-001). Cliente queda **dormant** (re-exportado pero ningún command lo invoca todavía — wiring en DG-073). `pnpm verify` verde: 43 test files / 310 tests (+8 nuevos). Cero cambios al `AnthropicLlmClient`, al contrato `LlmClient`, ni a los 3 agentes consumidores ✅
- **Cycle 65** — 🦙 **Phase 11 sub-increment 3: `OllamaLlmClient` con XGrammar opt-in** (DG-072 B): tercer y último adapter del Modo D. ~170 líneas + 15 unit tests con `fakeFetch`. Usa la **API nativa** de Ollama (`/api/chat`, no `/v1/chat/completions`) porque sólo la nativa soporta XGrammar constrained-by-grammar vía el param `format` desde v0.5+. Acepta `format` opt-in en constructor: JSON Schema object para XGrammar, `"json"` literal legacy, o undefined para texto libre. **Sin nueva dep** (`fetch` global Node 20+). Helpers exportados de auto-discovery: `isOllamaAvailable()` con timeout 1s vía `AbortController`, `listOllamaModels()` devuelve `readonly string[]` (vacío en cualquier error, no lanza). `pnpm verify` verde: 44 test files / 325 tests (+15 nuevos). Cliente **dormant**. **3 de 3 adapters del Modo D extraídos** ✅
- **Cycle 66** — 🔌 **Phase 11 sub-increment 4: provider registry + wiring runtime + bundle externals — PRIMER CICLO CON VALOR USER-VISIBLE** (DG-073 B): el sub-increment más grande de Phase 11. Compone los 3 adapters extraídos en un provider registry funcional + schema YAML versionable `.sentinel/agents.yaml` + 3 JSON Schemas hand-written para XGrammar + wiring runtime al CLI (`commands/triage.ts` refactored con `resolveAgentLlmClients` y precedencia `injected > CLI flag > yaml > Anthropic fallback`) + flag `--agent-provider <agent>=<provider>/<model>` repeatable + SecretStorage namespaceado con migración legacy + bundle `--external:openai`. **Multi-provider FUNCIONAL end-to-end vía CLI** — usuario puede crear `.sentinel/agents.yaml` con per-agent provider + setear `SENTINEL_<PROVIDER>_API_KEY` + correr `synaptic-sentinel triage`. **Retro-compat v0.2.0 PRESERVADA**: `ANTHROPIC_API_KEY`-only users siguen funcionando idéntico (fallback implícito a Anthropic Haiku 4.5). 2 feat commits (`5656e2d` core+agents + `e9ca983` cli+vscode-extension) + 1 docs commit. `pnpm verify` verde 48 test files / 375 tests (+50 funcionalmente). Smoke `vsce package` verde 1838 archivos / 3.08 MB (vs 1.27 MB; +143% por SDK openai) ✅
- **Cycle 67** — 🎛️ **Phase 11 sub-increment 5: Settings panel multi-provider del VSCode extension** (DG-074 B): cierra el gap UX dejado por DG-073 B. Comando nuevo **"SYNAPTIC Sentinel: Configure Brain Layer Providers"** abre un webview con 3 secciones: **Active Configuration** (provider/modelo resuelto por agente), **Managed Credentials** (12 providers con auth - Anthropic + 11 OpenAI-compat - con password input + Save/Delete/Test + badges), **Local Models** (Ollama auto-discovery via ping a `localhost:11434/api/tags` + lista de modelos pulled + Refresh). Patrón replicado de DG-039 B "tomo vivo": **renderer puro testeable** (`settings-content.ts` ~280 líneas con CSP+nonce+escapeHtml anti-inyección) + **provider con message passing** (`settings-view.ts` ~270 líneas). Las API keys NUNCA cruzan al webview en texto plano (state = `configured: boolean` por provider). `agents-yaml-writer.ts` con `agentsYamlHasComments` (special-case del header autogenerado) + `renderAgentsYaml` serializer. **Multi-provider FUNCIONAL end-to-end vía CLI y vía UI extension**. Deps workspace `@synaptic-sentinel/core` + `@synaptic-sentinel/agents` + `js-yaml` agregadas. Commit feat `7198a2f`. `pnpm verify` verde 50 test files / **403 tests** (+28 nuevos). Smoke `vsce package` verde 1838 archivos / 3.16 MB (+80 KB vs DG-073 B) ✅
- **Cycle 68** — 📋 **Phase 11 sub-increment 6: ground truth dataset (AI-draft) para benchmark cross-provider** (DG-075 C): define + popula el dataset que DG-076 va a consumir para puntuar cada `{provider, model}` × agente. **27 entries** cubriendo 13 fixtures deliberadamente vulnerables (9 OpenGrep SAST JS/TS + 7 SAST Python + 1 Gitleaks Secrets + 2 Checkov IaC + 4 Vibe-Detect VibeCoded). Schema Zod `BenchmarkGroundTruthSchema` en `core` con 3 capas de PASS por finding (Triage = classification + minConfidence + requiredKeywords; Context y Remediation = keywords + forbiddenInSnippet por NEGATIVA). **TODAS las entries marcadas como `reviewedBy: "ai-draft"`** — disclaimer explícito en `tests/benchmark/README.md`: no autoritativo para claims externos hasta revisión humana. **SCA (Trivy) deferido honestamente a DG-076 dynamic enumeration** (1 finding por CVE — cardinalidad alta). 5 archivos: schema + JSON + README + 16 tests + re-export. Commit feat `b978701`. `pnpm verify` verde 51 test files / **419 tests** (+16 nuevos) ✅
- **Cycle 73** — 🛒 **Phase 12 opener — preparación marketplace para v0.3.0 publish (cierre PARCIAL)** (DG-080 B): abre Phase 12 — Marketplace launch v0.3.0 con la preparación COMPLETA del lado código pero deja `vsce publish` como step manual del usuario. El usuario eligió en sub-pregunta del DG la opción "runbook ahora + publish después yo mismo" (por preferencia operacional). **3 deliverables**: (1) `packages/vscode-extension/README.md` REESCRITO de v0.2.0-era a multi-provider explícito (~14 KB vs ~5 KB original) con tagline updated a "multi-provider Brain Layer ... with cost visibility per session" + tabla de 7 filas de providers soportados (Anthropic native + OpenAI + DeepSeek + Groq + Gemini + Mistral/Together/Fireworks/Perplexity/xAI + AWS Bedrock/Azure + Ollama/LMStudio/vLLM) + sección "Cost visibility" con ejemplo del summary block + sección "Configuring providers" con 3 paths equivalentes (IDE Settings panel + `.sentinel/agents.yaml` + CLI flag `--agent-provider`) + sección "Known Limitations" honesta con 4 items resumidos (gpt-5 reasoning tokens, Ollama RAM, free tier quotas, tokens proxy chars/4) que enlaza al CHANGELOG para el full set; command list actualizada incluyendo `Configure Brain Layer Providers`. (2) NEW `docs/PUBLISHING.md` runbook completo (~10 KB) con: prerequisitos one-time (Azure DevOps account + Marketplace publisher GoLab + PAT con scope `Marketplace > Manage` en "All accessible organizations" + verificación `vsce` installed), 4 steps de publish (set `VSCE_PAT` + `vsce publish --packagePath` + verify listing público + announce optional), guía para releases subsiguientes (v0.4.0+), tabla de 6 failure modes con causes + fixes (PAT expirado, Publisher not found, Version already exists, ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT, Manifest validation, Norton TLS interception), sección Unpublishing emergency-only con caveats de irreversibilidad parcial. (3) RE-EMPAQUETADO `synaptic-sentinel-0.3.0.vsix` (3.18 MB / 1838 archivos) con el README actualizado bundleado dentro — **SHA-256 nuevo `DA07CA79CB8632C19037C16AAA7175E9504EA727CBF223585AA35B0B29A22185`** DISTINTO del GitHub Release `v0.3.0` (`5EA050B1...`) que queda histórico inmutable, son artefactos divergentes por content del README, NO regresión. `pnpm verify` verde 56 test files / **463 tests** (mismo conteo — DG-080 no toca código). **DG-080 cierra PARCIAL**: preparación done, publish pendiente. El listing `GoLab.synaptic-sentinel` SIGUE 404 hasta que el usuario ejecute el runbook. **DG-080 modela patrón nuevo: cierre PARCIAL con hand-off explícito al usuario** — cuando el usuario ejecute `vsce publish` y verifique el listing, una sola Entry follow-up en BITACORA cierra Phase 12 completamente (no requiere nuevo DG — es continuación operacional). **Anti-optimismo ilusorio**: la publicación NO está hecha; no declaramos Phase 12 cerrada ⏳
- **Cycle 72** — 🚀 **Phase 11 sub-increment 10 (último): CIERRE de Phase 11 con release v0.3.0 multi-provider** (DG-079 A): cierre formal de Phase 11 — Multi-Provider Brain Layer entera (10 sub-increments DG-070 → DG-079, Cycles 63 → 72). Bump `packages/vscode-extension/package.json` `0.2.0` → `0.3.0` + CHANGELOG nueva entrada `[0.3.0] - 2026-05-24` con headline **"Phase 11 closes: SYNAPTIC Sentinel is now provider-agnostic by design"** + 3 sub-secciones `### Added` (Multi-provider Brain Layer: 3 adapters + `.sentinel/agents.yaml` + Settings panel in-IDE + `--agent-provider` flag + SecretStorage namespaceado / Cost visibility: summary post-triage + sub-comando `cost-history` + tabla `triage_token_usage` v5 / Cross-provider benchmark plumbing: `pnpm benchmark:run` + verbose mode + filters + ground truth ai-draft) + `### Changed` (4 items: OpenAI gpt-5* quirks, DeepSeek default, pricing.json→pricing.ts, BC v0.2.0 preservada) + **`### Known Issues` (6 caveats honestos)** + `### Notes`. `pnpm verify` verde 56 test files / **463 tests**. `pnpm -F synaptic-sentinel package` exitoso: **`synaptic-sentinel-0.3.0.vsix` (3.17 MB / 1838 archivos)**. Manifest validado al extraer: version `0.3.0` + publisher `GoLab` + license `Apache-2.0` + description "The vibe-coding security sentinel..." + `engines.vscode ^1.95.0` + `galleryBanner #1a1a2e dark` + 12 keywords (incluyendo `byok`, `llm-security`, `ai-coding`). **SHA-256: `5EA050B10358534647C46E6E3B6E845F9174463D85DE2770CE334B669994AC29`** (verificable contra el asset que GitHub Release expone via `digest`). Annotated tag `v0.3.0` + push origin + `gh release create v0.3.0` con asset `.vsix` adjunto y release notes basadas en CHANGELOG. **Phase 11 CERRADA**. **Anti-optimismo ilusorio MAXIMO**: la sección Known Issues lista los **6 caveats abiertos** explícitamente — (1) path leak en synthetic findings (`tests/.../fixtures/vulnerable/` delata el origen al LLM), (2) gpt-5* reasoning tokens consuming `max_completion_tokens=1024`, (3) Ollama RAM saturation con modelos pesados (Gemma 4 9.6 GB, gpt-oss:20b 13 GB) tras 2 horas de inference batch, (4) free tier quotas Groq+Gemini exhausted con un benchmark completo + recalibration, (5) tokens proxy `chars/4` (±15-20% vs `usage` real del provider — el contrato `LlmClient.complete()` no expone usage), (6) ground truth dataset `ai-draft` sin human-AppSec review. **v0.3.0 es honest, no perfecto**. La página marketplace `GoLab.synaptic-sentinel` sigue 404 hasta Phase 12 (vsce publish con PAT del usuario) — el homepage del repo apunta ahí declarativamente. **NO incluye `vsce publish`** (Phase 12) ✅ — **PHASE 11 CERRADA**
- **Cycle 71** — 💰 **Phase 11 sub-increment 9: cost visibility (tokens proxy + USD estimado) + persistencia colony.db v5 + sub-comando `cost-history`** (DG-078 B): materializa el value-prop multi-provider en el CLI. Cada `triage` ahora imprime al final un **summary block** con tokens consumidos + cost USD estimado + latencia promedio, agrupado por `(provider/model, agente)`. **Persistencia aditiva**: nueva tabla `triage_token_usage` (schema v5) con `triage_session_id` UUID + `scan_id` + `fingerprint` + `provider_label` + `agent_id` + tokens proxy + cost + latencia. **Sub-comando nuevo** `synaptic-sentinel cost-history [--limit N]` consulta histórico agrupando por `(provider, agent)`, default últimas 10 sesiones. **`TokenTrackingLlmClient` decorator** (`packages/agents/src/token-tracking-client.ts`) envuelve cualquier `LlmClient` y registra observations (`inputTokens` proxy + `outputTokens` proxy + `latencyMs` + `ok/errorMessage`) por call sin modificar el contrato `LlmClient.complete()`. **Single source of truth de pricing** migrada de `tests/benchmark/pricing.json` (DELETED) a `packages/core/src/config/pricing.ts` como const TS exportada (`PRICING_TABLE`) + función pura `estimateCostUsd(label, input, output, table?)`. El benchmark runner ahora importa de `@synaptic-sentinel/core` (mismo patrón que el manifest de scanners en DG-059). **Caveat anti-optimismo ilusorio activo**: tokens son **proxies** `chars/4` (heurística ±15-20% vs `usage` real del provider — el contrato `LlmClient.complete()` no lo expone por decisión deliberada de DG-073 B). Cost USD puede divergir; documentado explícitamente en summary post-triage + output de `cost-history` + `docs/colony-db.md`. **Tests**: 21 nuevos (14 pricing + 5 token-count + 7 token-tracking-client). `pnpm verify` verde 56 test files / **463 tests**. Build CLI bundle 354.9 KB → 357.6 KB (+0.8%). Schema migration aditiva — usuarios con colony.db v4 no requieren reset. **`docs/colony-db.md` actualizado** con nueva fila + sección "Cost visibility" con caveat. **Sidebar webview en extension (Option C) deferido** a sub-DG futuro post-Phase-11 ✅
- **Cycle 70** — 🔬 **Phase 11 sub-increment 8: post-mortem del primer benchmark real + 5 surgical fixes + verbose mode + ground truth recalibration** (DG-077 A): ciclo extenso con 4 corridas reales del benchmark contra las 5 API keys del usuario (Anthropic + OpenAI + DeepSeek + Groq + Gemini) + Ollama local. El PILOT real expuso 4 hallazgos empíricos. **5 fixes aplicados**: (1) `OpenAiCompatibleLlmClient` switch automático `max_completion_tokens` para gpt-5* + omit `temperature` (2da iteración: gpt-5* solo acepta default=1); (2) `response_format: { type: 'json_object' }` siempre — mejora Gemini JSON adherence sin dañar otros; (3) DeepSeek default `v3.2`→`v4-flash` + `pricing.json` actualizado; (4) `BenchmarkGroundTruthSchema` extendido con `KeywordOrAlternativesSchema` (`z.union(string, string[])`) + scorer trata arrays como OR-groups de sinónimos; (5) **26 entries del ground-truth.json recalibradas** — keywords conceptuales (`code injection`, `XSS`, `SQL injection`, `command injection`, `deserialization`, etc) ahora arrays con sinónimos comunes (`RCE`, `cross-site scripting`, `SQLi`, `shell injection`, etc). **Verbose mode extension** (pedido del usuario tras saturación de su equipo en batch sin visibilidad): 3 flags nuevos al runner — `--verbose` (1 línea por LLM call con provider/agent/ruleId/PASS-FAIL-ERR/JSON-NOJSON/latency/200-char raw sample), `--entries <ids>` (filtra ground truth), `--providers <names>` (filtra providers). Documentación completa en `tests/benchmark/README.md`. **Resultados empíricos 3rd run cloud-only**: Anthropic Haiku Context PASS 2.6%→**46.2%**, Remediation 1.3%→**25.6%** (recalibration FUNCIONÓ). DeepSeek v4-flash debut: 25.6% Triage / 53.7% Context / 53.7% Remediation, JSON validity 85-100%, **$0.0092 USD** — best cost/performance cloud por amplio margen. Gemini cuota renovada: 52/52 runs / 24 errors (46% error rate vs 99% del PILOT). OpenAI gpt-5-nano 100% errors persistentes (reasoning tokens caveat). Groq+Gemini free tier quotas exhausted tras 2 runs. Ollama Gemma 4 9.6 GB saturó RAM del usuario tras 2 horas. **🔥 HALLAZGO DE RAÍZ vía `--verbose` probe** Anthropic + `sentinel-js-eval-usage`: el LLM hace meta-razonamiento sobre `Finding.location.path` — ve `tests/.../fixtures/vulnerable/` y clasifica como `inconclusive` (no `true_positive`) — root cause del 1.3% Triage PASS persistente de Anthropic. **Path leak queda como sub-DG futuro** documentado en README. 4 caveats abiertos documentados: path leak, gpt-5 reasoning tokens, free tier quotas, Ollama RAM con modelos pesados. `pnpm verify` verde 53 test files / **442 tests**. Reportes committeados: `v0.3.0-PILOT.md` (evidencia diagnóstica con 4 findings) + `v0.3.0.md` (3rd run post-fixes con Anthropic + DeepSeek útiles + Gemini parcial). **Anti-optimismo ilusorio MÁXIMO**: NO declara benchmark cross-provider operativo, declara plumbing battle-tested + 2 providers medidos útilmente + verbose mode reusable + 4 caveats abiertos ✅
- **Cycle 69** — 🧪 **Phase 11 sub-increment 7: cross-provider benchmark plumbing con `--dry-run` validado, ejecución real diferida al usuario** (DG-076 B): implementa el benchmark COMPLETO de extremo a extremo pero lo entrega validado solo con `MockLlmClient`s deterministas; la corrida real contra providers cloud queda diferida hasta que el usuario provea API keys y/o tenga Ollama corriendo (Gemma 4 con ~45 min ETA al momento del DG — el usuario clarificó que no encontró Mistral-Nemo y descargó Gemma 4 en su lugar). **6 archivos clave**: `packages/cli/src/benchmark/{scoring,report,run}.ts` (~200 / ~150 / ~280 líneas) + `packages/cli/tests/benchmark/{scoring,report}.test.ts` + `tests/benchmark/pricing.json` + `docs/benchmark/v0.3.0-DRAFT.md` (smoke output con disclaimer fuerte _"internal-comparative only / Do NOT cite externally"_). Script raíz `benchmark:run` en `package.json`. `scoring.ts` define funciones puras `triagePass` / `contextPass` / `remediationPass` + `buildSyntheticFinding` (sintetiza un `Finding` desde la entry sin correr scouts) + `scoutIdFromCategory` exhaustivo incluyendo `BusinessLogic`. `report.ts` define `BenchmarkReport` / `ProviderResult` / `AgentRollup` + `renderBenchmarkReport` (markdown con disclaimer si todas las entries son `ai-draft`, tablas con PASS rate / JSON validity / latency / p95 / cost / determinism + sección "Providers not run"). `run.ts` orquesta con `--dry-run` usando mocks deterministas + resolución de env vars `SENTINEL_<PROVIDER>_API_KEY` con fallback `ANTHROPIC_API_KEY` + cálculo de costo USD + token-count chars/4 (proxy ±20% documentado) + determinism cross-runs (% de pares de runs que matchean). **`pnpm verify` verde 53 test files / 442 tests (+23 nuevos)**. **Smoke `--dry-run`**: 26 entries × 3 runs = 78 invocaciones por agente, 0 errores, <1s total. Commit feat `a484935`. **🔴 ANTI-OPTIMISMO ILUSORIO MÁXIMO**: el `100% PASS / 100% JSON validity / 0ms latency / $0 cost` del reporte ES MATEMÁTICAMENTE CORRECTO PERO EMPÍRICAMENTE VACÍO — los mocks devuelven respuesta perfecta por construcción. El reporte es smoke test del plumbing, NO benchmark de calidad. El benchmark REAL lo corre el usuario con `SENTINEL_<PROVIDER>_API_KEY` setados ✅

## Tomo 001 — CERRADO

- **Cycles 1-50** · 2026-05-20 → 2026-05-22 · 50 ciclos, 100% de éxito · 58 decisiones (DG-001…DG-057 + Q1)
- Archivo: `tomes/tome-001.json` + `tomes/tome-001.md`

## Estado del repo

- 102 commits + tag `v0.2.0` · `origin` → `github.com/golab-arch/synaptic-sentinel` 🌐 **PÚBLICO** (DG-068 B · Apache-2.0 detectado por GitHub · 10 topics) · 🚀 **GitHub Release `v0.2.0`** con `.vsix` descargable (DG-069 B) · push por ciclo activo
- **Licencia unificada Apache-2.0** (DG-066 B — `LICENSE-PRO` eliminado, `packages/agents` re-licenciado) · **Producto íntegramente en inglés** (FI-011 cerrado) · **cache de scanners global operativa** (FI-004 cerrado)
- **5 scouts**: OpenGrep (**17 reglas SAST** — 11 pattern-based + 6 taint, JS/TS + Python) + Gitleaks + Trivy + Checkov + Vibe-Detect + `colony.db` (v4, **node-sqlite3-wasm**) + `Coordinator` + `reporters`
- CLI: `scan` (export **JSON / HTML / SARIF**, **`--fail-on`**) · `triage` · `mark-fp` · `scanners install [--global]`
- **Brain Layer (Apache-2.0)**: 3 agentes via **`@anthropic-ai/sdk` oficial** detrás del contrato `LlmClient` · BYOK Anthropic (clave del usuario en `vscode.SecretStorage`)
- Extensión VSCode: comandos/hover/Code Actions/status bar/pseudoterminal/webview + comando "SYNAPTIC Sentinel: Install Scanners" turnkey; **`.vsix v0.1.0` marketplace-ready** (`synaptic-sentinel-0.1.0.vsix`, 1.27 MB, id `golab.synaptic-sentinel`, logo OFICIAL GoLab + galleryBanner alineados con la familia SYNAPTIC) · bump a v0.2.0 + regenerado en DG-069
- **Brain Layer COMPLETO** · **UX verbose COMPLETA** · **memoria del enjambre COMPLETA** · **CI-native COMPLETA** · **Distribución COMPLETA** · **Detección con taint COMPLETA** · **Cliente LLM oficial COMPLETO** · **Marketplace polish COMPLETO** · **Sustrato legal unificado COMPLETO**
- `verify` (format:check / lint / build / **test:unit**) verde · 313 tests + 3 gated (302 unit / 11+3 integration)

## Notas / deuda

- 🔀 **Phase 9 abierta** — pivot estratégico a **OSS full Apache-2.0** ratificado por el usuario (DG-066 B). El producto se reposiciona como **"the world's best security sentinel for vibe-coded projects"** con todas las capacidades en un solo SKU bajo Apache-2.0 (la "capa premium" deja de existir como tier diferenciado). Monetización **diferida** (sponsors / consulting / hosted version a posteriori).
- 🏁 **`futureImprovements` sigue vacía** — cero deuda técnica registrada.
- **DG-001 B amendado** — `publish-oss.ts` allowlist obsoleto bajo el giro (no hay código Pro que filtrar). La decisión arquitectónica fue correcta para la estrategia de entonces; el cambio es estratégico, no arquitectónico.
- 🏁 **Phase 9 (Strategic Pivot) CERRADA** en 4 sub-increments balanceados (DG-066..DG-069). El producto unificado bajo Apache-2.0 con posicionamiento "The vibe-coding security sentinel" es **real, público, descargable e instalable** desde el repo público (independiente del marketplace).
- 🧭 **Phase 11 (Multi-Provider Brain Layer) ABIERTA** en Cycle 63 (DG-070 A) — el producto se reposiciona como **provider-agnostic-by-design**. Roadmap formal: 10 sub-increments (DG-070..DG-079) para extracción de adapters (OpenAI-compat + Ollama con XGrammar) + config registry YAML + UI panel + ground truth + benchmark empírico + prompt tuning + cost visibility + release v0.3.0.
- **Phase 10 (vsce publish v0.2.0 Anthropic-only) DEFERIDA y renumerada como Phase 12**: el primer screenshot del marketplace debe ser coherente con el posicionamiento provider-agnostic-by-design, no Anthropic-only. Marketplace listing `GoLab.synaptic-sentinel` debut con v0.3.0 multi-provider.
- **Anti-optimismo ilusorio activo**: DG-070 NO toca código del producto. El `.vsix v0.2.0` ya publicado como GitHub Release sigue intacto. Phase 11 va a tomar ~10 ciclos (3-4 semanas en la cadencia establecida). El benchmark empírico (DG-076) puede revelar que algún provider necesita re-tuning de prompts (PromptBridge probó 20-30% degradation sin esto).
- **Instrucciones permanentes**: cada DG incluye mi recomendación explícita (DG-045); commit + push por ciclo (DG-055).

## Decision Gate abierto

- **🏁 Phase 12 sigue CERRADA. DG-086 A CERRADO en Cycle 78. gpt-5 reasoning budget landed; 3 de 6 caveats heredados de v0.3.x siguen abiertos (50% cerrados).** Próximo DG a presentar al usuario (3 opciones):
  - **Option A (Recommended)**: sub-DG **Ollama RAM warning + docs** — el caveat documenta que Gemma 4 (9.6 GB) y gpt-oss:20b (13 GB) saturan RAM del usuario tras ~2 horas de inference batch. Settings panel del VSCode extension muestra tamaño del modelo seleccionado + warning visual si > 5 GB. Docs README de la extension agrega tabla recomendada con modelos ≤3 GB (gemma3:4b / qwen2.5-coder:7b). UX work fino pero scope acotado. ~1 ciclo.
  - **Option B**: sub-DG **free tier quotas handling** — el caveat documenta que Groq 100K TPD y Gemini RPM se exhaustaron tras 2 benchmarks. Agregar detección de error 429 con backoff exponencial + marker `quota-exhausted` en el reporte del benchmark. ~1 ciclo.
  - **Option C**: pausar el proyecto con SYNAPTIC Sentinel v0.3.3 live + verify gate fortalecido + 3 de 6 caveats cerrados como hito final temporal.

## Last Entry

Entry #96 — FEATURE_IMPLEMENTED (DG-086 A) — 2026-05-26 — SUCCESS · gpt-5* reasoning tokens budget fix. NEW constante `DEFAULT_MAX_TOKENS_GPT5 = 8192` en `OpenAiCompatibleLlmClient`. Lógica del `completeWithUsage` actualizada: `const tokens = request.maxTokens ?? (isGpt5 ? DEFAULT_MAX_TOKENS_GPT5 : DEFAULT_MAX_TOKENS)`. Override del caller respetado; non-gpt-5* models siguen en 1024. Test existente actualizado de assert 1024 → 8192 + 2 tests nuevos (anti-regresion override caller + regresion guard non-gpt-5). pnpm verify VERDE 485 tests (+2) + 2 gates. Cierra 1 de los 4 caveats heredados de v0.3.x Known Issues (gpt-5 reasoning tokens). Anti-optimismo: el impacto empírico (gpt-5-nano deja de devolver content=null) sólo se mide corriendo el benchmark contra OpenAI con keys del usuario — diferido honestamente. Cost del cap ≠ cost real (el provider cobra por tokens generados, no por el cap). NO bump version del .vsix v0.3.3 · synapticStrength 85

---
