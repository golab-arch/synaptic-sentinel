/**
 * @synaptic-sentinel/vscode-extension  [MIXTO OSS/Pro]
 *
 * Extension VSCode: shell OSS (diagnostics, hover, code actions, status bar,
 * tree view del tomo) + features Pro detras de feature-flags. BYOK via
 * vscode.SecretStorage. Implementacion en la fase Foundations / Inline UX.
 *
 * Nota: cuando se implemente requerira @types/vscode, un bundler (esbuild)
 * y @vscode/vsce; el manifest se completara entonces.
 */

export const PACKAGE_NAME = '@synaptic-sentinel/vscode-extension';
