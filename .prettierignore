dist/
coverage/
node_modules/
.scanners/
pnpm-lock.yaml
.synaptic/
context/

# Fixtures de tests y reglas de scanners: se mantienen tal cual se escriben.
**/tests/**/fixtures/
**/opengrep/rules/

# Reportes generados por `pnpm benchmark:run` (markdown del renderer; las
# tablas largas chocan con max-width de prettier — mismo patron que
# .synaptic/BITACORA.md / la documentacion legacy).
docs/benchmark/

# Documentacion tecnica externa redactada por el usuario; se preserva la
# forma original sin pasar por prettier (mismo patron que docs/benchmark/).
SYNAPTIC_SENTINEL_TECHNICAL_REFERENCE.md
